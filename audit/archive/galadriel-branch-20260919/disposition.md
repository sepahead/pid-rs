# Galadriel guide branch: historical archive

This packet preserves the complete two-commit history of
`sepahead/galadriel-placement-guide-v1` at
`75a7acaa3f9432dc323be7e49f5eee1f9af781fd`, above prerequisite
`718447aa2acc6600a3bdce1d81cda0dba4f4ab3b`. Its immutable status is
`historical_archive_with_local_recovery_evidence`. Publication and any later retirement are
recorded separately; this dated receipt grants no deletion authority.

The [bundle](galadriel-history-from-718447aa.bundle) is **1,114,215 bytes**, SHA-256
`dab1653d8b7efbe6d38d6282ce74855d0cbb5adc8fe1763ee8b9c06c32d199c6`.
Its one advertised ref is the original tip; its sole prerequisite is the named ancestor.
Native indexing confirms 39 original objects and 16 thin-delta bases. All 16 bases are reachable
from the prerequisite; the bundle supplies the additional branch objects.
The [manifest](manifest.json) binds both commits/trees, all 39 raw objects and 23 changed versions.
The exact-log/PID2 archive and its own prerequisite remain separate and unchanged.

## Exact successors and preserved historical bytes

Eighteen versions have exact same-path successors in main ancestor
`dbe7c11b40dda3110cbd11e769e64524d5bde1ea`: the complete guide, PDF, six figure assets and
asset manifest, latency receipt/raw archive, publication sources and check wiring. The remaining
five blobs are four earlier supporting documents and a 250,315-byte historical source-state
inventory. All 23 are also recoverable from this bundle and its prerequisite.

The four old documents are `AGENTS.md`, `CHANGELOG.md`, `README.md` and `scripts/README.md`.
Their guide-related added/removed lines agree with adopted main work, while their complete bytes
are different. A previous attempt to reconstruct them using later-main diff contexts was refused
as nonunique. That attempt remains a failure. This later native fetch recovered all four complete
blobs and checked their ordered changes; no semantic equivalence was substituted for byte custody.
The generated state preimage is preserved unchanged as historical inventory, not regenerated or
represented as a current execution receipt.

The [current guide](../../../PID_SENSOR_PLACEMENT_AND_GALADRIEL_GUIDE.md) retains its
current-versus-proposed capability, support and estimand boundaries. The
[latency receipt](../../evidence/categorical-pid-latency-718447aa-explicit-20260830.json) and
[raw archive](../../evidence/categorical-pid-latency-718447aa-explicit-20260830.tar.gz) record a
warm Criterion microbenchmark under a particular environment and configuration. Their preservation
does not establish generic latency, estimator calibration, actual sensor-placement value or a
deployed Galadriel integration. Archived scripts and instructions remain inert; older presentation
bytes do not replace corrected active publications.

## Native recovery and later publication gate

A fresh empty repository correctly rejected the missing prerequisite. After importing only main
at `959d91e`, it contained 8,717 objects and lacked all 14 objects unique to the branch. Recovery
from a copied bundle yielded 8,731 objects. All 39 raw delta objects, 23 changed versions, both 1,035-file tree
rosters, the four old documents and the state preimage matched. `git fsck --full --strict` passed;
no alternates, shallow boundary or shared hardlinked objects were used. No archived program ran.
This was a same-host native recovery drill, not retrieval from a published remote-main commit.

After the permitted archive is committed to accepted remote main, retrieve its exact bundle Git blob
from the containing commit and repeat the [recovery procedure](recovery.md). A separate receipt
must bind those publication facts, required current-main checks and any later expected-old lease
and post-removal reachability. Fresh process/worktree ownership and uncovered-byte checks remain
necessary before deleting a remote branch. This record is not rewritten to assert future events.
