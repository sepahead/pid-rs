# Archive status

The archive preserves two distinct historical layers. Existing proof-author fragments, publication preimages, and failed-development material retain their prior locations and dispositions. The nested `predecessor-package-2026-09-08/` directory additionally preserves the exact manifest, source-public map, editorial record, status, and every Markdown guide displaced or superseded by the current package candidate.

Read `predecessor-package-2026-09-08/HISTORICAL_ROOT.json` before interpreting those files. The archived package manifest and source-public map use paths relative to the former `audit/formal/lean-prefix-mgw-bias` root. They do not describe the nested archive directory and are not a current package validation.

Archival copying changes no historical status. A stored source, receipt projection, review, failed attempt, or guide grants no current proof, execution, production, hosted, or publication credit.
