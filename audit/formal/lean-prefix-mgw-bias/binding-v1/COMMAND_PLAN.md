# Future commands — unexecuted and not adopted

This source stage authorizes no runner, control, Lean, kernel or PDF invocation. First independently review the exact source diff, forty-three adapter input hashes, fifty-one control input hashes and complete source proposal manifest. The current identities are:

- Adapter: `127ffdc88b8174bdf8ab2b1027510faf4dd6516c6c01e6fea988866825e5868b`.
- Adapter manifest: `6783dffce953c0e6dd812658af049770b2e63889807acb34f11c75eb4d577b10`.
- Bias supervisor: `68b66b0635023d6f67dc2a813fdda48f07095f416a86b7d7aad0b32827b35293`.
- Control input pins: `08615604ff3db35cce59f0c5ed005e153667e17f499f01e84bf9551d3c0651cc`.
- Unchanged Runtime: `bd8a9f2272a20422863c9902ce2148d2957471bb958d13949fece923cb6a7f5d`.

The commands below are for a later root-owned stage. Each referenced registration is an actual separately authored, reviewed and hash-bound input, not this source registration or a synthetic control receipt. The exact root, stage and tool prefix must be canonical directories. Materialize every declared input byte, including the six fixture files under the hidden `.lake` path; a Git-visible subset is incomplete. Require ordinary single-link files. No downloaded tool, omitted file or ambient cache is silently substituted.

## Operational controls first

Use a control registration of schema `portable-bias-adapter-controls-execution-v1`, status `root-reviewed-source-ready-for-controls`, exact adapter digest, integer zero `new_bias_theorems_proved`, actual aware-UTC start/deadline of at most 45 minutes, and `source_sha256` for `adapter-controls.py` and `control-child.py`. Register at most two harness invocations under one original clock, normal then optimized. The harness internally exercises child modes 0 and 1; each invocation's source inventory is 106 case definitions, 212 mode cases and two shared-stage rows, totaling 214 rows. Those are planned control rows, not observed successes.

Use a separate supervisor-control registration of schema `portable-bias-supervisor-controls-execution-v1`, the same ready-for-controls status, integer zero proof credit, exact hashes for `supervisor-controls.py`, `stage-supervisor.py`, and `supervisor-child.py`, and one original interval of at most 15 minutes. Register at most two outer invocations, normal then optimized. Each internally exercises 12 cases in two child modes: 24 rows. A fresh output directory is required for each invocation.

```text
python3 -I -S -B <package>/portable-controls/proposed/adapter-controls.py --registration <adapter-control-registration> --registration-sha256 <actual-sha256> --output <fresh-ignored-normal-output>
python3 -O -I -S -B <package>/portable-controls/proposed/adapter-controls.py --registration <same-adapter-control-registration> --registration-sha256 <same-sha256> --output <fresh-ignored-optimized-output>
python3 -I -S -B <package>/portable-controls/proposed/supervisor-controls.py --registration <supervisor-control-registration> --registration-sha256 <actual-sha256> --output <fresh-ignored-normal-output>
python3 -O -I -S -B <package>/portable-controls/proposed/supervisor-controls.py --registration <same-supervisor-control-registration> --registration-sha256 <same-sha256> --output <fresh-ignored-optimized-output>
```

Inspect every actual case, dispatch, stream and deadline cause. Synthetic `fresh_semantic_kernel_pass` strings are control transport only: the driver prohibits external process execution and creates labelled fake objects. Supervisor controls use inert Python children only. None can serve as live proof evidence. Preserve failures and do not manufacture adoption. Actual normal/optimized policy/helper controls for the unchanged source families remain separately scoped evidence; review their prior exact receipts and any required current reuse gate. They do not automatically cover this binding.

## Production replay after actual control adoption

Root must author an actual `CONTROLS_ACCEPTANCE.json` satisfying the exact template and bind its digest into a fresh production registration. Use the registration template's six attempt slots, one wrapper, 186 compiler/kernel ceiling and six probes. Register once for 120 minutes under `<root>/.local/prefix-mgw-bias-public-replay-v1/<fresh-lowercase-stage>`. Tool hashes must be freshly observed for real `lean` and `leanchecker` binaries under the reviewed tool prefix; neither inert binary fixtures nor old hashes prove a current installation.

The root-owned supervisor plan has schema `portable-bias-supervisor-plan-v1`, status `root-reviewed-ready-to-execute`, canonical `root`, `stage`, `adapter`, `toolchain_prefix`, the actual `registration_sha256`, and the exact `supervisor_sha256` above. It uses the production branch. The supervisor executes exactly this ordered matrix in one original stage:

| Order | Candidate | Python | Action | Compiler/kernel children |
|---|---|---|---|---|
| 1 | Selected | Normal | Full check | 31 |
| 2 | Selected | Optimized | Full check | 31 |
| 3 | Cosmetic | Normal | Full check | 31 |
| 4 | Cosmetic | Optimized | Full check | 31 |
| 5 | Selected | Normal | Wrong final target after actual earlier selected pass | 29 |

```text
python3 -I -S -B <package>/portable-controls/proposed/stage-supervisor.py --plan <reviewed-production-plan> --plan-sha256 <actual-plan-sha256> --output <fresh-ignored-supervisor-output>
```

Only after the actual five wrappers close on time and root verifies every receipt and command/stream/source/object artifact may a separate replay disposition be authored. The supervisor terminal status itself is explicitly not adopted. Required new hosted execution, publication integration, source/package checks and local/remote main remain separate. This source proposal cannot satisfy any of those steps.
