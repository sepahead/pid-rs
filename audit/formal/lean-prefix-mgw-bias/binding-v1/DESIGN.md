# Bias portable binding revision 1 — source proposal

This project-defined adapter proposes fresh replay of the five already locally accepted finite categorical MGW bias families. It supplies no new formal execution or proof credit. The runtime, four source policies, all thirty Lean modules, two candidate variants, and existing helper/dependency pins retain their exact source identities. The mean adapter and supervisor remain unchanged at their original locations.

The accepted subject is a fixed categorical MGW atom, fixed raw horizon, and iid complete source-and-target rows; dependence within a row is allowed. A block has a fresh anchor and that horizon of further rows. Quantities are in nats. The signed error is the population atom minus the expectation of the native block statistic; conventional statistical bias has the opposite sign. The exact source statement and targets govern, including positive-support restrictions and any population floor premise. This transport package does not change those premises.

The historical campaign acceptance is `e46380b7a2b38cf4969c1996a5256ae7e6bacc4397458bdec9dec41dcadba698`. The exact selected candidate is `c945862f2bb64825ad5b754cd6fcd4f32da02c0377f13074d9192b6568e3fb45`; cosmetic candidate is `ac65476b7a101d344fff4fe0f4a9370ef7438b83e63d3854883e205c5ee33481`. A historical receipt stays historical and cannot satisfy a newly registered stage's prior-pass requirement.

The exact ordered exports are `actual_finite_bias`, `fiber_inverse_moment`, `support_moment_bounds`, `projected_support_bias`, and `mass_floor_bias`. Their declaration names are in `PidPrefixMgwBiasCandidate`; raw and alias targets are in `PidPrefixMgwBiasRawTargets` and `PidPrefixMgwBiasAliasTargets`. Fiber inverse moment has universe parameters `[u,v]`; the other four have `[u,v,w]`. Exact elaborated types and axiom arrays appear in the expected record input. Permitted proof axioms remain `Classical.choice`, `Quot.sound`, and `propext`; `sorryAx` is excluded. The fresh semantic judge checks both raw and alias types and the exact public export roster.

## Explicit binding and complete graph

The mean supervisor hardcodes its original adapter and 48-input manifest. This revision has a separate bias schema and exact adapter/manifest pins. Its production branch always selects that bias adapter. Its control branch selects only the bounded inert supervisor child; it is never used to admit a real compiler.

The supervisor process-observation, session/group, signaling, stream caps, cleanup reserve and finalization code is retained. Its changes are domain/pin literals and strict duplicate-key/nonfinite JSON decoding at four existing admission/receipt reads. There is no new scheduler, plugin mechanism, generic binding dispatcher or process supervisor implementation. This changed binding still needs independent source review and actual controls.

`SOURCE_GRAPH.json` supplies twenty-four dependency modules, three bias type modules, bias candidate, semantic judge and `BiasCandidateRoot`: thirty modules total. Every positive run freshly compiles all thirty, then invokes `leanchecker --fresh BiasCandidateRoot`. The dependency semantic judges must emit exactly eleven MGW, six prefix-probability and three mean records. The bias judge must emit five exact records. Package library objects remain external pinned dependencies; fresh project compilation is not source-to-dependency-object authentication.

The adapter binds exactly forty-three package inputs. Its manifest excludes downstream controls, supervisor and the manifest itself to keep the hash graph acyclic. A separate fifty-one-member control input manifest includes the forty-four-file adapter package, six exact helper/toolchain/import fixture inputs and the mean adapter retained as inspection text for one rejection control. The complete proposed-file manifest binds all of these layers. Input hashes do not establish authenticity or mathematical truth.

## Resources and ordering

Each positive run has 31 compiler/kernel children and one tool-version probe. Wrong-final has 29 compiler children and one probe, ending at the bias semantic judge; no root compiler or kernel follows that rejection. The required four positive runs plus one wrong-final require 153 compiler/kernel children and five probes.

Six attempt slots and six probes are retained from the mean route. The bias compiler/kernel ceiling is explicitly derived as `6 * 31 = 186`, because the mean ceiling 150 cannot hold even the required five-run bias matrix. This change accounts for the larger fixed graph; it does not waive a failure or lower a proof predicate. Preparation-only and failed invocations consume attempt slots. A failed campaign or expired original clock requires a separately reviewed successor; retries cannot reset clocks or erase evidence.

A live stage has one original aware-UTC 120-minute interval, one execution wrapper, and a supervisor with a 45-second cleanup reserve. Each compiler receives `-t 0 -M 4096`; Runtime observes a 4-GiB process-group RSS bound and 4-MiB caps for each stream, with a 600-second child ceiling and 60-second version-probe ceiling, each intersected with remaining stage time. Existing operating-system and cooperative-enforcement limitations remain explicit in TRUST_BOUNDARY.md.

## Negative control

The selected candidate's final public declaration is replaced by exactly `theorem mass_floor_bias : True := by trivial`. The adapter checks the resulting bytes against the archived wrong-final candidate, runs the accepted final-control source policy, and requires a same-stage earlier non-preparation selected full-pass receipt with identical original bytes and exact typed registration and semantic records. It then requires nonzero semantic-judge exit without an operational failure or stderr, both named last raw/alias target diagnostics, `DNF_JUDGE_TARGET_TYPE`, type mismatch text, and the first four exact accepted records. Generic failure, timeout, missing input or wrong source cannot satisfy that predicate. Root must review the actual prior and negative command/stream/object evidence before adoption.

The original ten campaign attempts, failed development routes and original source-author interruption remain preserved separately. This source package does not rewrite their status or claim a new proof from repeats, controls, portability or packaging.
