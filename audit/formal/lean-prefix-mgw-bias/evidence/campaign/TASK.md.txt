# Prospective exact finite-bias proof campaign

This package is a source-only proposal. No new bias proof, candidate, operational test, Lean child, or kernel result has been produced in its preparation. Root must inspect the exact sources, run actual operational controls in a new registered lane, and adopt that freeze before accessing any bias candidate.

The frozen public candidate exports, in order, are `actual_finite_bias`, `fiber_inverse_moment`, `support_moment_bounds`, `projected_support_bias`, and `mass_floor_bias`. The fiber theorem has two universe parameters; the other four have three. The source contract, raw types, alias types, prepared reporter and source policy are copied unchanged from the sealed type-preparation packet. Allowed proof axioms remain `Classical.choice`, `Quot.sound`, and `propext`. The six other reviewed statements remain helper obligations; they are not additional accepted exports.

The estimand is the native finite-product expectation of the complete-row iid prefix statistic and the actual finite categorical MGW atom. Coordinates of each row may depend. The contract retains explicit population laws, support conditions, native source groups and target-retention rank. No desired bias premise, surrogate integral, nominal/observed support replacement, empirical plug-in transfer, continuous-PID claim, or estimator-superiority claim is introduced here.

Historical source/object correspondence is explicit. Development imports 24 historical accepted mean-closure objects and three previously elaborated bias type objects. Full runs freshly compile all 27 source modules, the new candidate, its semantic judge, and the final root: 30 compiler children plus one `leanchecker --fresh BiasCandidateRoot` child. The wrong-final control compiles 29 modules and must reject the final `mass_floor_bias` type while retaining the first four exact accepted records. Development uses one compiler child and cannot accept a proof family.

The prospective live interval is 720 minutes, 80 development slots, eight full slots (including wrong-final), one wrapper at a time, and graph-derived per-attempt child ceilings. Every child retains the inherited 600-second ceiling, remaining-stage ceiling, sampled 4 GiB group RSS policy, 4 MiB stream caps, process-group cleanup and command/source custody. The root chooses whether to adopt these proposed bounds before the stage starts; an adopted stage cannot reset its clock or ledger. STOP and deadline are checked before children and after final evidence preservation. The operational stage is separately registered for 35 minutes and cannot access a candidate.

Complete campaign acceptance requires identical five-record semantic results for selected and cosmetic source in normal and optimized Python, a fresh complete kernel pass for each, and an actual wrong-final rejection. The selected source is the first chronological complete full pass. Cosmetic bytes are precisely the selection-rule prefix plus selected bytes plus its suffix. Four positive runs and the negative control count as one five-family acceptance, with zero extra dependency families. The whole project remains separately active after this campaign closes.

This successor repairs independently identified operational source blockers. Its four control families (35 original entrypoint, 57 exact policy, 35 instrumented attempt, 35 strict closure cases per mode) must all have actual normal/optimized receipts before root adoption. Production entrypoints reject pinned synthetic control markers. No prior source-only case is relabeled as executed or as a proof. The exact finite-bias contract and all five formal target/source objects remain unchanged.
