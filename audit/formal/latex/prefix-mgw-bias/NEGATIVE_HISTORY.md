# Retained bias publication failures

The earlier production stage closed after all eight permitted productions on 8 September 2026. Its raw 1,340-member manifest has SHA-256 `db6cef1c85fa58353b40b56da146d1b0165a3fe458592d2826ec8a09b69e3232`. The original stage is not reopened by this public source proposal.

The first full attempt had a finalizer failure and a Type 3 fallback. The initial summary renderer invocation failed before its child. Full02 exposed page-flow problems. Full04 and full05 produced identical bytes, SHA-256 `7c7ba1579f74b19f79250e1cc7b02fdf5afd69d8a343cd2591f1e54eae153bd9`, but fourteen of thirty bookmarks targeted wrong pages and some same-page anchors targeted the wrong vertical location. Those repetitions remain navigation-blocked. Their exact shared PDF is the [negative control fixture](controls/blocked-full-04.pdf).

The corrected full-kind derivative loads hyperref after the local styles and titlesec and protects eight short formal identifiers at nine occurrences. Corrected producer SHA-256 `5ab99a50b09f4d804373906041fc146ab48e9a1b320acf00ace701c642b6be77` produced full08 once. Recorded normal/high-resolution rendered review and all thirty destination observations accepted that artifact's visual/navigation scope. Corrected-full repetition remained open when the stage closed. Summary03, summary06 and summary07 shared the same accepted summary bytes. None of these old observations executes the new public builder.

Two reader mistakes are also retained. The initial object diagnostic rejected any OpenAction; [the exact adjudication](archive/open-action-reader-adjudication.json) identifies the own-first-page Fit action and does not admit scripts or external-file actions. The [initial coordinate reader](archive/navigation-reader-initial.json) incorrectly required section anchors for subsection headings and treated four font-metric boxes as visible ink. [Its correction](archive/navigation-reader-adjudication.json) preserves the original findings and records distinct level-appropriate anchors and the actual ink observations. The current navigation contract compares each reviewed exact tuple instead of widening a generic coordinate threshold.

All ten canonical Markdown/SVG/style/template/filter/log inputs stayed unchanged through the final derivative repair. At that source-preparation stage, the 1,207-file bias package and its manifest SHA-256 `0a26c5ef0ea190ca03f680c493b6ef70d09240b8195bc1beda3e187f7abc5af4` were unchanged. That historical identity does not describe the later expanded package; the [current package manifest](../../lean-prefix-mgw-bias/PACKAGE_MANIFEST.json) lists its current members. Full private process and production records remain separately retained; their hashes do not establish off-host preservation or reproduce omitted records. Reconsider the selected route if any canonical input, protected proof input, producer admission, expected artifact or actual navigation observation changes. No old passing run supplies new-commit hosted credit or a new scientific result.

## URI action reader rejection

The first portable full build reproduced the reviewed 370,867-byte PDF exactly, then failed at the URI-action reader. Each of its 17 HTTPS action dictionaries contains `/Type /Action`; the reader incorrectly required only `/S` and `/URI`. The failed stage stopped after one started build and 21 primary commands. No summary ran and no complete reproduction was accepted. The correction requires the exact three-key Action shape and retains the URI inventory, forbidden-key checks and raw PDF identity. The original failed run remains evidence; a source correction alone supplies no production credit.

## Aggregate inventory ordering rejection

The first 12 September aggregate self-test rejected its baseline inventory because it compared sorted PDF filenames with sorted suffix-free stems. A hyphenated summary filename changes their relative order when the suffix is removed. The [failed-attempt record](archive/aggregate-stem-ordering/failure.md.txt) retains the exact diagnosis and ten correction routes; the [source preimage](archive/aggregate-stem-ordering/check-formal-pdf-set.before.sh.txt) retains the rejected comparison. The correction sorts validated stems before comparison. A fresh successor passed all 169 inventory and dispatch controls, plus the target-copy controller controls in both Python modes. This result covers the operational self-test; it does not record an actual full PDF aggregate run or a mathematical proof.
