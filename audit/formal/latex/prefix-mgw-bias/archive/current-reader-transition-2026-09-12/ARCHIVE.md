# Historical publication source bytes

These files are historical evidence. Do not run them as the current builder or controller. The current scripts use a later reader and control contract.

The [builder preimage](build-prefix-mgw-bias-pdf.py.txt) and [controller preimage](check-prefix-mgw-bias-pdf-self-test.py.txt) preserve the exact displaced source bytes. The [archive map](HISTORICAL_ROOT.json) records their former repository paths, byte counts and SHA-256 digests. Files end in `.txt` to keep the archive separate from current executable entry points.

The two documentation preimages preserve the earlier wording that described predecessor sources without including both source byte files in this public overlay, and that used an earlier package inventory as a current description. The corrected publication notes link here and distinguish that historical inventory from the current package. These corrections change preservation and documentation scope; they establish no new proof or production execution.
