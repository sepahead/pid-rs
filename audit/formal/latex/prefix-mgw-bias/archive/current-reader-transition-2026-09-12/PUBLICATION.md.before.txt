# Bias PDF source and reproduction contract

This package supplies the source profile for the [full bias paper](../../../../output/pdf/prefix-mgw-bias.pdf) and [one-page summary](../../../../output/pdf/prefix-mgw-bias-summary.pdf). The canonical [exposition](../../lean-prefix-mgw-bias/EXPOSITION.md), [summary](../../lean-prefix-mgw-bias/SUMMARY.md), three SVG figures, two local styles, template, filter and strict log checker are the ten document inputs. The corrected Markdown remains outside the unchanged accepted 43-input bias and 48-input mean proof manifests. The [current publication manifest](publication-inputs-v2.json) binds the current bytes, expected derivatives and root-admitted corrected discovery references; it does not record a successful execution of this successor builder.

The [negative history](NEGATIVE_HISTORY.md) preserves the earlier production and navigation failures and the exact boundaries of their correction.

The [current local reproduction](CURRENT_REPRODUCTION.md) records the completed 12 September 2026 controls and eight exact bias-PDF builds. Its [typed evidence](CURRENT_REPRODUCTION.json) is separate from the immutable input profiles and historical discovery receipts.

The [builder](../../../../scripts/build-prefix-mgw-bias-pdf.py) uses the unchanged [mean replay runtime](../../lean-prefix-mgw-mean/replay-support/runtime.py) for child supervision and stable input capture. The [current native profile](native-tools-v2.json) binds the primary executable bytes, Python version and executable, five explicit font inputs, selected LuaLaTeX format, and the [current PDF reader source inventory](reader-source-v2.json). Machine-specific selectors belong in a fresh private execution registration. The public files use repository-relative source paths.

The admitted producer is the recorded Darwin/arm64 Pandoc 3.10.2 and LuaHBTeX 1.18.0/TeX Live 2024 route. The primary binaries and named inputs are pinned. This is a bounded exact-byte profile: it does not bind every dynamic library or the complete TeX distribution, and it is not a hermetic-execution or authenticity claim. The final raw PDF bytes must match the reviewed reference exactly. Unsupported producers fail closed; no normalization or alternate PDF equivalence is admitted.

The Python, process-observer and format pins originated in source preparation for the predecessor route. The current production separately captured and checked its actual inputs against those pins. Each future invocation must do the same. The current reader uses the separately pinned pypdf 6.16.1 source: it copies the 58 declared source files into a fresh private package and imports that copy under isolated Python without installed bytecode. The retained `reader-source-v1.json`, `native-tools-v1.json`, `publication-inputs-v1.json`, builder and control sources remain immutable predecessor evidence. The source inventory and bounded old/new compatibility comparison are separate from production acceptance; neither authenticates the package or replaces exact output checks.

## Corrected derivation and navigation

The original SVG, styles and template bytes remain unchanged. The corrected Markdown makes population and row assumptions explicit without changing the accepted mathematical targets. The full-kind derivative keeps the reviewed figure positions and three introduction/display groups together, uses the exact corrected F7 introducer, loads hyperref after the local styles and titlesec, and boxes eight short formal identifiers at their nine literal occurrences. The summary requires exactly one original paragraph-spacing declaration before changing 0.48em to 0.34em, retaining the template's 11pt type. Both the raw Pandoc TeX and the corrected final TeX must equal their stored expected bytes before LuaLaTeX runs. SVG subscript/superscript glyph conversion must equal each stored reviewed derivative before rsvg-convert runs.

The [navigation contract](navigation-v1.json) binds all thirty full-paper heading titles, levels, distinct named destinations, pages, destination coordinates and visible word-box coordinates. It also binds both readers' expected pages and occurrence counts for all eight protected identifiers. Each actual new full build must obtain fresh Poppler word boxes and layout text and a separate pinned pypdf observation. Generic bounding-box thresholds are not substituted for the reviewed per-heading tuples. The retained [initial coordinate reader](archive/navigation-reader-initial.json) and its [adjudication](archive/navigation-reader-adjudication.json) explain the distinction between font boxes and actual ink.

Each new PDF must retain the reviewed A4/PDF 1.7 profile, language, embedded/subset/Unicode font table and HTTPS URI inventory. Internal actions are restricted to the reviewed heading destinations and the exact own-first-page Fit opener. The [earlier opener adjudication](archive/open-action-reader-adjudication.json) preserves the initial broad-reader false positive. These finite object checks do not certify general viewer safety or accessibility. The PDFs are not tagged-accessibility publications.

## Separate execution stages

The current local controls and four bias producer invocations passed on 12 September 2026. Each future source revision requires the applicable [inert controls](controls/CONTROLS.md) after independent review, before admitting production. Preserve each actual receipt, failure and stream. A source correction after a failed registered attempt requires a newly reviewed bounded successor; it does not reopen the failed attempt or reset its clock.

The completed bias stage ran full normal, full optimized, summary normal and summary optimized under one original forty-five-minute deadline. Each invocation made two fresh builds with two LuaLaTeX passes per build. The direct-command budget was 44 per full invocation and 38 per summary invocation: 164 total. The mean paper has a separate control and reproduction stage; its two required invocations add four PDF builds and 20 direct commands. Internal process-group observations and dependencies are additional operating-system processes. For a new bias repetition, use the [full v2](execution-full-v2.template.json) and [summary v2](execution-summary-v2.template.json) templates, replacing unresolved values with actual reviewed selectors, roots, hashes, modes and aware UTC bounds. Each invocation must fit within forty-five minutes and its enclosing stage within ninety minutes. Bind original endpoints before launch and use fresh work directories. The external supervisor records outer exits, streams and bounded observations. Any aggregate repeat is an additional registered invocation.

The exact root must contain every manifest member, the builder and the unchanged runtime. Work directories must be fresh canonical POSIX paths whose parent exists; search-path separators and line breaks are outside this route. The configured reader directory is the canonical installed pypdf source package, not a new installation request. Keep output null during validation; reviewed output adoption is a later owner operation.

```text
python3 -I -S -B scripts/build-prefix-mgw-bias-pdf.py \
  --root <canonical-assembled-root> --kind full --exact --check \
  --registration <fresh-full-registration.json> \
  --registration-sha256 <actual-full-registration-sha256> \
  --work-dir <fresh-full-work-directory>
python3 -I -S -B scripts/build-prefix-mgw-bias-pdf.py \
  --root <canonical-assembled-root> --kind summary --exact --check \
  --registration <fresh-summary-registration.json> \
  --registration-sha256 <actual-summary-registration-sha256> \
  --work-dir <fresh-summary-work-directory>
```

Run the corresponding optimized invocations with `-O` and separate fresh registrations and work directories; set each registration's integer optimization field to match its actual interpreter flags. Mean invocations have their own recorded plan. No registration template constitutes an actual started stage.

The [aggregate PDF gate](../../../../scripts/check-formal-pdf-set.sh) includes both bias Markdown/PDF pairs. Exact mode requires thirteen external values: the selected `PID_RS_BIAS_PYTHON`; separate `PID_RS_BIAS_CONTROL_NORMAL_` and `PID_RS_BIAS_CONTROL_OPTIMIZED_` values ending in `REGISTRATION`, `REGISTRATION_SHA256` and `OUTPUT`; and separate `PID_RS_BIAS_FULL_` and `PID_RS_BIAS_SUMMARY_` values ending in `REGISTRATION`, `REGISTRATION_SHA256` and `WORK_DIR`. Create the two control registrations immediately before the aggregate with matching integer modes 0 and 1 and one original twenty-minute control window. The full and summary registrations use mode 0 and the production stage's original bounds. After inventory, the aggregate runs normal controls, optimized controls, full production and summary production before the longer existing gates. Child admission validates the contents and byte identities; the aggregate forwards values without manufacturing a registration or renewing a clock. Inventory-only and cross-toolchain requests need none of these values. The aggregate's mean/full/summary calls add six PDF builds and 92 direct commands; its other publication gates have separate accounting. Its two fresh inert-control invocations are additional control evidence, not PDF builds.

Every invocation retains the exact input snapshots, environment, child command registrations, stdout/stderr, resource outcomes, PDF structure data and final result. A nonzero exit, diagnostic, timeout, output limit, source/tool drift, wrong raw PDF, navigation mismatch or finalization after the deadline fails the attempt. The runtime's process-group accounting is bounded cooperative supervision, not a hard memory cap, atomic census or guarantee about escaped descendants. No automatic extra build is permitted after failure.

Every fresh full build must equal `d84ef0523047f5ea6fd7852711d309aab033018bf2276cd109232e2bc9bdd91a` (14 pages, 373,350 bytes). Every fresh summary must equal `6d8cd31d068312c58ffaa15ff263fed4468e4384826d7d1bcff259c1808cc9d4` (one page, 35,706 bytes). These identities come from the separately admitted corrected discovery and were reproduced by all eight current local builds. Exact matching preserves the scope of the existing rendered review of those bytes. Any different PDF bytes require new source/visual adjudication and new applicable production; page count or text similarity cannot replace it.

Cross-toolchain requests return status 2 before registration reads, output creation or tool launch. There is no admitted Linux bias-PDF reproduction relation. Aggregate registration, public integration, exact-commit hosted evidence and mainline promotion are separate milestones. PDF results do not replay the five bias exports, establish Rust implementation correspondence, or add variance, MSE, coverage, stopping or application guarantees. The finite categorical MGW assumptions and nats units remain those of the canonical papers and theorem map.

The final-result writer checks the clock after persisting RESULT. If that write completes at or after the registered deadline, it preserves the exact first-written bytes as RESULT.before-late-finalization.json, writes a failed disposition through a fresh temporary record, replaces only this attempt's own RESULT, and raises a deadline error. A filesystem failure in this correction is also nonzero; preserve every resulting partial file. This internal guard does not prove that the outer process exited before the deadline. The owner must retain actual outer start/end/exit observations and reject late completion regardless of an inner record.

The native reference PDFs use URI dictionaries with exactly `/Type /Action`, `/S /URI`, and an HTTPS `/URI` string. The reader requires that shape and the exact URI inventory. It does not accept arbitrary extra action fields or generalize to other PDF producers.
