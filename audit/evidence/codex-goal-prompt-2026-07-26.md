# Codex `/goal` prompt — complete pid-rs and the Wibral PID research program

Paste everything below after `/goal` in Codex:

---

Complete the unfinished `pid-rs` program end to end, with the repository at
`/Users/torusprime/Development/sepahead-github/pid-rs` as the primary workspace and
`/Users/torusprime/Development/sepahead-github/*` as the later cross-repository scope. The central
scientific object is the Makkeh--Gutknecht--Wibral shared-exclusions PID. The goal is not merely to
make tests pass: produce publication-grade mathematics, statistics, formal assurance, numerical
software, provenance, documentation, full-detail PDFs, release evidence, and necessary authorized
cross-repository integration. Continue autonomously through all safe work; loop review, improvement,
and verification as long as needed. Do not mark the goal complete until every required milestone
below is genuinely finished and pushed.

Start by reading, completely and in this order:

1. `AGENTS.md`;
2. `audit/evidence/completion-handoff-2026-07-26-ksg-rev4.md`;
3. `audit/evidence/completion-active-resume.md`;
4. only the latest delta in `audit/evidence/completion-run-ledger-2026-07-25.md`;
5. `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md` where routed by the resume manifest; and
6. the active KSG claim/failure/formal/checker files named by the handoff.

Authenticate the hashes and Git state listed in the handoff before changing anything. Query the
current goal and agents. Do not reload old audit folders, prior model transcripts, or deferred claim
packets wholesale. Load them only to resolve a named obligation. Preserve all existing dirty-tree
work: it belongs to the user and multiple unfinished waves. Never use `git add -A`, never overwrite
or discard unrelated changes, and never commit the ambient mixed tree. Use an alternate index or a
clean temporary worktree based on the declared pushed parent to synthesize each exact milestone.

Apply this scientific method to every consequential claim and change, using at least five
applicable lenses and normally more:

- semantic object/estimand/domain/range/quantifier/support lens;
- exact algebra/combinatorics/analysis/boundary/counterexample lens;
- formal proof or certificate lens, including explicit premises and mutation adequacy;
- binary64/numerical/signed-zero/overflow/association lens;
- compiled production-dataflow/debug-release/serial-parallel/platform lens;
- statistical identifiability/calibration/dependence/UQ/simulation-oracle lens where relevant;
- provenance/custody/catalog/release/software-identity/Git-isolation lens;
- citation/source/novelty/authority lens; and
- downstream ecosystem and publication/PDF semantic/visual lens.

For each important decision, independently derive the answer from first principles, construct or
search for falsifiers, and obtain hostile review from native agents plus Fable 5 Max and Opus 5 Max
using configured `.env` Anthropic credentials with generous budgets when available. Treat every
model review as advisory attack input, never authority or proof. Count routes as independent only
when their failure mechanisms differ; record shared premises, fixtures, generators, source text,
human transcriptions, kernels, and compiler lineages. Preserve negative results, smallest
counterexamples, failed searches, inconclusive discriminators, intermediate mutation counts, and
correction history. Never rewrite a frozen claim after observing evidence; create a new revision.

Maintain the firewall among MGW categorical shared exclusions, continuous Ehrlich estimators, KSG
mutual information, Williams--Beer `I_min`, fitted quantized compositions, heuristics, incomplete or
mixed-dimensional PID3, project-defined diagnostics, and software wrappers. No implementation or
test transfers a theorem between these objects without an explicit mapping theorem. Information
units are nats. Continuous support is declared, not inferred. Added noise changes the estimand.
Negative shared-exclusions atoms are valid and must not be clamped. PID MI inputs use signed
estimates. Every valid result must state exactly the assumptions and bounds under which it holds;
bounded or conditional results are welcome when those conditions serve real ecosystem needs, but
must not be promoted beyond them.

Use the loop:

`review -> freeze scoped claim -> plan -> edit -> implement -> test -> adversarial review -> improve
-> settled-byte replay -> isolated staged-tree verification -> small unsigned commit -> push main ->
durable ledger/manifest update`.

Do not credit tests run while source, fixtures, catalogs, formal files, generated views, or identity
inputs were moving. Run final gates only after all writers stop, then rerun after the last byte
change. Commits and PR text must contain no AI/agent attribution, no co-author trailers, and no
signatures. Push `main` in small coherent milestones. If a file cannot truthfully contain the hash
of its own commit, use a following receipt commit rather than a self-reference.

Milestone 1 — close KSG integer-harmonic revision 4:

- Preserve revision-2 and frozen pre-closure revision-3 bytes exactly, including the restored
  40-line `formal-assurance-v3.md`; verify every hash in the handoff.
- Treat the exact positive-integer harmonic identity, exclusive/inclusive maps, sharp local
  `-D <= T <= D` bound, schema-v2 corpus, endpoint correction, 8-epsilon bounded observation,
  40 ties, signed-zero results, W1/W2 witnesses, and 150/354 selected-prefix left-association
  discriminator only within their stated domains and non-implications.
- Finish the revision-4 Lean source and checker as 19 exact theorems with 14 semantic mutants,
  including the explicit rational-to-real/digamma/bound bridge. Finish Z3 as four premise-explicit
  obligations with 11 mutants. Retain the analytic digamma premise and shared-cut limitations.
  Persist tool/source/checker/result/axiom or premise receipts. Run normal and optimized modes.
- Implement and mutate the bounded three-prime modular certificate described in the handoff. It
  may prove exact zero iff the structural endpoint only for the frozen 8,198 rows; preserve the
  rejected-prime collision as a negative control and make no universal harmonic-zero claim.
- Keep Python and Rust row-derived 240/114 endpoint checks, all-`+0` selected outputs, zero `-0`,
  150 selected-prefix ordinary-left nonzeros, and the production-private ordered W1 brute/kd-tree
  diagnostic.
- Author revision-4 claim, obligations, routes, behavioral witnesses, formal assurance,
  implementation map, correction ledger, failures, evidence matrix, decision, and revision index.
  Revision 4 is post-result integration, not preregistration.
- Add canonical `active-packet-v4.json` custody, a `--claim-only` checker route, hash-first deletion
  and edit mutants, and one-hash-rebased semantic mutants so hash masking cannot create a false
  green. Require exactly one active revision and the explicit MGW non-implication.
- Derive the catalog reverse dependency closure from the two KSG roots: exactly 21 nodes, excluding
  only the non-numerical shared-config contract, hence exactly 20 affected methods. Bind exact
  revision-4 evidence targets, require them to exist, protect the remaining 49 method objects, all
  references, and top metadata, and retain forbidden later-wave token controls.
- Bind exactly 15 affected and 20 protected release family full objects plus top-level metadata.
  The four PID2-emitting families stop at KSG-only bridge revisions; reject later combined strings.
- Add exact phase isolation against the declared parent: reviewed changed-path allowlist, protected
  path/blob projections, full blob hashes for shared risk files such as `stats.rs` and
  `parallel_bit_identity.rs`, and mutations that attempt to stage PID2 exact-sum, I_min, later
  claims, unrelated PDFs/formal files, or combined identity bytes.
- Synthesize the KSG-only candidate from the pushed parent. Rebuild shared files from parent plus
  reviewed KSG hunks. Freshly recapture the 13 KSG-only serial constants and prove parallel/thread
  equality without reusing combined-tree PID2 values.
- Regenerate `METHODS.md`, release-scope Markdown, KSG review evidence, dispositions, assurance
  registry, ecosystem capabilities, and software identity only after JSON/source settles. Preserve
  protected base projections and exclude all later waves.
- Run generator no-write normal/optimized, exact/Fraction/Decimal/binary64/main checker and all
  mutation routes, Lean and Z3 routes, modular certificate, focused Rust debug/release tests,
  brute/kd-tree and serial/parallel checks, formatting, clippy, rustdoc, stable/no-default/all-feature
  debug/release, Python binding tests, method/release/review/ecosystem/identity/self-tests, and the
  relevant CI/release-audit matrix. Record exact commands, toolchains, tree/commit, hashes, results,
  and every negative result.
- Obtain final proof-blind and source-blind hostile reviews; adjudicate every point independently.
  Commit and push only when the isolated candidate is coherent. Use a follow-up receipt/decision
  commit if necessary to bind the implementation commit.

Milestone 2 — close PID2 represented-sum revision 4 on the pushed KSG parent:

- Reread only `claims/PID2-REPRESENTED-SUM-001/` and its routed evidence.
- Preserve the exact accepted-then-times-16-rejected threshold witness, the finite-atoms/infinite
  exact-reconstruction result, signed-zero path semantics, fail-closed constructor behavior, and
  the stronger exact family for every scale exponent `k=1..1023`.
- Use “ordered-binary64 positions,” not “bits” or an unsupported ULP claim. Keep the `MAX/16`
  no-overflow condition separate from acceptance, and state exact sufficient acceptance conditions
  only on their proved ranges.
- Correct the live-versus-copied inventory prose; add historical custody, copied-root baselines,
  toolchain receipt, compiled-test-name mutation, pin mutations, and source/release/catalog phase
  isolation. Leave exact-versus-Neumaier guard equivalence INCONCLUSIVE unless proved.
- Replay exact, formal, compiled debug/release, Rustdoc, catalog/release/review/ecosystem/identity,
  full mutation, and hostile-review gates on settled KSG-parent bytes. Commit/push a small milestone.

Milestone 3 — categorical MGW SxPID3 Programs A through E:

- Activate `claims/SX-CERTIFIED-AVERAGED-PID3-001/` only after PID2.
- Freeze producer and independent verifier interfaces and bind all 108 coordinates through event,
  count, probability, shared-exclusion connective, antichain/mask order, Möbius inversion,
  target-weighted averaging, exact/certificate, compiled, mutation, and archive routes.
- Prove the bounded statements using materially different methods; exhaust finite fixtures where
  feasible; retain count-weighting, event connective, exact-zero/product-one, mask/array-order, and
  negative-net-atom failures. Distinguish categorical empirical MGW results from continuous
  estimators and project wrappers. Commit/push each coherent program milestone.

Milestone 4 — pursue valid frontier results:

- Reopen only well-specified Wibral/MGW questions with explicit estimands, support classes,
  domains, ranges, quantifiers, falsifiers, and ecosystem relevance.
- Preserve the refuted common-radius smooth-torus bridge and any other negative results. Do not use
  random search failure as proof. Seek replacement theorems, certified finite-alphabet bridges,
  finite-sample bounds, or formal lattice/event results that are both correct and useful.
- Attack each proposed breakthrough with exact derivation, counterexamples, at least two formal or
  certificate methods where possible, compiled refinement, statistical calibration when relevant,
  source-blind review, and clearly bounded claims. Do not label project composition as scientific
  novelty without a literature review.

Milestone 5 — publish the research process and complete-detail PDFs:

- Produce a canonical publishable methods paper for mathematical/statistical PID discovery and
  multi-agent assurance: object firewalls, frozen claims, quantifier tables, AND/OR obligation
  hypergraphs, critical cuts, independence accounting, agent information flow, correction and
  negative-result rules, compaction-safe durable state, liveness/termination, schemas, mutations,
  exact replay, resource cost, limitations, and worked pid-rs successes/failures/open cases.
- Perform a real literature and citation review before any novelty statement. Obtain independent
  hostile methodological review and retain conflicts or limitations.
- Every scientific PDF must include all definitions, assumptions, domains, ranges, full
  derivations, proof/certificate correspondence, statistical design, counterexamples, failed and
  open routes, implementation mapping, hashes/receipts, limitations, and replay commands—not just
  final results. Generate from canonical source, compare extracted text for semantic parity, render
  every page, visually inspect every page, fix every layout defect, and store verification receipts.

Milestone 6 — final pid-rs and cross-repository closure:

- Run the complete repository CI/release/identity/archive/package/SBOM/security/coverage/property/
  fuzz/platform/Python matrix appropriate to the final candidate. Preserve exact release metadata
  truth and never claim authenticity, compatibility, scientific validity, or cross-platform
  identity from software-identity equality.
- Inspect each sibling sepahead repository independently, read its instructions, identify only
  necessary integration changes, respect external authority and credentials, test locally, and
  commit/push its `main` in small coherent milestones. Do not modify a sibling merely for cosmetic
  synchronization and do not claim downstream qualification without downstream evidence.
- Update the durable resume manifest and append-only completion ledger after every milestone and
  before any stop. Keep the active context narrow so compaction reloads only current evidence.

Completion means: all safely actionable handoff recommendations and later user requirements are
implemented or explicitly, rigorously classified as impossible/out of scope/open with retained
evidence; every promoted mathematical/statistical/software claim is correct on its stated domain;
all required formal, numerical, compiled, provenance, publication, and cross-repository gates pass
on settled committed bytes; complete-detail PDFs are semantically and visually verified; small
unsigned commits are pushed to `main`; and the durable ledger truthfully records final state. Do not
stop early, report partial work as completion, weaken gates to turn red evidence green, or mark the
goal complete because of time, token, or compute usage.

---
