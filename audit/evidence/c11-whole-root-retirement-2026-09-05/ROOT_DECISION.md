# Guarded C11 retirement decision

This is an operational disposition under the user's explicit transfer and cleanup instruction.
The coordinator read the full independent initial ten-route/fifty-lens critique, consolidation
plan, bounded task, actual create_recovery.py source, archive/restore results, exact source and
ref manifests, fetch-equivalence probes, persistent custody manifest and supporting Git logs.
The reviewer shares this environment and prior recovery work; no external independence is claimed.

The selected route combines selective accepted-source succession with full rejected-history and
working-byte custody. C11's clean HEAD is already an ancestor of accepted remote main. The archive
retains all inventoried regular bytes and modes, including Git metadata, ignored target outputs
and cache. The independent restore and separate no-hardlink bare recovery endpoint were actually
verified. Git bundles alone, unchecked copies, bulk old-tree merges and unconditional deletion
were rejected for the reasons in the initial critique. No-action is appropriate if fresh ownership,
source, configuration or recovery guards fail. Ordinary Git and byte comparison are sufficient;
no PID reinterpretation is relevant to retiring an exactly superseded clone.

C12 depends on this source only through a selected local remote URL. Its exact fetch mapping was
exercised on two independent owned consumers with equal resulting refs. Root will rebind only
that URL to the verified persistent bare endpoint. C12 and its linked checkout keep all refs,
HEADs, indexes, statuses and nonignored bytes. Their ignored caches are not an inventoried byte
projection for this config-only action; this is not authorization to remove either checkout.

Orca was initially stopped. Following the Orca skill, root started it and queried the ready
runtime. C11 is absent from its three managed pid-rs workspaces. C12 and the linked workspace
have no live terminals, attached PTYs or reported agents. The runtime terminal list is empty.
Repeat current managed and OS ownership observations in the action script. Other primary and
VM/SSH cwd owners remain outside this action. Root owns the config change under the transferred
authorization; the observations do not prove absence of an unobservable future writer.

The last reversible boundary is a same-filesystem rename of exact original C11 into the owned
custody store. Match the full manifest again there before removing it. Retain the complete archive,
bare recovery endpoint and all receipts. Remove the three newly created verification copies only
after their actual output evidence and final byte manifests are retained; they must not become
extra abandoned working lanes. Stop on any discrepancy and retain the exact partial-action state.

No theorem, estimator, statistical calibration, scientific novelty, release or exact-C12 lifecycle
status changes. Archive copies are local recovery, not an off-host durability claim. ACLs, owners,
extended attributes, timestamps and atomic filesystem snapshots remain outside this contract.
