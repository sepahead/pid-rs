# Fable 5 Max adversarial advisory — formal-method portfolio and durable recovery

You are an advisory hostile reviewer, not an authority or proof source. Work from first principles.
Question every assumption. Give concrete counterexamples, proof obligations, smallest useful
bounds, and executable verification designs. Do not reward breadth that adds no failure-diverse
evidence.

## Scientific program

The repository implements and studies several objects that must remain firewalled unless a mapping
theorem is proved:

1. positive-integer four-term KSG local arithmetic;
2. KSG mutual-information estimation;
3. continuous Ehrlich shared-exclusions estimators and continuous PID2/PID3 compositions;
4. categorical Makkeh--Gutknecht--Wibral shared-exclusions PID;
5. Williams--Beer `I_min`;
6. fitted quantized compositions, heuristics, diagnostics, and software wrappers.

All information quantities are in nats. Continuous support is declared, not inferred. Added noise
changes the estimand. Negative shared-exclusions atoms are valid. PID MI inputs are not clamped.
Bounded evidence must never be promoted universally.

The mandated order is: finish isolated KSG integer-harmonic revision 4; then PID2 represented-sum
revision 4; then categorical MGW SxPID3 Programs A--E binding all 108 coordinates; then only
bounded, falsifiable frontier theorems; then a literature-reviewed research-process paper and
complete-detail PDFs; finally the complete release and authorized downstream integration.

## Current KSG facts to attack

- Domain: `n >= 2`, `1 <= k < n`.
- Exclusive KSG counts satisfy `k-1 <= nx,ny < n` and map to `x=nx+1`, `y=ny+1`.
- Inclusive Ehrlich counts satisfy `k <= x,y <= n` and pass `x,y` directly.
- Under the typed premise `psi(m)=H_(m-1)-gamma` for positive integers,
  `T = H_(k-1)+H_(n-1)-H_(x-1)-H_(y-1)`.
- With `D=H_(n-1)-H_(k-1)`, exact monotonicity yields `-D <= T <= D`.
- Lean currently checks 19 theorem declarations with 14 semantic mutations. Its remaining trusted
  cut includes the positive-integer digamma premise, statement/index transcription, Lean kernel,
  Mathlib, and checker.
- Z3 checks four premise-explicit QF_UFLIRA obligations with 12 satisfiable countermodel mutations.
  Harmonic order premises are explicit; the harmonic function is uninterpreted. This is not an
  analytic proof of digamma.
- A frozen 8,198-row rational corpus has 354 structural endpoints and 7,844 nonendpoints. Three
  primes `1,000,033`, `1,000,037`, and `1,000,081` separately produce zero residues for all
  endpoints and nonzero residues for all nonendpoints. Rejected prime `1,000,003` has four
  nonendpoint collisions. The three selected fields are fault diversity, not three independent
  proofs and not CRT reconstruction.
- Directed-Decimal and exact-rational routes distinguish a rounded reference metric from exact
  rational truth. The selected binary64 helper has a frozen-corpus rounded-reference maximum of
  exactly `8*EPSILON`, attained on 40 rows. The exact rational route has a different unique
  maximizer below `9.761311*EPSILON`; the project gate is `32*EPSILON`. None is a universal ULP,
  correct-rounding, estimator-error, or PID bound.
- Production witnesses cover exclusive W1 and inclusive W2, debug/release, brute/kd-tree,
  serial/parallel, positive/negative zero, and association order. Catalog, release, identity,
  custody, and phase-isolation layers are separate.
- Repository integration remains NO-GO until the phase checker, settled-byte replay, staged-tree
  verification, hostile review, and pushed receipt genuinely close.

## Recovery event

An uncommitted isolated worktree under `/private/tmp` disappeared while its two earlier coherent
formal commits remained safely pushed. The ambient checkout was preserved. Exact Codex session
transcripts retain each tool call and edit, and the work is being reconstructed from committed
objects, frozen hashes, and recorded patches into a durable adjacent worktree. The new policy is:

- never place a long-horizon candidate under an ephemeral directory;
- use content-addressed alternate-index snapshot refs and durable Git bundles after each edit wave;
- commit and push every coherent, truthfully scoped milestone to `main`;
- retain a machine-readable recovery manifest and human recovery report;
- rerun every settled-byte gate after reconstruction; do not credit pre-loss runs as final.

## Requested hostile advice

Return a rigorous review with these sections:

1. **First-principles reconstruction audit.** Identify ways transcript replay, ambient-diff replay,
   hash matching, generated views, or snapshot refs can silently reconstruct the wrong scientific
   object. Give exact fail-closed checks and attacks.
2. **Formal-method portfolio.** Rank additional methods beyond the existing Lean, Z3, exact
   rationals, modular certificate, Decimal/binary64, and Rust tests. Consider Kani/CBMC, Verus or
   Creusot, cvc5 with proof production, Gappa/Flocq/Rocq Interval, MPFR/Arb interval certificates,
   independent certificate checkers, proof-producing SAT/SMT, algebraic decision procedures,
   model checking, and process/schema formalisms. Recommend a method only when it closes a distinct
   obligation. State trust base, semantic bridge, boundedness, mutation plan, and rejection rule.
3. **Evolutionary/genetic/CEGIS route.** Specify useful search spaces for discovering
   counterexamples, extremizers, candidate identities, lattice failures, adversarial floating-point
   associations, and mutation-resistant invariants. Explain how each search result is minimized
   and converted into exact/certified evidence. State why search failure proves nothing.
4. **KSG closure attacks.** Try to falsify the exact theorem, index maps, modular implication,
   floating-point measurement language, W1/W2 production refinement, and release/custody claims.
5. **PID2 and MGW SxPID3 forward design.** Propose at least two failure-diverse correct routes for
   each major obligation, including the 108-coordinate binding. Preserve negative atoms and the
   object firewall.
6. **Statistical theorem discipline.** Separate estimand identifiability, estimator bias/variance,
   dependence, calibration, multiplicity, resampling class, and application validity. Identify
   which statements can be formalized and which require simulation or real data without turning
   empirical evidence into theorem proof.
7. **Machine/human/PDF parity.** Design a machine-readable obligation/evidence/negative-result
   schema and a human paper/PDF projection that cannot omit assumptions, failed routes, open gates,
   hashes, replay commands, resource costs, or shared cuts. Give semantic-parity and page-review
   mutations.
8. **Decision table.** List each recommendation as `must`, `high-value`, `optional`, or `reject`,
   with a falsifiable completion check.

Be direct. Multiple correct solutions are welcome. Distinguish independent routes from correlated
re-encodings. Flag any request above that is ill-posed or risks overclaiming.
