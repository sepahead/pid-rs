# Fable 5 Max hostile review — KSG revision-4 preclosure

Act as a hostile scientific, mathematical, formal-methods, numerical, Rust-dataflow, and
evidence-custody reviewer. Your output is advisory falsification input, not authority. Do not
reward length, effort, multiple tools, multiple models, or internal agreement. Find the smallest
load-bearing defect you can.

The reviewed object is deliberately narrow:

```text
KSG positive-integer local arithmetic
T = psi(k) + psi(n) - psi(x) - psi(y)
```

under the stated positive-integer digamma premise and the exact exclusive/inclusive argument
maps. The packet says its exact/formal/bounded-corpus core is GO while repository/publication
integration is NO-GO. Do not broaden the object to complete KSG MI, continuous Ehrlich shared
exclusions, continuous PID2, categorical Makkeh--Gutknecht--Wibral PID, Williams--Beer I_min,
fitted quantization, heuristics, PID3 validity, wrappers, or consumers.

Independently attack at least these lenses:

1. exact object, domain, range, quantifiers, units, signs, and `k=n` seam;
2. direct-to-symmetric harmonic algebra, monotonicity, sharp signed bound, and smallest
   counterexamples;
3. exclusive KSG versus anchor-inclusive Ehrlich index maps and actual Rust call sites;
4. Lean/Z3 premise correspondence, theorem strength, shared cuts, and mutation adequacy;
5. modular implication direction, denominator invertibility, selected-prime role, rejected-prime
   collision signs, and corpus-only versus universal classification;
6. binary64 association, Neumaier prefix, signed zero, `8*EPSILON` absolute-nats wording,
   150-versus-121 prefix distinction, and ordered-binary64 positions versus ULP;
7. W1/W2 compiled bridges, serial-reference execution, parallel identity, debug/release, and
   false-green paths;
8. canonical JSON, duplicate/nonfinite keys, exact path set, path escape, symlink/regular-file
   checks, digest cycles, historical-byte custody, and preclosure-to-final lifecycle;
9. checker/self-test use under normal and optimized Python, hash masking, resealed semantic
   mutations, copied versus live roots, exact output contracts, and unmutated critical premises;
10. object-firewall, novelty/citation language, release non-implications, and downstream authority.

For every alleged flaw:

- identify the exact artifact and textual/code locus;
- state a concrete counterexample, mutation, command, or proof obligation;
- classify severity as blocker, major, minor, or nit;
- explain which current result it invalidates and which results survive; and
- distinguish a real defect from an optional assurance enhancement.

Explicitly answer:

1. Is the exact identity and two-sided bound correct on the declared domain?
2. Does the modular evidence justify exactly the frozen-corpus iff and no more?
3. Is the claim manifest/checker fail-closed against an unresealed edit and a leaf+manifest
   resealed semantic edit?
4. Can `--claim-only` be green while the default checker remains truthfully red?
5. Is any prose silently transferring KSG local arithmetic to Ehrlich/MGW/PID validity?
6. What is the strongest remaining counterexample or mutation not already covered?

Finish with two separate verdicts:

```text
bounded exact/formal/certificate core: GO | NO-GO
preclosure claim/custody lane: GO | NO-GO
repository/publication integration: must remain NO-GO
```

Use GO only if no unresolved blocker or major remains in that exact scope. A clean verdict must
still list shared cuts and the strongest open falsifiers. Do not propose weakening a gate to make
it pass.
