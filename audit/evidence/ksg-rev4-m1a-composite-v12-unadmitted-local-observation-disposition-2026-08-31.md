# Exact-C12 unadmitted local-observation disposition

Status: **hash-bound external retention; unadmitted local observation; zero
qualification credit**.

## Object identity

An external private record has these exact properties:

| Field | Value |
|---|---|
| SHA-256 | `c5f289793b859f70407f47e6c967c86b66f1df82ec3ecfe3fd59ce5a9f3eb042` |
| Byte length | 80,611 |
| JSON schema label | `pid-rs/ksg-rev4-m1a-composite-local-closure/v12` |
| Exact C12 commit | `01466e88b0550333c2718f1716289e9642e30dc6` |
| Exact C12 tree | `c445a647963ac12fbe1c352cc042956a82c7d69e` |
| Recorded command | `just ksg-composite-v12` |
| Recorded exit status | 0 |

The raw JSON is deliberately not introduced into reachable `pid-rs` history,
including under an alternate near-name. It uses the historical L12 local-closure
schema. Publishing it beside the terminal record could be misread as admitting an
L12 term or bypassing the reserved R12 path contract. This disposition preserves
discoverability without making that transfer.

## What was checked

The frozen historical v12 validator accepts the exact record in normal and optimized
Python. On its bounded terms, it checks canonical JSON and schema semantics, the
exact C12 Git identity, 34 named C12 authority bindings, 16 reviewed executable
bindings, declared resource bounds, and the internally recorded exit-zero command
transcript. The validator result is evidence about the internal consistency of this
one retained record and its named historical objects.

The validation does not authenticate the operator, Git object store, operating
system, interpreter, executable bytes, clock, or transcript. It does not establish
hermetic execution, complete process closure, absence of interference, independent
replication, hosted attempt-1 status, or a complete privacy or secret scan. It
provides no PID, KSG, theorem, estimator, numerical, statistical, scientific,
security, application, accessibility, or release evidence.

## Terminal non-transfer

The current exact-C12 authority remains
`ksg-rev4-m1a-composite-v12-terminal-failure-2026-08-23.json`:

- `L12 = not_adjudicated`;
- `Q12 = false`; and
- `R12 = permanently_unissued`.

The failed attempt-1 repository-CI and dedicated-v12 terms make `Q12` false for
either possible Boolean value of L12. Therefore this local observation cannot change
Q12. It was never introduced at the reserved R12 evidence path with the required
hosted capture and receipt, so it cannot issue R12 or supply qualification credit.
It also cannot retroactively become exact-C12 attempt-1 hosted evidence.

No commit may use the forbidden R12 message or introduce the reserved path
`audit/evidence/ksg-rev4-m1a-composite-local-closure-v12-2026-08-23.json`.
The current preservation checker must continue to prove that both remain absent from
reachable history.

## Retention and disclosure

The exact raw bytes are retained in two access-restricted private local stores and
remain at their original source location. Those copies are on the same device and do
not constitute independent or off-device durability. Do not remove the source
worktree or either private copy until a third copy has been written to, and
digest-verified on, a genuinely independent off-device store.

A bounded disclosure scan found no user-home path, user name, email address,
private-key marker, or common GitHub/AWS token pattern. The record still contains
five URL occurrences (two unique URLs), exact platform-version metadata, and an
opaque environment-route digest. The raw bytes therefore require a separate
privacy, provenance, and publication review before any public release.

This summary may be cited as process history. It is not evidence that the raw record
should be published, and it gives the mathematical KSG revision no additional truth
credit.
