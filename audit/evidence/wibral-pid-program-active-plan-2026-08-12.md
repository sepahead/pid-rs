# Wibral PID program active plan — 2026-08-12

Status: mutable coordination record; not scientific or release evidence

Purpose: extend, improve, and complete the distinct PID research lines represented here, including
multiple papers coauthored by Wibral, for ecosystem consumers without implying affiliation or
transferring assumptions or results across constructions. A checked box means its exact acceptance
predicate passed; an unchecked item is not a claim.

## Scientific-object firewall

Every task must name: paper/revision, object/estimand, domain/support, units, sample role, source
locator, code route, evidence class, reviewer class, open correspondence edges, and nonclaims.
Results do not transfer among:

1. MGW empirical categorical shared-exclusions SxPID;
2. Gutknecht parthood/formal-logic constructions;
3. Schick-Poland's proposed measure-theoretic construction;
4. Ehrlich analytic continuous shared exclusions and its kNN estimator;
5. KSG mutual information;
6. PID2 algebraic composition and incomplete/research PID3;
7. Williams–Beer `I_min`;
8. fitted quantized, same-sample, hyperbolic, hierarchy, UQ, and report wrappers defined by pid-rs.

A transfer needs a typed mapping theorem or an explicit scoped empirical comparison. Similar names,
shared code, citations, hashes, formal tools, or passing tests are not mappings.

### Prospective AI-assisted research-integrity checklist

This is an unchecked cross-cutting intake checklist, not evidence about prior pid-rs work. Apply it
separately to each scientific object and link the resulting object-specific packet rather than
merging PID families or proof/certificate/application statuses.

- [ ] Seal the exact per-attempt task revision before candidate access, including claim,
  definitions, imports, permitted axioms/trust routes, source/toolchain identities, acceptance
  predicate, resource budget, stopping rule, and selection rule.
- [ ] Keep candidate writes within the declared payload and the pre-bound judge/verifier outside
  candidate authority; fail closed on target, definition, import, axiom, verifier, test, fixture,
  reference, trust-route, network/secret, or toolchain drift.
- [ ] Inventory the exact import/axiom/kernel/toolchain trust surface and keep formal acceptance,
  finite-certificate acceptance, executable refinement, estimator validation, and application
  readiness as separate dispositions.
- [ ] Re-derive every bounded certificate through a separately scoped route with its dependency and
  custody overlap declared; add hostile changes for endpoints, coverage, margins, prerequisite
  predicates, status propagation, and fail-closure.
- [ ] Preserve every pre-packet output as retrospective exploratory evidence while permanently
  denying it preregistered, selection-unbiased, or strict-confirmatory credit. A formal/exact
  artifact may later receive only its separately checked predicate; empirical promotion needs a
  candidate-inaccessible confirmation route or declared selection-aware design.
- [ ] Retain a minimum ledger entry for every attempt and full permitted context for promoted,
  adjudicating, load-bearing failed/withdrawn, and selection-bearing unselected valid attempts;
  record selection, reopen conditions, and explicit omissions/redactions without claiming complete
  hosted-model replay. An unavailable decisive selection record makes strict selection incomplete.
- [ ] Record an independence vector for every route pair and obtain a separately scoped human
  disposition where human scientific review is claimed. Different models, prompts, sessions,
  implementations, or deterministic checkers do not establish independence by label alone.
- [ ] Use category-theoretic notation only when an object-specific packet names the objects,
  morphisms, equality, identity/composition laws, and a commuting or bounded-error witness that
  closes a real mapping obligation. Otherwise keep the typed multigraph. Treat Markov-category or
  Blackwell-order work as a distinct research route and never map it into MGW, Schick--Poland,
  Ehrlich, Williams--Beer, PID2, or PID3 by vocabulary, diagram shape, or shared mutual information.

## Status-separated research inventory

This inventory is a coordination snapshot bound to the published C4/C5/C6/C7/C8 engineering
lineage, the operative composite-v9 successor contract, and the unchanged catalog, not a novelty
claim or a completion percentage. The rows are **not additive**:
a dependency, workflow composition,
validation package, theorem, solver obligation, and PID definition are different object types.
Published C4 commit `da253576a5f76e99633fff4de5cf1118f967b90d` changed no PID definition,
estimator, theorem source, or scientific code. Published C5 commit
`be862b155d710573ec95356fc1cbe9a96a2b83b9` preserves that zero-science scope. Published C6 commit
`0c3afa0ab5b264370072a18d24655df35b90574c` preserves it again. Published C7 commit
`23b69abafb4bfdaab4b2321eb6cee7be7e1cd32e` preserves the same zero-science scope, but its hosted
qualification terminated with two distinct failures and R7 is permanently unissued. Published C8
commit `7c80d48db415279fc4d744eadb1515797606912b` preserves that scope again; its hosted
qualification failed at the retained certified-SxPID2 binding edge and R8 is permanently unissued.
Composite-v9 defines the same bounded zero-science scope for the prospective direct-child C9
repair. Those are exact tree/diff observations for named published commits or the current frozen
successor contract, not permission to infer the same about a future descendant without recomputing
it.

| Category | Landed inventory | Open inventory and boundary |
|---|---:|---|
| Globally novel PID theories or functionals claimed | **0** | There is no novelty quota. A future novelty claim requires its own source, priority, correspondence, proof/certificate, implementation, and review dispositions. |
| Published PID theory/functional lineages represented | **3** | Williams--Beer `I_min`; MGW categorical shared exclusions; and Ehrlich continuous shared exclusions. This is a lineage count, not a novelty claim. |
| Implemented PID decomposition method routes | **4** | Williams--Beer categorical `I_min`; categorical MGW shared-exclusions SxPID for 2--4 sources; Ehrlich continuous two-source PID; and research-only full mixed-dimensional continuous PID3. PID3 is a distinct 18-node mixed-dimensional route within the Ehrlich lineage, not a fourth theory lineage or a new PID. Two explicit estimator gaps remain: hyperbolic shared-exclusions PID and general mixed-support continuous shared exclusions. |
| Separately cataloged PID-defining redundancy dependencies | **1** | Ehrlich continuous shared-exclusions redundancy. It feeds a PID construction but is not itself another decomposition route or theory lineage. |
| Project-defined PID workflow/composition extensions | **10** | Fitted-quantized and same-sample quantized `I_min`; incomplete continuous-PID3 availability; hierarchy and PID2 screening; PID3 permutation; PLS-to-PID composition; quantized-SxPID2 bootstrap; fitted-quantized SxPID; and same-sample quantized MGW SxPID. No finite backlog is inferred from this count; each future composition needs a typed estimand and applicability argument. |
| Cataloged PID-facing validation/assurance extensions | **6** | Certified SxPID2 reference, dependency-colored concentration, finite-alphabet convergence, foundational SxPID audit, support-change continuity, and the two-source count-to-atom bridge. These are six assurance contributions, not six PIDs. |
| PID/KSG-relevant source-written Lean theorems | **275** | The total is 246 bridge/convergence, 19 KSG integer-harmonic, 7 exact-log-product, and 3 descriptor-factorization theorems. No arbitrary theorem-count target is asserted. |
| Exact solver obligations | **9** | Five PID-algebra QF\_LRA obligations and four conditional KSG QF\_UFLIRA obligations are landed; solver answers are not kernel proof objects. Future obligations follow the named open packets rather than a predeclared count. |
| Categorical MGW SxPID3 | Stable implementation computes the **18-antichain** three-source lattice | The stronger assurance program is **0/5 Programs A--E** and **0/108 component-coordinates** complete. Do not transfer the implementation status into certification status. |
| Named core-science work units | **0/7 current units closed** | KSG M1c correspondence (1), PID2 revision 4 represented-sum assurance (1), and categorical MGW SxPID3 Programs A--E (5). Python verifier custody and the C4/C5/C6/C7/C8/C9 qualification lineage are engineering/evidence work and are counted separately. |
| Mutable plan checklist | **31/73 checked; 42 open** | This is task bookkeeping, not evidence strength, scientific completion, or a weighted percent. |

The catalog currently contains 73 method rows and 45 references: 17 paper-defined, 15
paper-derived, and 41 project-defined. Every row records `scientific_novelty_claim = none`.
Co-information, O-information, target-free `Red°`/`Vul°`, KSG mutual information, support
diagnostics, resampling procedures, and run-log/software-identity infrastructure must not be added
to either the theory/functional-lineage count or the implemented-route count.

Those counts are for base commit `bc3aa80fb6025e709c2906a08bce25a4fac40578`, whose unchanged
`method-catalog.json` is 401,485 bytes at SHA-256
`6e8fb1143019b3f2bffe982636586705d5e99cca5decfe17a810d694b50ed8aa`; the generated
`METHODS.md` is 488,874 bytes at SHA-256
`a24a7c9a789902bd92dadcae442bdcf96e3d47de1632764fdcc0d67c9c125af4`. The family count uses one
row per distinct implemented decomposition-defining redundancy/atom construction. Raw/report
surfaces, source-count variants, bindings, wrappers, and workflow compositions do not create
additional families; a redundancy dependency without its own decomposition is reported separately.

### What pid-rs adds relative to the cited Wibral-coauthored work

The version-pinned cited papers are the defining sources for their paper-defined scientific
objects; they are not authorities by label for implementation correspondence, validity, or
applicability. pid-rs currently adds five kinds of repository work around those objects; none is
presently a global-priority claim:

1. **Executable routes.** Safe-Rust implementations cover categorical MGW shared-exclusions SxPID
   for two through four sources, Ehrlich continuous shared-exclusions redundancy, continuous
   two-source PID composition, and a separately gated research-only mixed-dimensional PID3 route.
   Williams--Beer `I_min` and KSG MI are separate cited method families, not Wibral-defined PIDs.
2. **Typed applicability and failure boundaries.** Report-first APIs carry estimand, support,
   units, sample role, resource budget, warnings, abstentions, and method identity. Deterministic
   serial/parallel paths, Python bindings, and run logs improve use and auditability; they do not
   prove estimator validity or define a new PID.
3. **Compositions and research workflows.** The ten project-defined PID-facing compositions add
   quantized/same-sample adapters, screening, PLS-to-PID, permutation/FDR, and bootstrap routes.
   Their scientific object is the declared composition, never an alias for the underlying paper
   functional and never a generic transfer theorem.
4. **Mathematical and numerical assurance.** Repository work includes finite-alphabet convergence,
   support-change continuity, dependency-aware concentration/drift bounds, exact
   counterexamples/factorization checks, formal count-to-event-to-Möbius-atom bridges, and exact
   24-coordinate SxPID2 certification. These establish only their stated finite/formal predicates;
   they do not by themselves close paper-to-code correspondence, estimator calibration, or an
   application claim.
5. **Source fidelity and anti-conflation.** Versioned source observations, catalog provenance,
   hostile controls, and typed transfer firewalls expose formula/custody gaps and prevent evidence
   from moving among categorical MGW, Schick--Poland, Ehrlich continuous, KSG, `I_min`, PID2/PID3,
   quantized, mixed-support, resampling, and downstream-objective routes without a named mapping
   theorem or scoped comparison.

The strongest accurate summary is therefore: **implementation, typed composition, and bounded
mathematical assurance around published PID objects; zero claimed new PID definitions so far.**
The open P4 packets are the route by which stronger, publication-ready extension claims must be
earned rather than inferred from code volume or verifier counts.

### Why carefully typed extensions matter

Many high-impact PID failures are object-identity failures rather than arithmetic failures: using
an estimator outside its support assumptions, treating KSG MI as a PID, moving a categorical MGW
result into an Ehrlich continuous route, relabeling a quantized proxy as the paper functional,
clamping signed atoms before an identity subtraction, or mixing training-fitted, same-sample,
permutation, and surrogate roles. A bare floating-point value cannot prevent any of these errors.

A scientific result type should therefore carry, where applicable: object/estimand identity, PID
family, support/domain, units, sample and fitting role, estimator versus evaluator identity,
method/source revision, assumptions, artifact verification/invalidation plus links to separate
evidence and claim records, warnings/abstention, resource budget, and admissible interpretation.
Its value has three distinct layers:

- **Potential scientific error prevention:** strong in mechanism, but not yet quantified, because
  invalid transfers can become explicit or unrepresentable and a result can retain the quantity it
  actually estimates.
- **Potential engineering and ecosystem composition:** strong in mechanism, but not yet evaluated
  for usability or cross-package error rates, because Rust, Python, run-log, resampling, and
  downstream consumers can exchange structured results without guessing what an unlabeled number
  means.
- **Scientific novelty:** not automatic. A type system becomes a potential methods/software-paper
  contribution only when its formal composition rules, prevented counterexamples, usability, and
  cross-package behavior are evidenced; adding type names alone is not a new PID or theorem.

This pattern generalizes beyond PID to causal estimands, posterior summaries, uncertainty
intervals, surrogate tests, imaging transforms, and action-conditioned forecasts. The transferable
principle is: **an unlabeled number is not a self-interpreting or safely transportable quantitative
scientific result; its object identity, assumptions, units, production route, and admissible
interpretations must travel with it.** Types still do not establish
paper-to-definition correspondence, estimator consistency/calibration, numerical correctness,
kernel soundness, or application validity; those require separate proofs, certificates,
experiments, and review.

### Anti-infrastructure-drift priority

Published C4 failed hosted qualification and R4 remained unissued. Published C5 also failed its
attempt-1 hosted qualification, so R5 is permanently unissued. Published C6 failed because `rg`
was absent from the hosted dependency closure, so R6 is permanently unissued. Published C7 then
exposed two distinct hosted defects, so R7 is permanently unissued. Published C8 then exposed one
five-binding retained operational-custody defect through both repository CI and dedicated-v8. No
L8 record is installed; no operator-invocation history is claimed. R8 is permanently unissued.
Composite-v9 C9/R9 is the bounded selected
append-only correction and the current evidence-contract migration in this phase. After
exact C9 local and attempt-1 hosted qualification issues R9, work must move to
the seven named P4 science units and their publication packets. Do not start another
repository-wide harness, receipt generation, formal
replay, schema family, or CI expansion merely because it is possible. Additional infrastructure is
authorized only when a named scientific claim packet identifies a concrete failure mode, shows
that the existing machinery cannot express or check it, and bounds the smallest required change.

The harness is a means to trustworthy research, not the research endpoint. Priority after C9/R9 is
KSG M1c correspondence, PID2 revision 4, categorical MGW SxPID3 Programs A--E and 108 coordinates,
high-value consumer APIs with exact object identities, and thesis/publication-ready positive and
negative results. Ecosystem usefulness is a downstream requirement, not permission to replace the
core PID program with generic engineering.

### Problem-specific autoresearch and durable publication loop

This program is autoresearch-like only as a typed, human-adjudicated hypothesis loop. The
transferable pattern is: freeze the object and judge; keep a small exploit/near-miss/structural
beam; run high-information falsification checks, negative controls, and counterexample searches;
retain failures and selection-bearing alternatives; then promote a
coherent result. A leaderboard-style scalar, submission count, model vote, or passing harness is
never a substitute for the separate functional, estimator, support, formal, numerical,
implementation, statistical, and application dispositions.

Every P4 claim packet, method-transfer decision, disposition, and publication milestone receives a
council review. Members submit independent-first memos, cover the applicable ten-plus lenses, state
shared dependencies, and retain minority objections. The integrator must adjudicate the exact
sources and evidence; a vote or same-system consensus is not scientific independence.

- [ ] For each of the seven P4 science units, open a versioned task packet before any new scored
  candidate and give the beam entries distinct mathematical hypotheses rather than cosmetic
  implementation variants.
- [ ] With explicit maintainer authorization, commit and push each coherent accepted
  packet/result/disposition/publication milestone to remote `main`; do not leave ledger-identified
  load-bearing bytes only in `/tmp`, a worktree, a side branch, or a conversation. Do not expose a
  blind/embargoed packet or confirmation route: use restricted durable storage and publish a safe
  commitment until the boundary closes. Record the remote URL/ref/OID/time and verify tip or
  ancestor reachability before deleting local state.
- [ ] For publication-grade results, bind exact paths, sizes, digests, provenance and nonclaims,
  and add a release/archive locator where repository hosting alone is insufficient. Record any
  protected-data or provider-artifact omission and its retention boundary.
- [ ] Report the attempt population, stopping and selection rules, rejected/inconclusive outcomes,
  deviations, and remaining open edges when publishing the research process itself. Treat reused
  samples, fixtures, prompts, and visible verifier feedback as development evidence; require a
  frozen confirmation route or valid selection-aware design for any promoted empirical claim.

## P0 — hosted and release truth

- [x] Close the workflow-PDF page-25 cross-toolchain raster mismatch without widening navigation
  or raster tolerances. Exact-SHA hosted CI `31651702557` passed the Markdown-2.23 list ownership,
  64-page text/navigation/geometry/fonts, and color/gray raster gates.
- [x] Obtain terminal all-green exact-SHA hosted CI and CodeQL for the verifier/PDF correction.
  Commit `bbdfda40...` passed CI 45/45 and CodeQL 4/4; alerts 181–191 closed automatically without
  dismissal and no replacement alert was introduced.
- [x] Correct current “reviewed source” language to source offered for review.
- [x] Make current `review-source` and immutable historical `review-tagged` distinct fail-closed
  modes; bind the protected 186-row inventory and hostile mutations.
- [x] Add deterministic self-excluding source-scope state and post-commit CI state artifact; avoid
  a self-referential committed SHA.
- [x] Remove repository-byte dynamic execution from the current/post source-state verifiers;
  retain only fixed-root standalone checkers, fixed schema-byte contracts, fixed Git routing, and
  CLI-only normal/optimized hostile suites. Exact-`bbdfda4...` hosted CodeQL subsequently closed
  every targeted replacement alert without dismissal or a new alert.
- [x] Complete the Lean-bound v2 wiring batch: retain AGENTS' current-state examples as `-I -S -B`,
  bind the stdout/stdin-only post-commit route, and append the fresh `r4` current Lean replay
  receipt. Finalized `r3` remains exact prior evidence; do not imply it covers these later
  operational bytes or give `r4` execution credit before the exact receipt exists and validates.
  The append-only r4 receipt is finalized and passed hosted normal/optimized custody replay.

## P1 — source fidelity and correspondence

- [x] Implement and independently replay the versioned source-errata registry/checker/self-test for
  four Ehrlich and four Schick-Poland observations, with `reviewer_derived` versus
  `author_confirmed` statuses.
- [x] Correct public Schick-Poland wording: arXiv v2 proposes a construction under stated
  Radon/Borel premises; pid-rs does not adjudicate pointwise existence/version-independence and
  does not implement the construction.
- [x] Preserve the versioned assurance authority and add a non-authoritative typed five-edge view:
  source→specification→formal object→executable algorithm→language/numeric execution→application.
- [x] Generate a compact PID-only protocol and compact stable-first method view from existing
  authorities. Preserve process-case studies as non-PID evidence.
- [x] Route PID2 and PID3 component-source observations through their exact underlying Ehrlich
  construction card as dependency pointers only; re-adjudicate applicability and transfer no
  review, implementation, correspondence, or independence credit.
- [ ] Add machine-readable, object-specific paper-symbol→repository-field→formal-definition maps
  for load-bearing claims. Require a wrong-but-type-correct hostile mutation for each new claim
  edge; a generic five-edge assurance row does not establish formula-level correspondence.
- [ ] Preserve the pinned `continuouspidestimator` validation-code revision through a
  license-compatible Software Heritage identifier, bounded archive, or minimal snapshot if one is
  available. Keep it validation-only and non-load-bearing; archival custody is not author
  confirmation, scientific correspondence, or an implementation dependency.
- [ ] Add explicit independence vectors to every oracle/review claim.

## P2 — APIs, provenance, and numerical assurance

- [ ] Remove default-off provenance-losing Python flat adapters before 1.0; stable paths remain
  structured. Add consumer-loss tests proving provenance flattening cannot happen silently and
  provide no generic provenance-loss override.
- [ ] Add versioned stable `PidError` machine codes and causal/source-chain fidelity across Rust,
  Python, and run-log surfaces. Error identity and transport are software contracts, not scientific
  evidence or proof that an estimator result is valid.
- [ ] Split prospective result schemas into numeric estimate, artifact-verification/invalidation,
  accepted-evidence link, claim-record link, and scientific-interpretation objects, and version
  published versus project-analogue invariant identities.
  Convenience serialization must not silently discard warnings, method identity, or exclusions.
- [ ] Replace Boolean/call-order fit-role inference in pipeline and same-sample PLS routes with
  typed `TrainingFitted`, `SameSampleDescriptive`, and `ExternallySpecified` roles carried into
  results; no role name promotes a descriptive composition to a calibrated population estimator.
- [ ] Add versioned unit-bearing values and metric/quantizer semantic revisions to new standalone
  schemas while preserving current nats behavior.
- [ ] Extend signed-atom cancellation diagnostics without clamping.
- [ ] Add quantizer boundary corpus: `nextafter`, signed zero, subnormals, huge range, repeated
  extrema, serialization round trips, and training/evaluation-role checks.
- [ ] Extend exact brute-force/kd-tree backend parity with `nextafter`-adjacent and boundary-shell
  coordinates while retaining the existing duplicate-row, repeated-coordinate, and tie cases; do
  not treat backend agreement as an independent scientific oracle.
- [ ] Add exact-rational regression vectors for every public Williams–Beer `I_min` lattice size,
  with explicit nats/method identity and no transfer to MGW categorical SxPID or continuous PID.
- [ ] Add PCA/PLS/logistic residual, orthogonality, reconstruction, conditioning, iteration, and
  independent-solver strata.
- [ ] Research aligned/no-replacement PID2 resampling that retains all four coordinates and failure
  patterns. Do not call it calibrated uncertainty without a theorem or validated calibration study.

## P3 — publication, CI, and external review

- [ ] Generate a uniform PDF-set manifest and prospective front-page assurance capsules; keep exact
  build/render/navigation/visual predicates and historical artifacts immutable.
- [ ] Extract repeated CI mechanics into checked scripts with stable gate IDs and a claim-to-job
  view; do not weaken pinned actions or permissions.
- [ ] Add scheduled read-only aggregate dependency compatibility reporting.
- [ ] Produce clean-archive SBOM/provenance/evidence bundles only for actual release artifacts.
- [ ] Publish release PDFs as downloadable assets and verify provider-observed bytes plus
  `application/pdf` content type; transport metadata is not visual review, reproducibility,
  authenticity, or scientific validity.
- [ ] Seek named external scientific and code dispositions bound to exact blobs, claims, roles, and
  independence vectors. Model review does not satisfy this item.

## P4 — core scientific program

- [ ] Complete KSG M1c source/specification/implementation correspondence and negative controls.
  The bounded M1a implementation, correction, and hosted-recovery lineage now terminates at exact
  unsigned recovery commit `bc3aa80fb6025e709c2906a08bce25a4fac40578`, tree
  `7d87f87953a42edb91e40880d918471c7cbe4414`. Exact-SHA CI run `31773937366`
  completed 45/45 jobs and 573/573 steps successfully; CodeQL run `31773937102` completed all four
  analyses successfully. Those are hosted process observations, not KSG/PID correctness or M1c
  integration evidence.

  Do not issue the frozen composite-v3 receipt. It is uninstantiable for two separately sufficient reasons:
  its fixed CodeQL language order conflicts with increasing analysis-ID order on the exact recovery
  observations, and its exact-three-additions topology cannot also update the self-excluding
  current-source manifest. Preserve the v3 checker, self-test, schema, and absence of its reserved
  receipt. Composite-v4 published unsigned direct-child C4
  `da253576a5f76e99633fff4de5cf1118f967b90d`, but its first hosted qualification failed: CodeQL
  passed, while repository CI had four terminal failed jobs and the dedicated v4 route failed in
  static-validation preflight. R4 is permanently unissued. Composite-v5 published direct-child C5
  `be862b155d710573ec95356fc1cbe9a96a2b83b9`, tree
  `37ae61554284a2fabb460d3a20a731b6ade5f8f4`, with the predecessor failure, five operational
  repairs, the boundary PDF, and fresh replay r10. Exact-C5 attempt-1 CodeQL succeeded, but
  repository CI and the dedicated-v5 route failed, so R5 is permanently unissued. Composite-v6
  published direct-child C6 `0c3afa0ab5b264370072a18d24655df35b90574c` with the bounded
  association-rule repair and fresh replay r11. Exact-C6 CodeQL succeeded, while repository CI and
  dedicated-v6 failed because `rg` was absent; R6 is permanently unissued. Composite-v7 published
  direct-child C7 `23b69abafb4bfdaab4b2321eb6cee7be7e1cd32e` with the dependency repair and
  fresh replay r12. Exact-C7 CodeQL succeeded, while dedicated-v7 failed during setup on a 39-hex
  upload-action ref and repository CI separately failed at the final mathematical-workflow
  cross-toolchain layout comparator; R7 is permanently unissued. Composite-v8 published exact
  direct-child C8 `7c80d48db415279fc4d744eadb1515797606912b` with those two operational
  repairs and fresh replay r13. Its CodeQL route passed, while repository CI and dedicated-v8 both
  reached the retained certified-SxPID2 checker and failed on
  `certified SxPID2 claim check failed: release-audit just dependency line exact digest changed`.
  Read-only sequential digest-repin analysis found the exact five changed operational
  bindings; source reconstruction guards that difference set without establishing unique
  counterfactual necessity or order. No L8 record is installed; no operator-invocation history is
  claimed. R8 is permanently unissued. Composite-v9 is the bounded append-only
  replacement: publish direct-child C9 with that five-binding custody repair, the disjoint
  six-destination pre-existing refresh-destination fixture correction, a third disjoint exact
  GIL-enabled CPython 3.14.6 correction for the local recipe and hosted post-setup gates, and fresh
  replay r14. The fresh C9 changed-path set contains 32 paths; the rejected candidate's changed-path
  set contained 31. The sole path-set membership difference is the fresh C9 addition of
  `scripts/check-mathematical-workflow-pdf-self-test.sh`; this is a set-membership statement, not a
  claim that shared paths have identical bytes. One hosted pre-setup
  checkout-normalizer call plus
  four normal/optimized action-pin checker/self-test calls remain runner-Python surfaces outside the
  exact-3.14.6 lane. Exact source-slice equality observed on CPython 3.11.13, 3.12.11, 3.13.7, and
  3.14.6 is documentary only and receives no qualification or portability credit;
  require exact-C9 local,
  attempt-1 CI, CodeQL, and dedicated-v9 success; then permit
  direct-child R9 to add
  only its durable local closure record, successor hosted capture, and typed receipt while
  regenerating current-source last. Neither commit may alter scientific code or promote M1a beyond
  `integration_no_go`.

  The two earlier failed local seal/checkpoint tuples remain unauthenticated negative observations
  and must never be reused or relabeled: `952c1bb...` / `cf84ab1...` / index SHA-256
  `4354d8be...`, and `37473f8...` / `66f33f4...` / index SHA-256 `fb892aea...`.
- [ ] Complete PID2 revision 4 with exact four-coordinate identities, support/scaling assumptions,
  conditioning, failure retention, and non-calibration boundaries.
- [ ] Complete categorical MGW SxPID3 Programs A–E and all 108 coordinates with exact/high-
  precision reconstruction and source correspondence.
- [ ] Advance bounded frontier mathematics only where theorem hypotheses and object identity are
  explicit; retain negative evidence and abstentions.
- [ ] Complete the research-process paper/artifacts with PID and unrelated case-study evidence
  separated.
- [ ] Revalidate ecosystem consumers with exact version/API/source identities; make no application,
  compatibility, or scientific-validity transfer from repository-local success alone.

## Operational closure

- [x] Update `AGENTS.md` with the scientific-object firewall, evidence/reviewer classes, durable
  plan links, and no-conflation rules; keep `CLAUDE.md` a thin import shim.
- [x] Because `AGENTS.md` is Lean-replay-bound, freeze all operational bytes once, run a fresh
  versioned Lean 4.33 replay, reseal the non-cyclic projection, and pass normal/optimized checker
  and hostile self-test modes. Finalized `r2` remains preserved; append-only `r3` is the finalized
  39-command replay for that earlier exact operational surface after the certified-claim
  catalog-projection documentation correction and now remains exact prior evidence.
- [x] Freeze the current stdout/stdin-only post-commit v2, CodeQL, zeta and certified-SxPID2
  container reseals, and operational documentation bytes; preserve `r3` byte-for-byte; then
  generate and validate append-only `r4` as the fourth 12 August UTC receipt under the unchanged
  schema-v2 scientific boundary. Receipt r4 is finalized and exact-SHA hosted replay is green.
- [x] After the KSG M1a claim, runtime, lifecycle, CI, and documentation bytes are frozen, preserve
  finalized `r4` byte-for-byte and generate append-only `r5` as the fifth receipt in the versioned
  sequence that originated on 12 August and the sixth receipt in the accepted/historical lineage. The
  11 August historical receipt is outside that sequence; the suffix does not denote a calendar
  date, schema, theorem, review, or independence revision. No Lean theorem source changes, and r5
  receives execution credit only when its exact receipt exists and validates under normal and
  optimized custody checks. Finalized r5 is exact prior evidence at SHA-256
  `872175ca504efb24752633704fe13e57802e43ae25bb3c463c4fb8c9dfd073f7`.
- [x] After the M1a composite custody-correction bytes are frozen, preserve finalized `r5`
  byte-for-byte and generate append-only
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-13-r6.json` as the sixth
  receipt in the versioned sequence that originated on 12 August and the seventh receipt in the
  accepted/historical lineage. The 11 August
  historical receipt is outside that sequence; the suffix does not denote a calendar date, schema,
  theorem, review, assurance tier, or independence revision. No Lean theorem source changes, and
  r6 receives execution credit only when its exact receipt exists and validates under normal and
  optimized custody checks. Finalized r6 is exact prior evidence at SHA-256
  `f14e7a33c01909055cc868fc955e6b2520ae15ebf0d598730911ec57a7f4c5ea`.
- [x] After the failed public correction observation and hosted-recovery bytes are frozen, preserve
  finalized `r6` byte-for-byte and generate append-only
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-13-r7.json` as the
  seventh receipt in the versioned sequence that originated on 12 August and the eighth receipt in
  the accepted/historical lineage. The 11 August historical receipt remains outside that
  sequence; the suffix does not denote a calendar date, schema, theorem, review, assurance tier,
  or independence revision. No Lean theorem source changes, and r7 receives execution credit only
  when its exact receipt exists and validates under normal and optimized custody checks. Finalized
  r7 is exact prior evidence at SHA-256
  `3dd2df7d7064bac93cf4806cdeac28d9ecc747444689162a4636029228822abb`.
- [x] After the failed r7-based local seal is recorded and the append-only verifier repair is frozen,
  preserve finalized `r7` byte-for-byte and generate append-only
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-14-r8.json` as the eighth
  receipt in the versioned sequence that originated on 12 August and the ninth receipt in the
  accepted/historical lineage. The 11 August historical receipt remains outside that sequence; the
  suffix does not denote a calendar date, schema, theorem, review, assurance tier, or independence
  revision. No Lean theorem source changes, and r8 receives execution credit only when its exact
  receipt exists and validates under normal and optimized custody checks. Finalized r8 is exact
  prior evidence at SHA-256
  `86251c48c0f720d1ca021dcac87dfbf6e1a54adf409ea8a8981102cea1769611`.
- [x] After every composite-v4 C4 operational byte, process document, and PDF is frozen, preserve
  finalized r8 byte-for-byte and generate append-only
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-15-r9.json` as the ninth
  receipt in the versioned sequence that originated on 12 August and the tenth receipt in the
  accepted/historical lineage. The 11 August historical receipt remains outside that sequence. The
  suffix does not denote a calendar date, schema, theorem, review, assurance tier, or independence
  revision. No Lean theorem source changes, and r9 receives execution credit only when that exact
  receipt exists and validates under normal and optimized custody checks. Generate it only after
  all operational bytes settle; apply only the two reviewed projection/custody finalization edits;
  then regenerate current-source last. A first local one-shot execution passed its bounded command
  predicates but was rejected before push when C4 static closure exposed a terminal-line-feed
  mismatch against the current-source generator's newline-free compact projection. Preserve that
  exact 132,710-byte candidate at the explicit `prepublication-closure-rejected-2026-08-17` path
  as bounded execution-custody evidence, with zero C4-publication, hosted, scientific,
  accepted-current-replay, or independence credit. Finalized r9 was generated fresh, validated, and
  published in C4 without overwriting or relabelling the rejected document.
- [x] After all C5 operational bytes settle, preserve finalized r6, r7, r8, and r9 as exact prior
  evidence and generate
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-18-r10.json`. Here r10
  is the tenth receipt in the versioned sequence that originated on 12 August and the eleventh
  receipt in the accepted/historical lineage; the 11 August historical receipt is outside that
  sequence. The suffix does not denote a calendar date, schema, theorem, review, assurance tier,
  or independence revision. C5 receives execution credit only when that exact receipt exists and
  validates under normal and optimized custody checks; regenerate current-source last. Published
  C5 carries the accepted 135,997-byte r10 at SHA-256
  `ee8990136f16eda164b97cd6769ddc9cd3ef6446e5d84577d3517d4272df956f`; it validates as exact
  finalized prior evidence.
- [x] Preserve and withdraw unpublished local C5 candidate
  `08cf441c34b0d7a547d906ed682693c57ff29e25`, tree
  `c599c698faae5a1afae9b0fb7bde123c5525fd1c`. Its 135,997-byte `r10` had SHA-256
  `68dceeff2e031339a985e91438c7e60c939da914171b90fb55a8472d006b350a` and passed Lean execution
  and receipt-finalization checks, but the required local composite-v5 recipe then exposed a stale
  positive fixture: under its intentional `umask 077`, the checkout-normalizer self-test created a
  claimed mode-0644 residue as mode 0600. Archive commit
  `062025c3fc283db9bdb105219dbcc82719771175` has the rejected candidate's exact tree and raw receipt.
  The mutable provider ref `refs/heads/archive/composite-v5-rejected-umask-20260818` was observed at
  that archive commit. That dated observation supplies a recovery locator, not durable preservation,
  authentication, accepted-on-main C5 publication status or credit, or main-line authority. The corrected suite
  makes its restrictive umask explicit, binds causal fixture modes, and adds a separate 0600
  hostile. Do not merge, relabel, or credit the withdrawn candidate; reopen C5 only with all
  corrected bytes frozen, a fresh one-shot `r10`, current-source regenerated last, and the complete
  local route passing. This correction does not add a sixth predecessor repair surface. The
  accepted r10 slot remains available only to a fresh one-shot replay over the corrected final
  bytes; archived `08cf441` and its receipt remain outside `prior_replay_*` and are never copied,
  merged, or relabelled as accepted evidence.
- [x] Publish unsigned, human-attributed, fast-forward composite-v4 C4; retain its exact attempt-1
  hosted failure, keep both v4 capture/receipt paths absent, and record R4 as permanently unissued.
  C4 is `da253576a5f76e99633fff4de5cf1118f967b90d`; its CodeQL route passed but CI and the dedicated
  v4 route did not, so no partial or rerun credit is granted.
- [x] Publish composite-v5 C5 and adjudicate conditional R5 as separate unsigned, human-attributed
  fast-forward milestones. C5 is exact published commit
  `be862b155d710573ec95356fc1cbe9a96a2b83b9`, with publication-quality boundary PDF/SVG and
  receipts, fresh r10, exact path policy, and current-source regenerated last. Its fresh attempt-1
  CodeQL run succeeded, while repository CI and dedicated-v5 failed; therefore R5 is permanently
  unissued and no partial or rerun credit is granted.
- [x] After all C6 operational bytes settle, preserve finalized r10 byte-for-byte and generate
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-18-r11.json`. Here r11
  is the eleventh receipt in the versioned sequence that originated on 12 August and the twelfth
  receipt in the accepted/historical lineage; the 11 August historical receipt remains outside that
  sequence. The suffix does not denote a calendar date, schema, theorem, review, assurance tier,
  or independence revision. No Lean theorem source changes. C6 receives execution credit only
  when that exact receipt exists and validates under normal and optimized custody checks; generate
  it only after every other operational byte settles and regenerate current-source last.
- [x] Publish composite-v6 C6 and adjudicate conditional R6. C6 is exact commit
  `0c3afa0ab5b264370072a18d24655df35b90574c`; its CodeQL route passed, while repository CI and
  dedicated-v6 failed because `rg` was absent. R6 is permanently unissued; no rerun credit applies.
- [x] After every non-cut C7 byte settles, preserve finalized r11 byte-for-byte and generate
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-18-r12.json`. Here r12
  is the twelfth receipt in the versioned sequence that originated on 12 August and the thirteenth
  receipt in the accepted/historical lineage; the 11 August historical receipt remains outside that
  sequence. The suffix does not denote a calendar date, schema, theorem, review, assurance tier,
  or independence revision. No Lean theorem source changes. C7 receives execution credit only
  when that exact receipt exists and validates under normal and optimized custody checks.
- [x] Publish composite-v7 C7 and adjudicate conditional R7. C7 is exact commit
  `23b69abafb4bfdaab4b2321eb6cee7be7e1cd32e`; CodeQL passed, while dedicated-v7 failed at setup
  on a 39-hex upload-action ref and repository CI separately failed at the reached final
  mathematical-workflow cross-toolchain layout comparator. No L7 record is installed and R7 is
  permanently unissued; no rerun or predecessor-transfer credit applies.
- [x] After every non-cut C8 byte settled, preserve finalized r12 byte-for-byte and generate
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-19-r13.json`. Here r13
  is the thirteenth receipt in the versioned sequence that originated on 12 August and the
  fourteenth receipt in the accepted/historical lineage; the 11 August historical receipt remains
  outside that sequence. The suffix does not denote a calendar date, schema, theorem, review,
  assurance tier, or independence revision. No Lean theorem source changes. C8 receives execution
  credit only when that exact receipt exists and validates under normal and optimized custody
  checks.
- [x] Publish composite-v8 C8 and adjudicate conditional R8. C8 is exact commit
  `7c80d48db415279fc4d744eadb1515797606912b`; CodeQL passed, while repository CI and
  dedicated-v8 both reached the retained certified-SxPID2 checker and failed on
  `certified SxPID2 claim check failed: release-audit just dependency line exact digest changed`.
  No L8 record is installed; no operator-invocation history is
  claimed. R8 is permanently unissued; predecessor reruns and partial successes receive zero
  credit.
- [x] Reject C9 candidate `0a6ece9c525ad7aad061f55b3edea83554891b42`, tree
  `1d5446f19d34b742feeb51429bf58a0706750757`, which was not observed accepted or published on
  `main` as C9 in the bounded provider/history checks, with zero credit. The required inherited-
  `umask 077` composite run's last confirmed output line was
  `ok 273 - refresh writer reports an injected second-replacement failure`; the next
  observed stable diagnostic had prefix `refresh destination mode drifted: ` and path suffix
  `/root/output/pdf/workflow.pdf`. No complete raw transcript or whole-run digest of that required
  run was retained. A separate direct workflow-PDF self-test under `umask 022` passed 366/366
  controls but is documentary only, was not checker-replayed as a C9 qualification run, and receives
  zero qualification credit.
  Its 145,611-byte r14, SHA-256
  `2a882358e158ebeae06dbdf8d1cd35637d698f59ce217c1e2fbecf1d8787dfb7`, is archive-only outside
  `prior_replay_*`. Mutable provider ref
  `refs/heads/archive/composite-v9-rejected-workflow-pdf-umask-20260821` was observed at the
  candidate commit but is only a mutable recovery locator, not authentication, durability,
  accepted-on-main C9 publication status or credit, or qualification. The deterministic checker
  binds the recorded identifiers but does not query provider archive, main, or workflow-run
  endpoints or require the sibling commit object. There is no L9;
  GitHub exposed no workflow run for the candidate on 21 August and still resolved `main` to exact
  C8. Do not merge or cherry-pick the rejected commit or reuse/copy its `r14` or evidence.
  Reapplying reviewed non-evidentiary source/contract bytes onto a fresh C8 child is permitted; byte
  reuse transfers no execution, replay, qualification, or acceptance credit. Rebuild the same C9
  slot as a fresh direct child of C8. The fresh C9 changed-path set contains 32 paths; the rejected
  candidate's changed-path set contained 31. The sole path-set membership difference is the fresh
  C9 addition of `scripts/check-mathematical-workflow-pdf-self-test.sh`; this is a set-membership
  statement, not a claim that shared paths have identical bytes. Normalize exactly six synthetic
  pre-existing refresh-destination fixture files to `0644`, retain the exact C8
  PDF/source/production gate, and generate a completely fresh one-shot r14. The repair is not a
  sixth stale C8 binding, production-writer change, PDF change, theorem change, estimator change,
  or scientific result. A third disjoint operational correction selects exact GIL-enabled CPython
  3.14.6 for the local recipe and hosted post-setup gates. One hosted pre-setup checkout-normalizer
  call plus four normal/optimized action-pin checker/self-test calls remain runner-Python surfaces
  outside that lane. Exact source-slice equality observed on CPython 3.11.13, 3.12.11, 3.13.7, and
  3.14.6 is documentary only and receives no qualification or portability credit. Bounded runtime
  preflights do not authenticate interpreter bytes, prove atomicity or TOCTOU absence, or enumerate
  every transitive process. The AST/source-route checks and hostile mutations are finite regression
  evidence for a fixed lexical roster, not proofs of semantic soundness, causal execution, or
  non-bypass. Dynamic namespace mutation and arbitrary execution custody remain outside those
  analyses. Exact whole-file, tree, replay, and human-review custody remain authoritative within
  their stated bounds.
- [x] Reject the second precommit C9 candidate with zero credit. Unsigned archive commit
  `113cbad2e58a9cfa40cf43b1c0ffc260b566aa92`, tree
  `ae3204d72c012dddaa5b634d9f5c4c745d5823d2`, was observed on 22 August through mutable provider
  recovery ref `refs/heads/archive/composite-v9-rejected-r14-fixed-point-20260822`; the ref then pointed
  to the commit, `main` remained exact C8, and GitHub exposed zero workflow runs for it. Its final
  145,356-byte same-slot `r14` has SHA-256
  `9ae3b4915f3cf4fd062723c8b80d80e0319ddc9db250662c50584b3f764d373c` (provisional SHA-256
  `41fafe5dfdfbaf23c206ae366913082ff255e6e5f92217f02cfc95b83a5fc048`); all 39 receipt records
  carry `exit_code: 0`, but the artifact and candidate receive zero credit. The mandatory
  precommit certified-SxPID2 baseline failed in normal and optimized modes with exact stderr
  SHA-256 `5994ccdfb8dcfe35fac7646050c15ef4f19eee8524233b1cd57f651d19d78611` over 255 bytes: expected
  justfile digest `74fb7bfd4500d8b121666a738a412fbdb409e7acf673b156645d215453ab310f`, observed
  `93399171cfbb743dba93c7be1ec85e446a33193e41ada3977d198b0e4ecc6437`. Its 124-mutation self-test was
  operator-observed passing in both modes with identical 58-byte stdout SHA-256
  `2f163d400569a0897533ef5f5bdae357bd97962d0888ac2bbf68cfa5fe753351`, but therefore did not
  establish the baseline. Independent review also found that three documents conflated named
  self-test output with `r14` command custody. The archived `r14` exact argv roster contains zero
  records for the certified baseline, certified self-test, Lean-freeze self-test, or C9 self-test;
  ten other named self-test command records are present. Canonical receipt bytes contain zero literal
  `live-pre-replay-ready` occurrences, but stream payloads retain only byte-count/SHA-256 descriptors,
  so that is not a raw-stdout absence claim. Separate normal/optimized Lean-freeze self-test outputs
  were operator-observed identical at 268 bytes with SHA-256
  `a77c6d4634ad134975d9a42520a4dc16cd696d51879614a1a4f711eab8ce9f93` and report 132 mutations,
  including `live-pre-replay-ready`; this is outer observation, not `r14` invocation/stdout custody.
  At a separate generator call site, the same validator implementation evaluates the equivalent
  live-cut predicate once before the replay command sequence. Full static checks and custody snapshots
  precede the sequence; full static checks and custody/executable comparisons follow it before
  publication. These correlated, common-mode endpoint checks are not per-command or atomic custody,
  and source custody is not invocation/stdout custody. The observed mutable ref was a recovery
  locator, not authentication or durability. No L9 was issued for the candidate, and the bounded
  provider/history check observed no accepted-on-main C9 publication; there is no permission to reuse
  its `r14`. A fresh current `r14` must exclude all three rejected same-slot final `r14` artifacts from current and
  `prior_replay_*` lineage. During fresh recovery review, the first `justfile` repin exposed a second
  fail-fast baseline edge at `scripts/README.md`: expected
  `daedd86d0307984df8885849528ddfdd2d096a7b9d2799e308358ad4af59b33a`, observed
  `c7fd28e0180bc19ebb09644840266e47f5a93c9b5af7e9062c7f0bbd2012e857`; its exact 273-byte stderr
  has SHA-256 `e94271b9e1c1b7e885fb78d1839b2d8dacebf79aa6a72e6233db5773ded93ade`. This was an
  operator-observed recovery-worktree diagnostic, not archived-candidate qualification or `r14`
  custody. The repair now binds all five mutable certified surfaces plus the exact CI job and just
  recipe sub-blocks.
- [x] Reject the third unsigned direct-child C9 candidate with zero credit. Archive commit
  `769547a6d6ed70a074707d90bc2f55393fd34fa4`, tree
  `fb89c31922454dfc6d3da3d8ffa26dbe491b353e`, was observed on 22 August through mutable provider
  recovery ref `refs/heads/archive/composite-v9-rejected-local-authority-oversize-20260822`; the ref
  then pointed to that commit, `main` remained exact C8, and GitHub exposed zero workflow runs for
  it. Its 145,356-byte same-slot `r14` has SHA-256
  `66fdc640aad886c6de25a3a544a24ba016f4f2e73989abe5319f562da1c08919`; all 39 receipt records
  carry `exit_code: 0`, but the artifact and candidate receive zero credit. Deterministically
  substituting final Lean custody
  `281b7504b96cabe88e4faa4db46c04d32832b4d42a3540f462951ffd68aea07c` with replay custody
  `15d5fa25c532380db6d7f0a938dac84300ecfacf6c559332eda99bf7bc09fd96` in the canonical final `r14`
  bytes reconstructs the provisional `r14` byte identity as SHA-256
  `eece30e6d8477cb7aa3464df31d3fae590393a8b0dc47e21ea759ae5f3d6ab17`. This is a deterministic
  reconstruction only, not an observed or retained provisional artifact, and receives no replay,
  custody, or credit. Its 202,419-byte
  self-excluding current-source manifest has SHA-256
  `23d37f444b52d2bb8854e6cc7df53d0207074eceee6a080c89ace6729a850243`; that is archived byte
  identity only, not acceptance, attestation, or semantic-correctness evidence. The production
  local recorder failed closed and issued no L9. Its generic 51-byte stderr has SHA-256
  `11da5230cf3da2dc9a8e4a1378e4707e90ba5b612f8cab4830e392d268cc5b40` and contains only
  `ERROR: bounded local closure capture failed closed`; alone, that fixed public error discloses
  neither the failure stage nor the production command streams. In the exact rejected source
  route, `run_bounded` returns before `validate_record_value`, whose post-command record validation
  calls `validate_authority_roster`. That source ordering does not retain or authenticate the
  production stdout or stderr. Separate direct and sanitized operator invocations of
  `just ksg-composite-v9` both exited zero and produced identical 32,248-byte stdout with SHA-256
  `acf47c3a89810bd9cd47a5f3454d4cd5b519766dc3544cafd8565f94816bd41c` and identical 434-byte
  stderr with SHA-256 `c73f68757307c6c5d44f354043b10b0a4e62b579d3500b7cf152449a5e863009`.
  Those correlated command diagnostics are not local-recorder invocation or stream custody and do
  not issue or replace L9. A separate substituted postcondition diagnostic produced the exact
  104-byte, SHA-256 `4c9309bb307c001cd7231caff0dc92a9e6d6d2900116fece68ebe13a5b61dd81`
  two-line log `FAIL CaptureError: local closure named-oversize authority inventory changed` and
  `substituted_command_calls=1`. It isolates a stale named size-class roster: exact C9 self-test
  `scripts/check-ksg-m1a-composite-v9-self-test.py`, SHA-256
  `a704698097be3ffb0702a66f5fd0f9c794ca0a4ffbe137e6c3b94bd1825544db`, is 129,911 bytes and
  therefore exceeded the 65,536-byte classification threshold but was absent from the expected
  named-oversize set. It remained below the separate 2 MiB authority-stream maximum; this was a
  named-roster classification defect, not authority-size exhaustion. The substituted diagnostic
  made one substituted call and then failed at that postcondition, diagnosing the defect within
  the substituted route. It is not production execution or custody, does not prove or retain the
  production command streams, and its call must not be relabelled as the recorder-owned production
  `just` invocation. The mutable ref is a recovery locator, not authentication or durability. No L9,
  hosted, C9/R9, scientific, accepted-current-replay, or independence credit transfers. Do not
  merge or cherry-pick the candidate or reuse its evidence. A fresh current `r14` must exclude all
  three rejected same-slot artifacts from current and `prior_replay_*` lineage.
- [ ] After every non-cut C9 byte settles, preserve finalized r13 byte-for-byte and generate
  `audit/evidence/lean-4.33.0-darwin-aarch64-current-project-replay-2026-08-19-r14.json`. Here r14
  is the fourteenth accepted slot in the sequence beginning 12 August. Counting the separate 11
  August historical receipt, it is the fifteenth receipt in the accepted/historical lineage.
  Rejected same-slot artifacts are additional zero-credit documents; no total count of every
  generated receipt is claimed. The suffix does not denote a calendar date, schema, theorem,
  review, assurance tier, or independence revision. No Lean theorem source changes. C9 receives
  execution credit only when that exact receipt exists and validates under normal and optimized
  custody checks.
- [ ] Publish composite-v9 C9 and conditional R9 as separate unsigned, human-attributed
  fast-forward milestones. C9 must be the exact direct child of C8, retain the exact C8 publication
  family and failed hosted observations, repin only the five stale operational bindings and the
  binding-difference hostile fixture, correct only the six-destination pre-existing workflow-PDF
  destination fixture, publish no new PDF, publish fresh r14, and regenerate
  current-source last. Only fresh exact-C9 L9, CI,
  CodeQL, and dedicated-v9 success permits the exact four-path R9; otherwise R9 remains unissued.
- [ ] Run Rust, Python, method/catalog, release/review, formal, PDF, security, identity, package,
  platform, and downstream gates proportionate to changed surfaces.
- [ ] Commit unsigned with human-only attribution, fast-forward push to `main`, and require terminal
  exact-SHA hosted CI/CodeQL before calling the batch closed.
