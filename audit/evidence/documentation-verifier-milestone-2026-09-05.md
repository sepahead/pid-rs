# Documentation and verifier recovery — 5 September 2026

Status: the local repair checks below passed. Exact-commit hosted and mainline acceptance remain pending.

The recovery starts from `ab0c6970050e1b87216fbd0846911143a92c3904` and its inherited
working changes. The changes correct Python typed comparisons, publication tooling, and current
documentation. They do not change an estimator, numerical result, Lean theorem, or scientific closure status.
Program A remains partial/open; Programs A--E closed remains 0 of 5. Exact C12 remains terminal.

## Decision and review scope

The [verifier council](verifier-publication-council-2026-09-05.md),
[recovery council](recovery-council-2026-09-05.md), and
[scientific council](scientific-continuation-council-2026-09-05.md) each compare ten distinct routes
and record fifty named review lenses. They formed their initial findings before receiving the
primary reviewer's conclusions. They share sources, tools, storage, and the model system. Their
reports are advisory coverage, not external human custody or fifty independent proofs.

The primary review selects verifier route 10 and recovery route 2: finish the coherent inherited
repair, preserve its failures, verify the actual artifacts, then publish a small milestone.
Ordinary software checks are the appropriate non-PID method for this repair. Recovery route 6
governs later fragment selection. No whole-tree replacement or automatic branch deletion follows.
Scientific route 4 is the next proposed formal packet: DNF/order correspondence with explicit
all-pattern quantification. It has no new proof execution or closure credit in this milestone.

The primary reviewer inspected the recursive comparator and its call sites, the current claim
boundaries, publication scripts, exact PDF reproduction, and declared visual scopes. The reviewer
also checked the council's recommendations against the actual repository. Agreement did not
replace those checks. The concrete findings and execution limits remain in the linked reports.

## Preserved bytes and current derivatives

A fresh private archive records all 1,162 visible tracked or untracked integration paths, with
exact bytes and file modes. A separate Git bundle preserves the integration history. The private
inventory records their locations. Neither local archive is described as off-host publication.
The validation checkout was cloned without hard links and compared with the integration payload.

The [superseded receipt](../archive/blueprint-visual-receipt-20260905/DISPOSITION.md) retains the
interrupted 30-page receipt verbatim and assigns it no current publication credit. The
[current receipt](pid-discovery-verification-durability-blueprint-visual-receipt-2026-09-03.md)
binds the 31-page PDF to fresh color/grayscale contact-sheet review and 19 individually inspected
high-resolution pages. Both isolated builder outputs equal the inspected PDF byte for byte.
The accepted 29-page mainline reference was recovered and rendered with the same renderer. No
corresponding page raster is identical; the two added pages were also reviewed.

## Failed routes and corrections

| Observation | Cause or limit | Correction and reconsideration condition |
|---|---|---|
| Four typed substitutions were accepted by the old Program-A checker after coherent resealing | Python value equality conflates Boolean/integer and integer/float values | The recursive comparator now requires exact type, shape, and value at the record and parsed-registry boundaries. The [failure record](../../claims/SX-CERTIFIED-AVERAGED-PID3-001/failures/python-status-type-coercion.md) preserves the exact old commit and four witnesses. Reopen if a causal typed counterexample survives. |
| The blueprint gate rejected the inherited failure-record digest before building | The current record and the wrapper's current binding differed | Rebind only the reviewed current bytes. Historical decision and archive bindings remain unchanged. |
| A bare commit-ID sentinel matched twice | The failure record names the same commit in its explanation and preservation section | Bind the unique sentence that identifies the pre-correction checker. Repeated factual references are not a defect. |
| The wrapper required an absent fault-class label | The revised narrative records the actual compatibility witness instead | Bind the exact Boolean/integer Möbius witness row. Retain the scientific rejection requirement. |
| A missing-link hostile fixture failed at source size instead of link semantics | The fixture changed Markdown without updating the newly enforced source/receipt bindings | Reseal only the deliberate hostile fixture's source size, source digest, and receipt digest. Require the production link predicate to reject it. No production guard is bypassed. |
| Apparent PDF table collisions and a missing sentence space | High-resolution image presentation suggested overlap that the PDF coordinates and individual 120-dpi images disproved | Preserve the initial alarm and its retraction in the visual addendum. No layout change is justified by that alarm. |
| An isolated catalog invocation failed to import `json_schema_subset` | The recovery runner used `-I` on a checker whose documented command uses ordinary Python script imports | Use the documented normal and optimized commands. This is an invocation error, not evidence that the catalog rejected its data. |
| The Rust build rejected the catalog's embedded forensic digest | The inherited catalog update had no matching software-identity artifact update | Review and update the current catalog reference under the software-identity contract, then rerun the identity and build gates. Identity equality supplies no scientific or compatibility claim. |
| Current operational Lean hashes differed from the inherited documents | The current document bytes changed while the mutable bindings remained old | Update only the current operational map after the final edits. Preserve immutable r14 execution and terminal C12 records. |
| The first complete PDF-set run stopped at the Lean version probe | Its fresh checkout lacked the pinned dependency closure; the probe timed out while Lake started a Mathlib clone | Restore the exact nine dependency revisions, verify their tracked state and toolchain, build the current project, and rerun the complete gate. The failed run is retained as an environment failure. |

The unresolved checkout-integrity incident remains in its [own record](sxpid3-pdf-checkout-integrity-incident-2026-09-04.md).
The current builder defenses have bounded hostile evidence. Their success does not identify the
old cause or prove safety against every filesystem race or an untrusted initial interpreter.

## Verification and publication boundary

The following local checks passed on the reviewed draft or its final publication inputs. The
current-only dependency repairs are listed in the [binding record](current-documentation-bindings-2026-09-05.md).

| Check family | Observed result and scope |
|---|---|
| Program-A semantic bridge | Normal and optimized checks and hostile suites passed. The four old exact-type false positives remain preserved; scientific closure is unchanged. |
| Method catalog, summary, and transfer ledger | Normal and optimized checks and self-tests passed after the documented invocation correction. |
| Current Lean freeze, certified SxPID2 claim, and KSG preservation | All 12 checker/self-test invocations passed with exact normal/optimized stdout and stderr parity. The suites rejected 145, 126, and 36 named mutations respectively. Their common input manifest covers 1,168 paths, with no source drift during execution. |
| Ecosystem, mathematical audit protocol, identity, and release scope | All 12 additional normal/optimized invocations passed. The generated views agree with their current authorities. |
| Rust workspace and feature matrix | Stable workspace tests passed. The ten further commands passed: formatting, all-target/all-feature Clippy, no-default/parallel/all-feature/release tests, and the four documentation/docs.rs builds. |
| Review-source release state | `check-release-state.sh review-source v0.9.0` passed. This is not a release or tag. |
| Lean dependency setup and project build | The actual compiler reported pinned Lean 4.33.0. All nine dependency revisions and their tracked state matched the manifest. The current project build passed with 2,219 jobs. Existing dependency build caches were reused; project build outputs were not copied. This is not cache-independent kernel replay of the whole dependency closure. |
| Complete formal PDF set | `check-formal-pdf-set.sh --exact` passed after dependency recovery. It includes the declared publication-link, source, hostile, paper, and exact-render gates. The final blueprint also passed its exact check and all 81 controls after the incident-record whitespace correction. |
| Final source hygiene | The reviewed diff has no whitespace errors. The final source-state procedure uses separate normal/optimized outputs, byte comparison, atomic installation, and checker/self-test replay before commit. |

The binding-gate input manifest has SHA-256
`b90880c496bdffa26ebbd352febc7f40d92fe0b7d4dcb00017d5fac388f7d2e6`.
The private custody record retains exact commands, outputs, input identities and recovery
locations. Those local observations are not off-host archival or hosted execution evidence.
The validation clone differs from the final integration only by subsequent reviewed publication
record edits and the incident-record whitespace/current-wrapper binding correction; the affected
blueprint gates were rerun on those final bytes. Source-state regeneration is last. Post-commit
state uses a clean exact checkout and an external artifact, not a self-referential committed receipt.

The separate review of existing publications found inherited label crowding in the workflow's
OR diagram and a need for a concrete count table in its primer. Those findings belong to the
next publication revision. Passing the reproducibility gates does not establish that every
existing explanation or figure has completed a new visual or pedagogical review.

No branch or worktree has been retired by this record. The recovery review found useful exact
successors for two old workflow branches and active process ownership elsewhere. A separate
fragment ledger, recovery drill, live ref check, and guarded action are required for retirement.
No consumer readiness, audited ASD-STE100 conformance, mathematical novelty, or complete project
closure follows from this documentation milestone.
