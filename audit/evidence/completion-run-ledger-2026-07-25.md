# pid-rs completion run ledger — 2026-07-25

## Purpose

This is durable coordination state for the completion run requested on 2026-07-25. It is process
evidence, not mathematical or scientific evidence. It must not be cited as proving a theorem,
validating an estimator, qualifying a consumer, or establishing novelty.

## Resume protocol after any context compaction

The mutable, authoritative resume pointer is now
[`completion-active-resume.md`](completion-active-resume.md). Read it first and follow its bounded
bootstrap. This ledger is the append-only historical record; repeatedly loading every old
checkpoint or every inactive claim packet dilutes the active context and is no longer the recovery
protocol.

The bounded bootstrap still enforces the same science: authenticate governing bytes, reopen the
goal/worktree/agent state, load the complete active claim revision and retained failures, then
continue `review -> plan -> edit -> implement -> test -> verify` through the applicable semantic,
mathematical, formal, numerical, statistical, executable, provenance, release, and downstream-
authority lenses. A digest mismatch, workstream switch, unresolved contradiction, or changed
claim revision expands the read set as specified by the resume pointer. Historical packets and
external reviews are loaded only when a live dependency, custody check, or adversarial question
requires them.

## Bound objective

Audit and disposition every actionable recommendation in
`/Users/torusprime/Downloads/first_pid_rs_audit_gpt5-6pro`, implement complete and justified
`pid-rs` and sibling-repository work, close or explicitly retain every obligation, run the full
verification matrix, obtain independent adversarial reviews, then commit unsigned without agent
attribution and push each affected `main` branch.

Active goal thread: `019f9ac7-1deb-7663-a780-160d540ceaed`.

## Frozen intake

- Initial `pid-rs` revision: `ca2eaf31ce7b719d04a43e0d6e1d2c21c6ff06a7` on `main`, equal to
  `origin/main` at intake.
- Initial worktree state: clean.
- Audit handoff Markdown SHA-256:
  `7df374bedee2e3792f8f2f51634801bf9df18ac6f305e57a9edf60c291748d36`.
- Audit handoff PDF SHA-256:
  `0998b9ef79d61c95dabaf06cfa2ed398c9566a747b136ec2584ee75689971c92`.
- The handoff audited `d4bb0e741ea71ecddfdd414920e3874825fefc2d`; current work must verify
  whether each frozen-source gap is already closed at the exact current revision rather than
  inheriting the handoff verdict.
- The handoff is recommendation input, not authority. Its model conclusions do not change project
  dispositions without new retained evidence.

## Non-negotiable scientific method

- Freeze exact claims, domains, quantifier order, assumptions, units, non-solutions, falsifiers,
  evidence classes, and completion checks before solving.
- Model the work as an AND/OR obligation graph and identify minimal shared critical cut sets.
- Use at least two genuinely independent routes for every major claim and at least three applicable
  PID audit families. Model-name diversity is not method independence.
- Preserve failed routes, minimal counterexamples, old claim revisions, first-result records, and
  negative mutations.
- Convert exploratory computation into exact reductions, rational or outward-rounded certificates,
  small checkers, mutation tests, and independent replay where applicable.
- Keep definition/provenance, exact-real mathematics, formal semantics, certified numerics, Rust
  conformance, statistical calibration, consumer qualification, and release identity as separate
  gates. Evidence cannot be promoted across gates.
- Never conflate SxPID, `I_min`, KSG, fitted-quantized SxPID, continuous shared exclusions, or
  partial/full PID3.
- Never infer population support, independence, no drift, or application validity from finite
  observed data alone.
- Negative SxPID atoms remain valid; no silent clamping or unproved epsilon threshold is allowed.
- Any numerical-result change needs analytic ground truth, an exact/certified oracle, or a cited
  theorem with a checked application map.
- Every load-bearing imported theorem needs immutable provenance, a typed source-to-local mapping,
  premise evidence, ambiguity handling, and an adjacent-arrow/source-blind falsification route
  where applicable.
- Claims of novelty, validation, formal verification, production readiness, or downstream authority
  remain prohibited until their exact gates close.

## Active phase

Phase 1 — inventory and critical audit — is complete. Phase 2 — claim-scoped implementation and
adversarial qualification — is in progress. The evidence-backed intake disposition is retained in
[`completion-disposition-matrix-2026-07-25.md`](completion-disposition-matrix-2026-07-25.md).

Completed independent routes include recommendation integrity, theorem/statistical/proof-to-code
gap analysis, exact KSG arithmetic, categorical SxPID architecture, cross-repository dependency
mapping, and external Fable/Opus attacks. Active routes cover the exact-count/event Lean bridge,
the newly separated `I_min` atom-reconstruction symmetry defect, and certified-SxPID evidence
binding. Route agreement is counted only after common dependencies are identified.

Orca orchestration was unavailable because the local Orca runtime was not running. Built-in
shared-workspace agents are being used instead; this limitation changes coordination transport,
not scientific acceptance criteria.

## Decision log

### D-001 — Do not adopt the handoff's priority ordering verbatim

State: accepted process decision.

Reason: the handoff's principal blocker was inability to retrieve the frozen tree and tools. The
current workspace contains the exact Git history, nine PDFs, Rust, Lean, Z3, scripts, and later
assurance artifacts. Every recommendation must therefore be re-evaluated against current bytes and
current replay results.

### D-002 — External model reviews are advisory

State: accepted process decision.

Reason: `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md` explicitly excludes model confidence and model
agreement as evidence classes. Fable 5 Max and Opus 5 Max will be used to generate attacks and
alternative designs, with retained receipts, but proof/certificate/test evidence remains
load-bearing.

### D-003 — Correct the live identity break before scientific implementation

State: accepted implementation-order decision.

Reason: two independent read-only audits and a coordinator replay found that current `main` cannot
compile `pid-core`. The failure is an exact raw-byte mismatch at an existing fail-closed boundary,
not a numerical or estimator disagreement. Repairing that boundary restores the executable test
surface without changing an estimand, API declaration, numerical result, or validation claim.

### D-004 — Harden the exact-log Lean gate before citing it as assurance

State: accepted implementation decision; evidence binding remains in progress.

Reason: the production checker pinned source/toolchain bytes and audited theorem axioms, but had no
mutation self-test. A green checker without adequacy controls could survive theorem renaming,
inventory weakening, or proof escapes. The new self-test first requires the unmutated gate to pass,
kills ten proof/statement/inventory mutations in isolated sources whose digests are deliberately
rebound, and retains a nested-comment/string positive control. It runs normally and under
`python -O`. This improves fail-closed sensitivity; it does not bind concrete SxPID events or prove
Rust refinement.

### D-005 — Permit the KSG harmonic arithmetic revision only as an explicit estimator revision

State: accepted conditional implementation decision under `KSG-INTEGER-HARMONIC-001` revision 1.

Reason: exact rational replay establishes the positive-integer identity and index map, while two
independently written finite replays of the source-symmetric range form reduce the observed frozen
corpus error and eliminate `nx`/`ny` bit asymmetry. The current and proposed binary64 outputs differ
widely, so this is not a transparent refactor. Every affected estimator identity, fixture,
resource contract, and downstream composition must migrate explicitly. Non-cancelling heuristics
remain on their general digamma path.

### D-006 — Split the `I_min` tie intake from atom-reconstruction source symmetry

State: accepted specification correction; implementation is blocked on a new frozen claim packet.

Reason: the N=7 exact branch tie is real but the public scalar exposes no minimizer identity. A
hostile exhaustive public-API replay then found a different defect that invalidates both model
reviews' symmetry rationale: among all 12,869 binary count tables with total at most eight, 2,896
source swaps differ in synergy, with a minimal N=4 exact-zero witness. The cause is left-associated
binary64 atom reconstruction, not the `f64::min` tie. No arithmetic change is permitted until the
new claim freezes the exact invariant, acceptable operation order, compatibility boundary, and
complete regression domain.

### D-007 — Gate pid-rs build jobs on an explicit software-identity preflight

State: accepted CI-ordering decision.

Reason: the Rust build script already rechecks the raw-byte bindings independently, so this does
not add a new evidence route. It makes the identity predicate the first named failure and prevents
every expensive pid-rs build/package matrix from starting when the same commit is already known to
have an incoherent forensic reference. The Linux preflight uses full Git history, stable Rust,
Python 3.11, the full package inventory, and the 75-mutation self-test. Formal/PDF jobs and the
separately locked certified-SxPID audit crate remain independent because they do not build the
published pid-rs workspace.

### D-008 — Give continuous PID2 synergy an exact represented-input meaning and fail closed

State: accepted conditional implementation decision under `PID2-REPRESENTED-SUM-001` revision 2;
final external/release/archive closure remains open.

Reason: the historical left-associated expression has no paper or estimand meaning and admits
fixed-bit source-order splits, including positive zero versus the least positive subnormal. The
shared exact accumulator instead returns the unique round-to-nearest-even value of
`J-I1-I2+R`. An independent counterexample also disproves accepted-input compatibility: with
separately rounded unique atoms and the unchanged 32-ordered-bit identity guard, a historical
one-ULP residual can compensate for unique-atom rounding while the exact synergy correctly fails
the guard. Revision 2 therefore permits a one-directional availability narrowing and prohibits
perturbing synergy or loosening the guard to retain arbitrary inputs.

The branch proof establishes `accepted(exact-only) ⊆ accepted(historical hybrid)`, with strict
inclusion on the retained all-power-of-two witness. The change can alter accepted synergy bits or
turn a former `Ok` into the existing typed numerical-instability error; it cannot recover an input
previously rejected by the hybrid. This is represented-input arithmetic, not estimator accuracy,
attainability, frequency, calibration, or formal refinement.

`exp0` independently repeated the historical formula and is included in the correction. The
release closure contains four families, not three, because
`pid-core.research.isx-heuristics` explicitly owns the Python `compute_pid2` exposure whose atom
output changes; its standalone Rust heuristic redundancy scalars remain unchanged. The catalog
closure contains six methods. This policy resolution is recorded rather than treating one
reviewer's narrower Rust-symbol inventory as silent agreement.

## Current-state handoff triage — independent-audit checkpoint

This checkpoint combines three read-only repository audits. It remains provisional only where the
Fable/Opus attack routes are still running.

- `F-001`, `W0.1`: obsolete in this environment. Commit `d4bb0e7...`, all nine PDFs, and the formal
  artifacts are present locally. The handoff's missing-material conclusion is not a repository
  property.
- `F-002`, `W3.2`–`W3.5`: reject the inherited `0/16` count as current evidence. The rows mix
  library, transport, hardware-in-the-loop, policy, and field-calibration obligations owned by
  different repositories. Honest external/HIL rows remain open rather than being synthesized.
- `F-003`, `W2.6`: already bounded. Full continuous PID3 remains compile-time and runtime gated,
  research-only, and explicitly lacks a general consistency claim.
- `F-004`, `W2.1`: accept the binary64 cancellation witness; exact-real atoms are zero while the
  current operation order leaves nonzero sub-ulp residuals. A stable semantic/API change remains
  open pending independent design review; the existing exact certifier must not be duplicated or
  misrepresented as `pid-core` conformance.
- `F-005`, `W2.2`: accept the exact `I_min` tie witness and binary64 branch difference. Severity and
  remedy remain under review because the public value is explicitly approximate and does not
  expose the argmin identity.
- `F-006`, `W2.3`: reject a default BigRational rewrite. The exact harmonic identity and a bounded
  exhaustive/stress oracle already exist; a runtime harmonic evaluation change needs separate
  error, performance, and compatibility evidence.
- `F-007`, `W2.4`: the theorem premise and sharp failure cases are already documented. A public
  typed dependence contract is a new research/API work package, not evidence that independence can
  be diagnosed from rows.
- `F-008`/`F-009`, `W2.5`: the repository now contains a stronger support-change-tolerant
  exact-real continuity result. It remains distinct from binary64 enclosure and statistical
  coverage.
- `F-010`, `W1.4`: the missing end-to-end refinement chain is real, but the repository already
  prohibits an end-to-end-formal-verification claim. Closing one narrow counts-to-certificate
  theorem is a frontier work package, not a repair that may be declared complete by more isolated
  lemmas.
- `F-011`, `W3.1`: superseded for current bytes by correction `C-IDENTITY-001` below. Binary
  attestation and a release event remain separate future gates.
- `F-012`–`F-014`, `W4.1`: current exact Python/Rust/Lean/certifier artifacts close the scoped
  relation-model firewall, lattice checks, and exact log-product obligations more strongly than
  the handoff could observe. They do not prove an end-to-end Rust refinement theorem.

Cross-repository authority decision: only Prisoma and Galadriel consume PID results. Crebain owns a
canonical observation envelope; Galadriel owns analysis provenance; Haldir may eventually consume
sealed advisory evidence in record-only/restrict-only modes but must never gain authority or a
direct `pid-rs` dependency. Galadriel's open PR/branch state and Haldir's dirty divergent checkout
are explicit stop conditions. Prisoma's old submodule pin cannot be advanced until a candidate
`pid-rs` revision is fully qualified.

## Correction claim packet — `C-IDENTITY-001` revision 1

- **Claim:** at revision `ca2eaf31...`, the raw SHA-256 of `method-catalog.json` differs from the
  method-catalog digest embedded in `software-identity-reference-v1.json`; the fail-closed identity
  checker and `pid-core` build therefore reject the tree. Rebinding the manifest to the unchanged
  final catalog bytes restores coherence without changing the public API-signature revision.
- **Objects:** exact raw repository bytes of `method-catalog.json`,
  `release-scope-1.0.json`, and `crates/pid-core/identity/software-identity-reference-v1.json`;
  SHA-256; the build-script and Python identity-checker validation paths.
- **Quantifiers:** the statement concerns these exact files at this exact Git revision and every
  checked build feature because the manifest is feature-independent.
- **Assumptions:** Git object retrieval and local raw-byte hashing are correct; no concurrent edit
  occurs during each bounded hash/check invocation.
- **Units:** inapplicable.
- **Conclusion:** replace only the stale catalog reference digest `7746f84b...` with the actual raw
  digest `eb428177...`; preserve the release-scope digest and all declaration/source/build identity
  fields.
- **Non-solutions:** weakening raw-byte equality, canonicalizing silently, bumping an unrelated API
  revision, changing the catalog to match an unknown intermediate digest, or merely bypassing the
  build assertion.
- **Falsifiers:** equality of the current raw and embedded hashes; acceptance by both checker and
  build before repair; a declaration/source/release-scope change caused by the one-field repair; or
  failure after the correctly rebound final bytes.
- **Evidence class:** exact digest comparison, historical Git-byte comparison, fail-closed checker,
  build-script rejection, mutation regression, and post-repair executable replay.
- **Completion check:** the identity checker and its mutation suite pass; an artifact-side stale
  binding mutation is rejected; the approved stable build/test path and release identity/scope
  checks pass; the diff changes no estimator or public declaration.

Obligation graph:

```text
raw final catalog bytes --SHA-256--> actual digest ----+
                                                      AND--> coherent manifest --> build/test surface
manifest artifact identity ----typed path/schema----+
release-scope raw bytes --------unchanged digest-----+
```

The minimal critical cut set is the raw-file digest computation plus the typed artifact-path
binding. Independent routes are: direct `shasum`, the Python checker, and the Rust build script;
historical Git inspection independently explains how the stale intermediate value entered the
commit but is not needed to establish the mismatch.

### Load-bearing correction record

- **Error:** embedded method-catalog digest `7746f84b...` does not identify the committed catalog.
- **Detector:** Python identity checker and Rust build assertion, preceded by direct raw hashing.
- **Smallest witness:** the two unequal 64-hex digests for one referenced file.
- **Dependency reach:** every `pid-core` build, all Rust/Python tests that build it, compiled
  software-identity reports, and release checks downstream of the build.
- **Load-bearing status:** yes for software build/release coherence; no estimator theorem or
  numerical value depends on the stale value.
- **Proposed correction:** bind the manifest to `eb428177...` only.
- **Regression:** mutate the referenced catalog bytes after binding and require checker/build
  rejection; rebind the final bytes and require acceptance.
- **Residual boundary:** this proves local raw-byte coherence, not compatibility, authenticity,
  reproducible binaries, scientific validity, or external attestation.
- **Old artifact digests:** catalog `eb428177...`; manifest `ff9a788f...`.
- **Pre-edit replay:** method-catalog checker passed; identity checker failed with the exact
  mismatch; `cargo test --locked -p pid-core --no-default-features` failed at `build.rs:323` before
  tests ran.

### `C-IDENTITY-001` implementation checkpoint

State: corrected and targeted-verification complete; final-tree release verification remains open.

Changes:

- rebound only the method-catalog reference digest to `eb428177...`;
- added artifact-side mutations that rewrite otherwise canonical catalog and release-scope bytes
  after manifest binding while retaining the stale manifest and require exact mismatch rejection;
- documented that catalog generation must settle before rebinding and that identity checker,
  mutation suite, and build-script path must all run; and
- added an `[Unreleased]` changelog record with the non-attestation/scientific boundary.

Post-edit raw manifest SHA-256: `9510e4f06f746a5049777472f4f68b0a66cf02b9f5d73a240b4b9fb64001640f`.
The catalog and release-scope raw digests remain `eb428177...` and `90fb0c1d...` respectively.

Targeted results:

```text
python3 scripts/check-software-identity.py                         PASS
python3 scripts/check-software-identity-self-test.py               PASS (75 mutations rejected)
python3 scripts/check-method-catalog.py                            PASS (69 methods, 45 references)
cargo test --locked -p pid-core --no-default-features --quiet     PASS
git diff --check                                                   PASS
```

The isolated mutations prove Python-checker rejection in both referenced-artifact directions. A
clean full no-default build independently exercised the build script's raw workspace hash. An
intentionally stale copied-workspace Cargo
build was not added because it would duplicate the same exact predicate at substantially greater
test cost. The final verification matrix must rerun both paths after every later catalog or
identity edit.

## External advisory review checkpoint

The frozen prompts and retained review records are:

- `fable5-completion-triage-prompt.md`, SHA-256 `653e9aad...`, and the explicitly non-verbatim
  `fable5-completion-triage-review-2026-07-25.md`; and
- `opus5-completion-triage-prompt.md`, SHA-256 `ea96212a...`, and the complete visible response in
  `opus5-completion-triage-review-2026-07-25.md`.

Fable used `claude-fable-5`, max effort, 71 turns, and 66,434 reported output tokens. Opus used
`claude-opus-5`, max effort, 122 turns, and 75,565 reported output tokens. Both independently
localized `C-IDENTITY-001` and reproduced the original SxPID and `I_min` boundary witnesses. Their
remedy recommendations remain advisory. In particular, later exact KSG replay contests Fable's
no-change recommendation, and later exhaustive `I_min` replay falsifies Opus's claim that the
whole public result is exactly source-permutation invariant.

## Exact-log checker adequacy implementation checkpoint

State: source/self-test/CI wiring implemented; full claim/catalog/paper binding and final-tree replay
remain open.

Implemented controls:

- mask nested Lean comments, line comments, and strings before scanning for live proof escapes;
- require the exact source theorem declaration inventory and the exact qualified checker audit
  inventory;
- reject nine source mutations and one checker-inventory mutation after rebinding each isolated
  source digest, so failures reach semantic/kernel checks rather than stopping at the outer hash;
- accept a nested-comment/string proof-escape decoy; and
- run the self-test normally and under `python -O` in CI and `just certified-sxpid`.

Targeted replay:

```text
python3 scripts/check-lean-exact-log-product.py                    PASS (7 theorems)
python3 scripts/check-lean-exact-log-product-self-test.py          PASS (10 mutations, decoy)
python3 -O scripts/check-lean-exact-log-product-self-test.py       PASS (10 mutations, decoy)
```

Permitted Lean axioms remain `[propext, Classical.choice, Quot.sound]`. The new controls establish
fault sensitivity for the generic algebra and audit inventory only; concrete SxPID event binding,
bytes, Rust, binary64, sampling, and application claims remain outside the theorem.

## Next durable checkpoints

- Finish binding the exact-log self-test into the certified-SxPID claim checker, method catalog,
  assurance paper, and generated views.
- Freeze the separate `I_min` atom-reconstruction symmetry claim and retain the complete exhaustive
  replay before implementation.
- Implement and close the KSG and count-event claim obligations without promoting finite or formal
  evidence across layers.
- Add accepted claim IDs, exact implementation work packages, files, tests, and critical cut sets
  before edits begin.
- After each implementation wave, append commands, results, failures, revisions, and residual
  boundaries.
- Before any commit, append the complete verification matrix and independent final-review
  disposition.

## Compaction-resume checkpoint — 2026-07-25, second active context

The required resume protocol was replayed before substantive work continued:

- `AGENTS.md` was read completely;
- all 1,054 lines of `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md` were read in bounded chunks;
- this ledger, the active goal, the worktree, and the completion disposition matrix were reopened;
- the complete active `IMIN-TIE-SWAP-001`, `KSG-INTEGER-HARMONIC-001`, and
  `SX-COUNT-EVENT-BRIDGE-001` packets, including both retained `I_min` failures, were reread;
- the revision-2 certified-SxPID claim, obligations, bindings, evidence map, decision, and negative
  controls were reread; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, and
  downstream-authority boundaries were re-applied before reviewing live edits.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active. The worktree contained only the
completion-run changes described above. The exact-log, `I_min`, and count/event agents remained
active in distinct work packages. The Fable 5 Max and Opus 5 Max `I_min` attacks were still running
with no visible response. Because `method-catalog.json` was under active evidence integration, the
software-identity manifest remained deliberately provisional; no build result obtained before the
final catalog rebind will be cited as final-tree evidence.

## Compaction-resume checkpoint — 2026-07-25, third active context

Before any further substantive edit, the required resume protocol was replayed again:

- all 317 lines of `AGENTS.md` and all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md` were read completely in bounded chunks;
- this complete ledger, the 107-line completion disposition matrix, active goal, and worktree were
  reopened;
- every file in the active `IMIN-TIE-SWAP-001` and `KSG-INTEGER-HARMONIC-001` packets was reread;
- both revisions, obligations, routes, evidence matrix, theorem map, and retained failures for
  `SX-COUNT-EVENT-BRIDGE-001` were reread;
- the revision-2 certified-SxPID claim, bindings, obligations, evidence matrix, theorem map,
  decision, and retained exact-log negative controls were reread; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  and downstream-authority boundaries were reapplied before integration resumed.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 1,716,449 reported tokens used at
reload. The worktree remained on `main` at `origin/main` plus only completion-run changes. The
count/event formal phase reported green at 263 declarations and 201 theorem-axiom audits, with nine
static and five isolated semantic mutations killed; its repository-wide D1 documentation/catalog
binding remained open. The `I_min` and KSG read-only adversarial audits were still active. The
Fable 5 Max and Opus 5 Max `I_min` review sessions remained running with no visible output. The
software-identity reference remained deliberately provisional until all catalog and release-scope
bytes settle, so no pre-rebind Cargo result will be promoted to final-tree evidence.

## Compaction-resume checkpoint — 2026-07-25, fourth active context

Before another substantive edit, the required scientific-method resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 413 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the 107-line completion disposition matrix, active goal, worktree, and multi-agent states were
  reopened;
- both revisions, obligations, routes, decisions, implementation checkpoint, evidence matrix, and
  retained failures for `IMIN-TIE-SWAP-001` were reread;
- the complete active `KSG-INTEGER-HARMONIC-001` packet, including its exact-numerics route memo
  and call-site map, was reread;
- both revisions, conventions, obligations, routes, formal map, evidence, retained failures,
  verification record, and final bounded decision for `SX-COUNT-EVENT-BRIDGE-001` were reread;
- the active revision-2 certified-SxPID claim, obligations, bindings, evidence matrix, theorem map,
  decision, and retained negative controls were reopened; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  and downstream-authority lenses were reapplied before continuation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 1,992,871 reported tokens used at
reload. The worktree remained on `main` at `origin/main` plus completion-run changes only. The
count/event bridge phase A+B and the KSG independent audit had completed; the latter returned a
conditional go with explicit identity, fixture, mutation, and final-matrix blockers. The `I_min`
audit remained active and supplied new portability, exact-rounding-boundary, regrouping, and
documentation attacks. The Fable 5 Max and Opus 5 Max `I_min` sessions were polled and remained
running without visible output. The method catalog and release scope are still unsettled, so the
software-identity reference remains deliberately provisional and no pre-rebind Cargo result will
be promoted to final-tree evidence.

## Compaction-resume checkpoint — 2026-07-25, fifth active context

Before substantive work resumed after the fifth context compaction, the complete scientific-method
resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 443 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the 107-line completion disposition matrix, active goal, worktree status, and multi-agent state
  were reopened;
- the active revision-2 `IMIN-TIE-SWAP-001` claim, obligations, routes, evidence, decision,
  implementation checkpoint, and four retained corrections/counterexamples were reread;
- the complete active `KSG-INTEGER-HARMONIC-001` claim, obligations, route registry, exact-numerics
  memo, call-site map, implementation checkpoint, evidence matrix, and conditional decision were
  reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the active revision-2
  `SX-CERTIFIED-AVERAGED-PID2-001` packet, including retained failures and explicit residual cuts,
  were reopened; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  portability, and downstream-authority lenses were reapplied before continuation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 2,275,649 reported tokens used at
reload. The worktree remained on `main` at `origin/main` plus only completion-run changes. The
catalog/assurance and release-scope migrations and the independent `I_min` implementation audit
were still active. The final method-catalog, release-scope, and software-identity bytes remained
unsettled, so no pre-rebind Cargo result will be promoted to final-tree release evidence. External
Fable/Opus outputs remain advisory, and every recommendation continues to require independent
adjudication against retained exact, formal, numerical, executable, and mutation evidence.

## Compaction-resume checkpoint — 2026-07-25, sixth active context

Before substantive work resumed after the sixth context compaction, the complete scientific-method
resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, all 472 pre-checkpoint lines of this ledger, and the
  completion disposition matrix were read completely in bounded chunks;
- the active goal, worktree status, and multi-agent state were reopened;
- the complete new `PID2-REPRESENTED-SUM-001` packet and retained source-order failure were read;
- the active revision-2 `IMIN-TIE-SWAP-001` claim, obligations, routes, evidence, decision,
  implementation checkpoint, and four retained corrections/counterexamples were reread;
- the complete active `KSG-INTEGER-HARMONIC-001` claim, obligation graph, route registry,
  exact-numerics memo, call-site map, implementation checkpoint, evidence matrix, and conditional
  decision were reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including their formal maps, failures,
  evidence boundaries, and residual cuts, were reopened; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  portability, and downstream-authority lenses were reapplied before continuation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 2,575,325 reported tokens used at
reload. The worktree remained on `main` at `origin/main` plus only completion-run changes. The
catalog/assurance and release-scope integrations were paused at the continuous PID2 exact-sum
decision gate, while the independent implementation audit remained active. Final catalog,
release-scope, and software-identity bytes therefore remain unsettled, and no pre-rebind Cargo
result will be promoted to final-tree release evidence. External Fable/Opus outputs remain
advisory and require independent adjudication against retained exact, formal, numerical,
executable, provenance, and mutation evidence.

## Compaction-resume checkpoint — 2026-07-25, seventh active context

Before substantive work resumed after the seventh context compaction, the complete scientific-
method resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 529 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the 107-line completion disposition matrix, active goal, worktree status, and multi-agent state
  were reopened;
- both revisions, obligations, route registries, decisions, evidence, acceptance proof, and all
  retained failures for `PID2-REPRESENTED-SUM-001` were reread;
- the active revision-2 `IMIN-TIE-SWAP-001` packet, exact/bounded evidence, implementation record,
  and all four retained corrections and counterexamples were reopened;
- the complete active `KSG-INTEGER-HARMONIC-001` packet, including its exact-numerics memo,
  call-site map, implementation checkpoint, evidence matrix, and conditional decision, was
  reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps,
  residual cuts, and retained negative controls, were reopened; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  portability, and downstream-authority lenses were reapplied before continuation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 3,056,414 reported tokens used at
reload. The worktree remained on `main` at `origin/main` plus completion-run changes only. The
method catalog, release scope, and software-identity reference remain deliberately unsettled while
the dedicated continuous-PID2 checker and final catalog integration finish. No pre-rebind Cargo
result will be promoted to final-tree release evidence. Fable and Opus reviews remain advisory;
their outputs can generate attacks but cannot close any mathematical, executable, statistical, or
release obligation without independent replayable evidence.

## Dedicated KSG external-attack checkpoint

After the KSG source/checker and 35-family release partition settled, two read-only max-effort
adversarial runs were launched against `KSG-INTEGER-HARMONIC-001` revision 1:

- Fable 5 Max prompt SHA-256
  `ffd9df7bec08d218bdf114ae1efb3b91c14e5ee53d02f7e2dfab892e258aedd5`, with mathematical,
  off-by-one, binary64, compensation, and counterexample emphasis; and
- Opus 5 Max prompt SHA-256
  `209eed840c40e32e1261020cb8349a8d66c5ab39ae140446af1a644bdde83eb7`, with call-graph,
  release-family, resource, portability, provenance, and downstream-authority emphasis.

Both runs are advisory and read-only. Their prompts require independent replay, explicit finite-
evidence boundaries, and concrete attacks; neither model name nor agreement can close X1. Final
receipts, visible-output digests, and independent adjudication remain open.

## Compaction-resume checkpoint — 2026-07-25, eighth active context

Before substantive work resumed after the eighth context compaction, the complete scientific-
method resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 576 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the 107-line completion disposition matrix, active goal, worktree status, and multi-agent state
  were reopened;
- both revisions, obligation graphs, route registries, decisions, acceptance proof, evidence
  matrix, revision index, and all retained failures for `PID2-REPRESENTED-SUM-001` were reread;
- the active revision-2 `IMIN-TIE-SWAP-001` packet, implementation record, exact/bounded evidence,
  and all four retained corrections and counterexamples were reopened;
- the complete active `KSG-INTEGER-HARMONIC-001` packet, including the exact-numerics route memo,
  call-site map, implementation checkpoint, evidence matrix, and conditional decision, was
  reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps, route
  memos, residual cuts, and retained negative controls, were reopened; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  portability, and downstream-authority lenses were reapplied before continuation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 3,336,572 reported tokens used at
reload. The worktree remained on `main` at `origin/main` plus completion-run changes only. The
catalog/assurance, PID2 checker, and release/identity workers remained active in separate work
packages. Catalog, release-scope, and software-identity bytes remain deliberately unsettled until
the strengthened PID2 evidence gate closes, so no pre-rebind Cargo result will be promoted to
final-tree release evidence. Fable and Opus outputs remain advisory attack generation only and
must be independently adjudicated against replayable exact, formal, numerical, executable,
provenance, and mutation evidence.

## Objective addendum — final Wibral/shared-exclusions audit intake

The user supplied a superseding audit package at
`/Users/torusprime/Downloads/pid-rs-wibral-audit-final` and explicitly refocused the completion
goal on the Makkeh--Gutknecht--Wibral shared-exclusions PID as the repository's primary scientific
object. This addendum expands the existing active goal without falsely marking it complete or
discarding any prior claim history. The controlling intake documents are the final short report
and integrated final report; Revision 2 and the prior audit remain provenance only where the final
sections supersede them.

Frozen intake digests at first stable read:

```text
7c4aec062863c88f496176188eaace3baaae06201e2c85aa2c1ed200ac1d1330  deliverables/pid-rs-wibral-first-principles-audit-final.md
92edc78c27df2f3766706e2a1618c32b7a0dbcf483ddd5d2005a5b4d96a14d70  deliverables/pid-rs-wibral-first-principles-audit-final.pdf
11cd7c7886f96e8e23ee8cd38aa5b7c38bd1faa098e3ac32dc0e5b001bbbd475  deliverables/final-short-report.md
a3ef89ab73220415f4b011b725c1a1790190a12ddbe4f06a4324e8742251c3fe  checksums/SHA256SUMS
78d1406b04b7c6e014a5d7bcb9460f3a64f76354925f81903a2b2d95df17fa75  checksums/MANIFEST.csv
```

The package is recommendation input, not authority. Its listed SHA-256 entries replayed cleanly,
but its own validator did not pass the distributed tree: `logs/pdf-inspect.json` contains a plain-
text PDF-inspection transcript despite the `.json` suffix, while `validate_package.py` attempts
strict JSON parsing of every `*.json`. The retained
`logs/semantic-validation-with-pdf.json` itself records this failure. The current Downloads copy
also contains two Finder-created `.DS_Store` files outside the checksum inventory, and the local
default interpreter lacks `fitz`; those are separate environmental/transport causes and must not
be conflated with the intrinsic mislabeled-log defect. No package claim, headline number,
counterexample, or proposed repair is accepted until independently replayed against current
`pid-rs` bytes and frozen in a revision-preserving claim packet.

The expanded execution order is: authenticate and read the newest report; re-derive the SxPID3
finite algebra, mixed-rank continuous obstruction, and rational log-linear closure through
dependency-diverse routes; map them to current Rust/Lean/SMT/certifier code; create exact claims
and retained falsifiers before edits; implement only justified MGW-focused work; then finish the
existing PID2/Imin/KSG integrations, release identity, consumer dispositions, final verification,
and small coherent milestone commits/pushes to `main`.

## Compaction-resume checkpoint — 2026-07-25, ninth active context

Before substantive work resumed after the ninth context compaction, the complete scientific-
method resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 646 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, skill constraints, native multi-agent state, and unavailable
  Orca runtime were reopened;
- both revisions, obligations, route registries, decisions, evidence, acceptance proof, and all
  retained failures for `PID2-REPRESENTED-SUM-001` were reread;
- the active revision-2 `IMIN-TIE-SWAP-001` claim, implementation record, exact/bounded evidence,
  route graph, decision, and all retained corrections and counterexamples were reopened;
- the complete active `KSG-INTEGER-HARMONIC-001` packet, including its exact-numerics memo,
  call-site map, implementation checkpoint, evidence matrix, and conditional decision, was
  reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps, route
  memos, residual cuts, and both generations of retained negative controls, were reopened; and
- the semantic, mathematical, formal, numerical, statistical, executable, provenance, release,
  portability, resource, and downstream-authority lenses were reapplied before continuation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 3,631,478 reported tokens used at
reload. The user's additional constraint is now explicit: every theorem, counterexample, bound,
and executable certificate must bind the Makkeh--Gutknecht--Wibral shared-exclusions functional's
actual domain, antichain/event semantics, source count, averaging law, support assumptions,
quantifier ranges, and resource limits rather than borrowing a result from another PID. A major
claim cannot close from one formal method or several wrappers around one shared lemma; applicable
closure requires dependency-diverse formal, exact, executable, numerical, and adversarial routes
with their common cut sets named.

The PID2 checker was being canonically formatted after expanding to 74 mutations and ten compiled
regressions; its first settled hashes were invalidated rather than reused. Catalog, release-scope,
and software-identity bytes therefore remain deliberately held until replacement hashes and the
catalog integration settle. No pre-rebind Cargo result will be promoted to final-tree release
evidence. External Fable and Opus reviews remain advisory attack generation only.

## Compaction-resume checkpoint — 2026-07-25, tenth active context

Before substantive work resumed after the tenth context compaction, the complete scientific-
method resume protocol was replayed again:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 684 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, native multi-agent state, and three still-running external
  advisory sessions were reopened;
- both revisions, obligation graphs, route registries, decisions, evidence, acceptance proof, and
  every retained failure for `PID2-REPRESENTED-SUM-001` were reread;
- the active revision-2 `IMIN-TIE-SWAP-001` packet, implementation checkpoint, exact/bounded
  evidence, and all retained corrections and counterexamples were reopened;
- the complete `KSG-INTEGER-HARMONIC-001` packet, including its exact-numerics route memo,
  call-site map, implementation checkpoint, evidence matrix, and conditional decision, was
  reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including their conventions, formal maps,
  route memos, residual cuts, and retained negative controls, were reopened; and
- the controlling final-audit sections for categorical SxPID3, the mixed-rank continuous
  obstruction, rational log-linear closure, and the proposed work program were reread without
  accepting their asserted independence or headline results as evidence.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 3,835,971 reported tokens used at
reload. The semantic, mathematical, formal, numerical, statistical, executable, provenance,
release, portability, resource, and downstream-authority lenses were reapplied. The PID2 assurance
checker then reached a replacement freeze of 76 mutations (11 checker, 37 textual source, seven
claim-packet, nine release, ten catalog, and two CI/just wiring mutations), with checker SHA-256
`8af4acc053828d3d25b3db853ddb112012ab458be5e913d0926c818a4f791cf5` and self-test SHA-256
`5677bfa823a0e5f6da95b46a1d252c2ed94a7454594d3ed62cf8ee53a97274a6`. These supersede every
earlier PID2 checker hash and mutation count. Catalog regeneration and the final software-identity
rebind remain ordered after those bytes; no pre-rebind Cargo result is final-tree evidence.

## Compaction-resume checkpoint — 2026-07-25, eleventh active context

Before substantive work resumed after the eleventh context compaction, the complete scientific-
method resume protocol was replayed:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 718 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, and native multi-agent state were reopened;
- both revisions, obligation graphs, route registries, decisions, evidence, acceptance proof, and
  every retained failure for `PID2-REPRESENTED-SUM-001` were reread;
- the active revision-2 `IMIN-TIE-SWAP-001` claim, implementation checkpoint, obligations,
  routes, decision, evidence, and every retained correction/counterexample were reopened;
- the complete revision-1 `KSG-INTEGER-HARMONIC-001` packet was reread while preserving it as
  historical evidence pending a revision-2 correction packet;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps, route
  memos, residual cuts, and retained negative controls, were reopened; and
- the controlling final-audit sections for categorical SxPID3, mixed-rank continuous geometry,
  rational log-linear closure, and the proposed work program were reread as recommendations rather
  than authority.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 4,119,047 reported tokens used at
reload. The semantic, mathematical, formal, numerical, statistical, executable, provenance,
release, portability, resource, and downstream-authority lenses were reapplied. The independent
mixed-rank specification route sharpened the report's overbroad “generically unequal” wording to
an exact equality criterion, supplied an exact positive averaged gap series with first-term lower
bound `3/256`, and preserved the categorical-MGW/stochastic-consistency firewalls. The completed
Fable KSG attack is advisory but identified two claims that require independent correction-ledger
replay: stale compiled parallel bit fixtures and a mislabeled uncompensated four-term route. No
fixture value will be silently re-frozen, and revision-1 prose will not be rewritten.

## Compaction-resume checkpoint — 2026-07-25, twelfth active context

Before substantive work resumed after the twelfth context compaction, the complete scientific-
method resume protocol was replayed again:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 750 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, and native multi-agent state were reopened;
- both retained PID2 claim revisions, their obligation graphs, routes, decisions, evidence,
  acceptance proof, and all four failures were reread while treating the discovered in-place
  revision-2 edits as a load-bearing process defect requiring exact byte restoration and a new
  revision 3;
- the complete revision-2 `IMIN-TIE-SWAP-001` packet and all four retained corrections and
  counterexamples were reopened;
- the complete revision-1 `KSG-INTEGER-HARMONIC-001` packet was reread as immutable historical
  evidence pending a revision-2 correction ledger and packet;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps, route
  memos, residual cuts, and negative controls, were reopened; and
- the controlling final-audit sections for categorical SxPID3, mixed-rank continuous geometry,
  rational log-linear closure, and their work program were reread as recommendations requiring
  independent derivation rather than as authority.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 4,387,473 reported tokens used at
reload. The semantic, mathematical, formal, numerical, statistical, executable, provenance,
release, portability, resource, and downstream-authority lenses were reapplied. Three independent
workers remain active on the KSG revision-2 audit, PID2 revision-3 adjudication, and final
release/identity preflight. Until their results are reconciled, no stale numerical constant will
be silently updated, no externally suggested sufficient condition will be promoted to a theorem,
and no pre-correction identity or Cargo result will be cited as final-tree evidence.

## Compaction-resume checkpoint — 2026-07-25, thirteenth active context

Before substantive work resumed after the thirteenth context compaction, the complete scientific-
method resume protocol was replayed again:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 782 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, Rust guidance, native multi-agent state, and external KSG
  advisory session were reopened;
- both immutable PID2 claim revisions, their obligations, routes, decisions, evidence,
  acceptance proof, and retained failures were reread after independently verifying the restored
  revision-2 hashes;
- the complete active revision-2 `IMIN-TIE-SWAP-001` packet and all retained corrections and
  counterexamples were reopened;
- the complete revision-1 `KSG-INTEGER-HARMONIC-001` packet was reread as immutable historical
  evidence pending a revision-2 correction packet;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps, route
  memos, residual cuts, and negative controls, were reopened; and
- the controlling final-audit sections for categorical SxPID3, mixed-rank continuous geometry,
  rational log-linear closure, and their work program were reread as recommendations requiring
  independent derivation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 4,659,460 reported tokens used at
reload. The semantic, mathematical, formal, numerical, statistical, executable, provenance,
release, portability, resource, and downstream-authority lenses were reapplied. The independent
PID2 audit confirmed that revision 2 must remain byte-exact and revision 3 must record the stale-
test correction, signed-zero and typed-error behavior, Python report-path coverage, and explicit
migration scope. It also independently falsified the proposed `16 * |J|` sufficient acceptance
condition with a finite overflow counterexample and found that the retained `5/155` witness kills
left-associated reconstruction but not Neumaier reconstruction. The latter compiled mutation
obligation therefore remains open rather than being promoted from failed search. The KSG audit
independently found that 12 of 13 frozen bit constants change, one remains identical, the revision-
1 compensation label is false, and executable generator/source-shadow/serial-parallel bindings
require revision-2 correction. No frozen revision will be edited in place, no failure-to-find will
be called proof, and no pre-correction Cargo or identity result will be cited as final-tree
evidence.

## Compaction-resume checkpoint — 2026-07-25, fourteenth active context

Before substantive implementation resumed after the fourteenth context compaction, the complete
scientific-method resume protocol was replayed again:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 821 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, Rust guidance, native multi-agent state, and still-running
  external KSG advisory session were reopened;
- all three PID2 revisions, their obligation graphs, route registries, decisions, evidence,
  acceptance proof, correction ledger, and retained failures were reread, and the five frozen
  revision-2 artifact hashes remained the preservation boundary;
- both `IMIN-TIE-SWAP-001` revisions, implementation record, exact/bounded evidence, decisions,
  and all retained failures were reopened;
- both KSG revisions, their exact theorem and count map, correction ledger, tiny witnesses,
  evidence matrices, decisions, and retained failures were reread without treating the newly
  written revision-2 packet as self-validating;
- the complete revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet, including conventions, formal maps, route
  memos, residual cuts, and negative controls, were reopened; and
- the controlling final-audit sections for categorical SxPID3, mixed-rank continuous geometry,
  rational log-linear closure, and their proposed work program were reread as recommendations
  requiring independent derivation.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 4,907,870 reported tokens used at
reload. The semantic, mathematical, formal, numerical, statistical, executable, provenance,
release, portability, resource, and downstream-authority lenses were reapplied. The KSG
revision-2 evidence packet preserves all revision-1 bytes and explicitly reopens generator
custody, live-shadow mutation, tiny-count, and literal serial/parallel obligations. The PID2
Neumaier route wording was corrected so an incomplete search is not described as covering the
complete binary64 domain; the behavioral discriminator or an actual equivalence proof remains
open. Catalog, release-scope, and software-identity bytes remain deliberately unsettled until the
KSG/PID2 implementation and serial-reference transitions are independently replayed.

## Compaction-resume checkpoint — 2026-07-26, fifteenth active context

Before substantive implementation resumed, the complete scientific-method protocol was replayed
again:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 856 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the active goal, worktree status, Rust guidance, orchestration preconditions, native-agent state,
  and interrupted local/advisory sessions were reopened;
- all three PID2 revisions, obligation graphs, routes, decisions, evidence, acceptance proof,
  correction ledger, and retained failures were reread with the frozen revision-2 hashes intact;
- the complete active revision-2 Imin packet and retained failures, both KSG revisions and their
  correction/witness/failure records, the completed count/event bridge, and the conditionally
  supported certified-SxPID2 packet with conventions, formal maps, route memos, and negative
  controls were reread;
- the controlling final-audit sections for categorical SxPID3, the mixed-rank continuous
  obstruction, rational log-linear closure, and their proposed work program were reread as
  recommendations rather than authority; and
- the partially created `SX-CERTIFIED-AVERAGED-PID3-001` revision-1 specification was inspected and
  remains proposed because its referenced conventions, bindings, routes, obligations, failures,
  evidence matrix, and decision do not yet exist.

Goal `019f9ac7-1deb-7663-a780-160d540ceaed` remained active with 5,279,264 reported tokens used at
reload. The semantic, mathematical, formal, numerical, statistical, executable, provenance,
release, portability, resource, and downstream-authority lenses were reapplied. Two implementation
workers hit their service-usage ceiling before returning a handoff, so their live partial edits
must be audited from repository bytes rather than trusted from agent summaries. The interrupted
Cargo and external-advisory process identifiers no longer existed and will be rerun from frozen
commands where needed.

A newly supplied advisory review proposed an accepted-then-times-16 PID2 overflow witness. Its
numerical content is a new attack to replay, but its interpretation is not adopted: the retained
`acceptance-monotonicity-v2.md` concerns exact-only versus historical-hybrid acceptance, whereas
the rejected revision-3 `16 * |J|` statement is an atom-to-joint scale condition. If the witness
survives exact and compiled replay, it may support both a cleaner counterexample to that condition
and a separately named scale-nonmonotonicity result. Neither result changes the
Makkeh--Gutknecht--Wibral functional.

## Compaction-resume checkpoint — 2026-07-26, seventeenth active context

This is a combined recovery record. The preceding, sixteenth active context completed the
required governing-document and packet reads but compacted before it could append a durable
checkpoint. It is therefore not represented as a separate completed checkpoint. Before any
substantive edit in this seventeenth context, those materials were replayed again from repository
bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 895 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust implementation skill, its nine reference chapters, and the orchestration skill were
  reread in full; the existing native shared-workspace agent tree was retained to avoid creating
  conflicting edit ownership;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, its lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, and every existing dirty path were reopened;
  the user's continuation authorizes work on the same objective but does not authorize replacing
  or falsely completing the lifecycle object;
- all three PID2 claim revisions, the frozen revision-2 artifacts, revision-3 correction ledger,
  acceptance-subset proof, obligations, routes, decisions, evidence matrices, and every retained
  failure were reread;
- both `IMIN-TIE-SWAP-001` revisions, implementation record, obligations, routes, decisions,
  evidence, and all retained counterexamples/corrections were reread;
- both `KSG-INTEGER-HARMONIC-001` revisions, call-site map, exact-numerics memo and erratum,
  behavioral witnesses, correction ledger, obligations, routes, decisions, evidence, and all
  retained failures were reread;
- the completed revision-2 `SX-COUNT-EVENT-BRIDGE-001` packet and the conditionally supported
  revision-2 `SX-CERTIFIED-AVERAGED-PID2-001` packet were reread in full, including conventions,
  bindings, formal maps, route memos, external advisory receipts, residual cuts, and negative
  controls;
- the completed prospective `SX-CERTIFIED-AVERAGED-PID3-001` packet was read in full, including
  its five exact failure records. Its disposition remains `proposed` with `no accepted evidence`:
  the packet freezes the categorical MGW object and a five-program closure plan but supplies no
  current producer/verifier, dual formal closure, compiled keyed refinement, or archive/custody
  result; and
- lines 124--408 of the controlling final Wibral audit report were reread as recommendation input,
  including the categorical SxPID3 results, mixed-rank continuous obstruction, rational
  log-linear closure, and proposed work program.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses were
reapplied. The independently completed KSG takeover audit reports clean targeted debug/release,
serial/parallel, mutation, tiny-witness, and resource replay, but it also finds stale revision-1
catalog/evidence bindings, an unprotected 32-epsilon policy constant, one textually killed but
behaviorally equivalent Neumaier branch mutation on the positive-prefix domain, and no
dependency-disjoint formal index/symmetry route. Those findings are attack input pending root
replay; they do not promote KSG arithmetic into categorical MGW semantics, estimator consistency,
support validity, or publication closure.

The PID2 scale attack remains split into three distinct propositions. The frozen
`acceptance-monotonicity-v2.md` compares exact-only and historical-hybrid constructors on the same
input; revision 3 separately rejects the atom-to-joint `16 * |J|` sufficient condition; and
coordinate scaling monotonicity is a new binary64/API proposition. The independently derived
accepted-then-times-two overflow witness is strictly sharper than the supplied times-sixteen
witness and must be retained as a separately identified post-freeze negative result if compiled
replay and evidence binding pass. Neither witness establishes estimator attainability, frequency,
or a failure of Makkeh--Gutknecht--Wibral shared-exclusions mathematics.

## Compaction-resume checkpoint — 2026-07-26, nineteenth active context

This is another combined recovery record. The preceding eighteenth active context completed the
Rust/orchestration skill reads, governing-document and ledger reads, goal/worktree inspection, and
all six live claim-packet reads, but compacted while requesting the controlling audit excerpt and
before it could append a checkpoint. It is therefore not represented as a separately completed
checkpoint. Before any substantive edit in this nineteenth context, the entire recovery protocol
was replayed again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 953 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust implementation skill, all nine of its reference chapters, and the orchestration skill
  were reread in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, its lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every dirty path, and all three live native
  agents were reopened; the user's continuation authorizes the same objective but does not
  authorize replacing or falsely completing the lifecycle record;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including all retained failures, route
  registries, formal maps, evidence matrices, decisions, and revision-preservation boundaries;
- lines 124--408 of the controlling final Wibral audit were reread in four deliberately bounded
  sections, covering categorical SxPID3, the mixed-rank continuous obstruction, rational
  log-linear closure, and the proposed work program, without accepting its headline independence,
  coverage, or completion language as evidence; and
- the semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
  executable, provenance, release, portability, resource, and downstream-authority lenses were
  reapplied before integration resumed.

Concurrent source state now includes a post-freeze PID2 coordinate-times-two counterexample and
new KSG Lean/SMT work in progress. Those artifacts are not accepted from agent summaries: their
exact bytes, normalization gates, formal statements, axiom/solver boundaries, compiled behavior,
mutation adequacy, and catalog/release reach must be independently inspected and replayed. The
catalog/release preflight remains green except for the known PID2 active-v3 binding gap; catalog,
generated methods, changelog, release identity, and software-identity bytes remain unsettled until
the PID2/KSG artifacts and checker inventories stop moving. No result from moving bytes will be
promoted to final-tree or release evidence.

## Compaction-resume checkpoint — 2026-07-26, twentieth active context

The nineteenth active context completed the full scientific-method replay, appended its durable
checkpoint, restored the integration plan, coordinated all three live agents, and began an
independent PID2 coordinate-times-two audit. It compacted after requesting an oversized combined
read of three files and before the root audit or test replay finished. Before any further
substantive work in this twentieth context, the complete recovery protocol was replayed again from
repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 993 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust implementation skill, all nine reference chapters, and the orchestration skill were
  reread in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, its lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every dirty path, and the live native-agent
  tree were reopened; the user's continuation authorizes the same objective but does not replace
  or falsely complete the lifecycle record;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including all retained failures, route
  registries, formal maps, evidence matrices, decisions, and revision-preservation boundaries;
- lines 124--408 of the controlling final Wibral audit were reread in four bounded sections as
  recommendation input rather than authority; and
- the semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
  executable, provenance, release, portability, resource, and downstream-authority lenses were
  reapplied before integration resumed.

The root will now finish the PID2 times-two arithmetic/source/checker/compiled audit, inspect and
replay the new KSG Lean and SMT artifacts, then migrate the exact catalog inventories and settle
generated-method and software-identity bytes. The release preflight remains read-only evidence:
its conclusions must be independently checked during integration. No binary64 counterexample is
promoted into a defect of the Makkeh--Gutknecht--Wibral functional, no formal conditional algebra
is promoted into estimator validity, and no moving-byte test result is promoted into final-tree
or release evidence.

## Compaction-resume checkpoint — 2026-07-26, twenty-third active context

The twenty-second active context fully replayed the Rust and orchestration skills, the governing
documents, this ledger, and every file in all six current claim packets. It received the final KSG
formal, PID2 scale-counterexample, and release/catalog handoffs; launched new read-only Fable 5 Max
and Opus 5 Max frontier attacks; and independently reread each packet. It compacted only when an
oversized request attempted the final controlling-audit excerpt, so no substantive edit or test
result was lost or silently promoted.

Before substantive work in this twenty-third context, the entire recovery protocol was replayed
again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 1,030 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, its lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every dirty path, and the native-agent tree
  were reopened; the user's continuation retains the objective but does not authorize a false
  lifecycle completion;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including every retained failure, formal map,
  route registry, evidence matrix, decision, and revision-preservation boundary; and
- lines 124--408 of the controlling final Wibral audit were reread in five deliberately bounded
  sections as recommendation input rather than authority, including the exact categorical
  SxPID3 diagnostics, mixed-rank geometry claim, rational log-linear closure, and proposed work
  program.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are now
reapplied. The strongest accepted categorical MGW bridge remains the supplied-count two-source
cumulative result; the 108-coordinate SxPID3 target remains proposed with no accepted producer,
independent verifier, dual-formal closure, compiled keyed refinement, or archive result. The KSG
Lean/SMT artifacts and PID2 times-two counterexample remain handoff claims until root replays their
exact bytes, scopes, mutations, and compiled behavior. The new external Fable/Opus runs remain
active advisory attacks and cannot close an evidence node by agreement. Catalog, generated-method,
release-identity, and software-identity bytes remain unsettled, so no result from those moving bytes
is final-tree or release evidence.

## Compaction-resume checkpoint — 2026-07-26, twenty-fourth active context

The twenty-third active context fully replayed all governing documents, both required skills, the
durable ledger, every current file in all six live claim packets, and the controlling Wibral-audit
excerpt. It appended its durable checkpoint, restored the integration plan, reused the release
preflight worker for an independent PID2 scale-counterexample red-team, and began a root-level
audit of the new KSG Lean/SMT artifacts. It compacted only while reading the Z3 checker scripts;
no substantive code, catalog, release, or identity edit and no test result was lost or silently
promoted.

Before further substantive work in this twenty-fourth context, the complete recovery protocol was
replayed again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 1,072 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, its lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every known dirty path, and the three active
  native agents were reopened; the user's continuation retains the objective but does not
  authorize a false lifecycle completion;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including retained failures, formal maps,
  route registries, evidence matrices, decisions, and revision-preservation boundaries; and
- lines 124--408 of the controlling final Wibral audit were reread in five bounded sections as
  recommendation input rather than authority.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are again
active. The strongest accepted categorical MGW bridge remains the supplied-count two-source
cumulative result. SxPID3 remains proposed with no accepted producer, dependency-disjoint
verifier, dual-formal closure, compiled keyed refinement, or archive/custody result. PID2 and KSG
handoffs still require root replay; model advisors remain advisory; and catalog, generated-method,
release-identity, and software-identity bytes remain unsettled until those replays and exact
inventories finish.

## Compaction-resume checkpoint — 2026-07-26, twenty-fifth active context

The twenty-fourth active context fully replayed the governing documents, required Rust and
orchestration skills, durable ledger, all six current claim packets, and the controlling Wibral
audit excerpt. It appended its checkpoint, restored the integration plan, received the interim
PID2 red-team result, and independently inspected the KSG Z3 checker, its self-test, and all three
SMT scripts. It compacted only because the combined parallel Z3 replay returned oversized,
truncated output. No result from that invocation is accepted, and no substantive code, catalog,
release, identity edit, or test result was lost.

Before further substantive work in this twenty-fifth context, the full recovery protocol was
replayed yet again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 1,111 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every known dirty path, and the three live
  native agents were reopened;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including every retained failure, formal map,
  route registry, evidence matrix, decision, and revision-preservation boundary; and
- lines 124--408 of the controlling final Wibral audit were reread in five bounded sections as
  recommendation input rather than authority.

The new Fable 5 Max frontier advisory completed during reload. It remains advisory: its proposed
three-source extension is not accepted evidence, and its alleged count-bridge revision-skew finding
was independently identified as false because the supplied context omitted the completed revision
2 packet. The correction and failed recommendation are retained. The Opus advisory and PID2
red-team remain active.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are now
reapplied. The next action is to rerun each KSG Lean and Z3 checker and self-test individually,
normally and under optimized Python, with bounded output. No outcome from the prior truncated call
will be promoted. The accepted MGW state remains the supplied-count two-source cumulative bridge;
SxPID3 remains proposed without an accepted producer, independent verifier, dual-formal closure,
compiled keyed refinement, or archive/custody result; and catalog, release, and software-identity
bytes remain unsettled.

## KSG formal root replay — 2026-07-26

The previously truncated combined invocation was discarded. Root reran all eight formal gates
individually with bounded output:

```text
python3 scripts/check-lean-ksg-integer-harmonic.py
python3 -O scripts/check-lean-ksg-integer-harmonic.py
python3 scripts/check-lean-ksg-integer-harmonic-self-test.py
python3 -O scripts/check-lean-ksg-integer-harmonic-self-test.py
python3 scripts/check-z3-ksg-integer-harmonic.py
python3 -O scripts/check-z3-ksg-integer-harmonic.py
python3 scripts/check-z3-ksg-integer-harmonic-self-test.py
python3 -O scripts/check-z3-ksg-integer-harmonic-self-test.py
```

Every command exited zero. Both Lean checker modes kernel-checked all 14 theorem declarations with
the exact recorded axiom inventory; both Lean self-test modes killed all 9 semantic mutations.
Both Z3 checker modes returned three positive `sat` preflights and three exact `unsat` negated
obligations under Z3 4.16.0; both Z3 self-test modes exposed and rejected all 8 mutants as exact
`sat`.

The replayed SHA-256 values are:

```text
d10fe6d4b93cf342dfffddde0a22ef096f175a864fe8d994fbc636932b96d0cc  scripts/check-lean-ksg-integer-harmonic.py
9bedc54b853282febf2445a6a2bb609f00d2a5cf2d56e82826fb7f65ca532895  scripts/check-lean-ksg-integer-harmonic-self-test.py
55fba0a04689d618ad163d731e2cb62702d7879d51c98aaaaf9d5213cc594606  scripts/check-z3-ksg-integer-harmonic.py
4032d87cf83c411487c30280d8c35942da632dd68d6f05768d44aa358eab4a4d  scripts/check-z3-ksg-integer-harmonic-self-test.py
812188bd1e0d76d8a19f4f2b410b566b6909c7bddb5b0024f6a272a4f240f943  audit/formal/lean-ksg-harmonic/PidKsgIntegerHarmonic.lean
8ae66c11fb66541bc47766b2682cf1e53d9b656aa0fa12e6945ac22057816ed4  audit/formal/z3-ksg-harmonic/ksg-digamma-cancellation.smt2
71ea8db97df43f51da89496a5e799bedc6216f9ede40368207d2ffed8df40fe1  audit/formal/z3-ksg-harmonic/ksg-index-maps.smt2
add0fc3a371c65433fdfd8b1e51d3182c6ef78db0cfd1d372f461f1d030e19a9  audit/formal/z3-ksg-harmonic/ksg-symmetric-range.smt2
```

Accepted scope: exact rational harmonic recurrence in Lean, plus conditional four-term
cancellation, min/max range reassociation, source exchange, and exclusive/inclusive index maps in
the two formal encodings. Remaining shared cuts are the unproved analytic positive-integer
digamma premise, the human claim-to-formal map, runtime count production/geometry, and the selected
binary64 implementation. Z3 harmonics are intentionally uninterpreted and its UNSAT results have
no independently checked proof certificate. This replay supplies no Rust refinement, KSG
consistency, population-support, calibration, categorical MGW-event, PID-atom, or consumer result.

## Compaction-resume checkpoint — 2026-07-26, twenty-seventh active context

The twenty-sixth active context fully replayed `AGENTS.md`, the mathematical workflow, this
ledger, the Rust skill and all nine reference chapters, and the orchestration skill. It reopened
the paused goal and dirty integration tree, listed all five current agents including the root,
and reread all five complete current packets plus most of the proposed SxPID3 packet. The PID2
audit supplied an interim checker-hardening result. The context compacted before the final three
SxPID3 files, controlling audit excerpt, durable checkpoint, and root PID2 replay; no substantive
code, catalog, release, or identity edit and no test result was lost.

Before substantive work in this twenty-seventh context, the entire recovery protocol was replayed
again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 1,198 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every known dirty path, and all five current
  agents including the root were reopened;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including all retained failures, formal maps,
  route registries, evidence matrices, decisions, and revision-preservation boundaries; and
- lines 124--408 of the controlling final Wibral audit were reread in five bounded sections as
  recommendation input rather than authority.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are again
active. The strongest accepted Makkeh--Gutknecht--Wibral state is unchanged: the supplied-count
two-source cumulative bridge is complete only in its bounded formal-semantics scope, while the
108-coordinate SxPID3 assurance target remains proposed without an accepted producer,
dependency-disjoint verifier, dual-formal closure, compiled keyed refinement, or archive/custody
result. The PID2 times-two overflow witness remains a post-freeze binary64/API counterexample with
no implication for the exact MGW functional, estimator attainability, or frequency. Catalog,
generated-method, release-identity, and software-identity bytes remain unsettled until the PID2 and
KSG audits, exact inventories, and final replay finish.

## Compaction-resume checkpoint — 2026-07-26, twenty-eighth active context

The twenty-seventh active context fully replayed `AGENTS.md`, the mathematical workflow, this
ledger, the Rust best-practices skill and all nine reference chapters, and the orchestration skill.
It reopened the paused goal and dirty integration tree, listed all five then-current agents
including the root, reread every file in all six current claim packets, reread lines 124--408 of
the controlling final Wibral audit, appended its durable checkpoint, restored the integration
plan, and messaged the three active workers. It compacted only when an oversized combined request
attempted to read PID2 source and tests. No substantive code, catalog, release, or identity edit
and no test result was lost after that checkpoint.

Before substantive work in this twenty-eighth context, the complete recovery protocol was
replayed again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 1,238 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every known dirty path, and all four current
  agents including the root were reopened;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including every retained failure, formal map,
  route registry, evidence matrix, decision, and revision-preservation boundary; and
- lines 124--408 of the controlling final Wibral audit were reread in five bounded sections as
  recommendation input rather than authority.

The fresh Opus 5 Max hostile review completed during reload. Its independently checkable
inclusion--exclusion, connective-blind reconstruction, certifier-to-Rust containment, mixed-rank,
and KSG shared-premise attacks remain advisory work items. Its broad gauge conclusions, claimed
absence of asymmetric/weighted Rust coverage, and fabricated composite quotation were rejected by
independent source and mathematical review. The KSG formal/catalog audit also completed read-only:
the narrow Lean/Z3 arithmetic package is coherent, while catalog, review-evidence, CI, `just`,
receipt, dependency-checkout, and wording integration remain open. The PID2 checker audit remains
active and reports 125 current mutations, 17 compiled tests, stale 76/10 public inventories,
scope-specific output overclaiming, an omitted 536-by-24 exact-sum oracle test, and incomplete
behavioral mutation coverage for the accepted-seed-to-times-two counterexample.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are again
active. The strongest accepted Makkeh--Gutknecht--Wibral state is unchanged: the supplied-count
two-source cumulative bridge is complete only in its bounded formal-semantics scope, and the
108-coordinate categorical SxPID3 assurance target remains proposed without an accepted producer,
dependency-disjoint verifier, dual-formal closure, compiled keyed refinement, or archive/custody
result. The PID2 times-two overflow witness remains a post-freeze binary64/API counterexample with
no implication for exact MGW mathematics, estimator attainability, or rejection frequency.
Catalog, generated-method, release-identity, and software-identity bytes remain unsettled until the
PID2 and KSG audits, exact inventories, and final replay finish.

## Compaction-resume checkpoint — 2026-07-26, twenty-ninth active context

The twenty-eighth active context fully replayed `AGENTS.md`, the mathematical workflow, this
ledger, the Rust best-practices skill and all nine reference chapters, and the orchestration skill.
It reopened the paused goal and dirty integration tree, reread every file in all six current claim
packets, reread lines 124--408 of the controlling final Wibral audit, appended its durable
checkpoint, restored the plan, and independently adjudicated the fresh Opus and KSG-agent final
reports. After those two workers completed, it spawned two additional read-only PID2 agents beside
the existing PID2 checker agent. It compacted only when an oversized broad `rg` attempted to read
PID2 source, checker, documentation, catalog, and release bindings together. No substantive code,
catalog, release, or identity edit and no test result was lost after the checkpoint; the truncated
search output is not accepted evidence.

Before substantive work in this twenty-ninth context, the complete recovery protocol was replayed
again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and all 1,290 pre-checkpoint lines of this ledger were
  read completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- goal `019f9ac7-1deb-7663-a780-160d540ceaed`, lifecycle status `paused`, reported usage of
  5,329,883 tokens, the `main...origin/main` worktree, every known dirty path, and the native-agent
  tree were reopened;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including all retained failures, formal maps,
  route registries, evidence matrices, decisions, and revision-preservation boundaries; and
- lines 124--408 of the controlling final Wibral audit were reread in five bounded sections as
  recommendation input rather than authority.

The three PID2 audits independently agree on the principal hardening defects: isolated checker
scopes print aggregate claims they did not test; the 536-case-by-24-permutation exact-sum oracle is
missing from the compiled baseline inventory; the post-freeze accepted-seed-to-times-two
counterexample has incomplete mutation coverage; and the six public method entries still bind
revision 2, leaving catalog and aggregate scopes red. The exact times-two arithmetic, finite-atom
endpoint, inclusive 32-position seed acceptance, overflow-midpoint rejection, typed-error path, and
Makkeh--Gutknecht--Wibral scope firewall remain independently supported. Frozen revision-2 and
revision-3 packet bytes must not be edited; all corrections belong in mutable checker, source docs,
catalog, assurance, and review-evidence layers.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are again
active. The strongest accepted Makkeh--Gutknecht--Wibral state is unchanged: the supplied-count
two-source cumulative bridge is complete only within its bounded formal-semantics scope, and the
108-coordinate categorical SxPID3 target remains proposed without an accepted producer,
dependency-disjoint verifier, dual-formal closure, compiled keyed refinement, or archive/custody
result. Catalog, generated-method, release-identity, and software-identity bytes remain unsettled
until PID2 and KSG hardening, exact inventories, and final replay finish.

## Instruction intake — comprehensive scientific PDFs — 2026-07-26

The user added a binding release requirement: scientific PDFs must preserve the complete scientific
record rather than present only final results. Each such PDF must include the exact definitions,
estimand and method identity, assumptions, quantifiers, domains, ranges and bounds, theorem and
counterexample statements, derivations, proof obligations, negative and open results, failed
routes, implementation correspondence, independent verification methods and their shared failure
cuts, hashes and receipts, limitations, and reproducible commands. A polished result-only synopsis
does not satisfy this requirement. Before release, every generated PDF must be checked for semantic
parity with its authoritative sources and rendered for visual inspection, with any omission,
clipping, unreadable formula, broken cross-reference, or missing negative result treated as a
release-blocking defect. This entry records the instruction immediately; the full thirty-first-
context recovery checkpoint follows after the mandatory replay is complete.

## Compaction-resume checkpoint — 2026-07-26, thirty-second active context

The thirtieth active context fully replayed the governing documents, durable ledger, Rust
best-practices skill and all nine chapters, orchestration skill, all six current claim packets,
goal, worktree, and agent tree. It incorporated all three final PID2 audits and added the
comprehensive-PDF requirement to the active plan. It compacted when the controlling audit excerpt
was accidentally requested in one oversized call, so that truncated output was rejected.

The thirty-first active context again read all 317 lines of `AGENTS.md`, all 1,054 lines of
`MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, all 1,341 then-current pre-instruction lines of this
ledger, the Rust best-practices skill and all nine chapters, and the orchestration skill. It
reopened goal `019f9ac7-1deb-7663-a780-160d540ceaed` in lifecycle status `paused`, the
`main...origin/main` worktree, every known dirty path, and the completed three-agent PID2 audit
tree. It immediately persisted the user's complete-detail scientific-PDF rule in the separate
instruction-intake entry above, then reread every file in all six current claim packets. It reread
lines 124--374 of the controlling final Wibral audit in four bounded calls. The context compacted
only when the fifth bounded call requested lines 375--408; that truncated output is rejected. No
substantive source, catalog, release, identity, formal, or PDF edit, no test result, and no commit
was lost after the replay. The user additionally required smaller, more frequent verified commits
and pushes, so this recovery/ledger artifact is the first standalone milestone.

Before substantive work in this thirty-second context, the complete recovery protocol was
replayed again from repository bytes:

- all 317 lines of `AGENTS.md`, all 1,054 lines of
  `MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md`, and every current line of this ledger were read
  completely in bounded chunks;
- the Rust best-practices skill, all nine reference chapters, and the orchestration skill were read
  in full before implementation or coordination decisions;
- the paused goal, `main...origin/main` worktree, every known dirty path, and the current
  native-agent tree were reopened;
- every current file in `PID2-REPRESENTED-SUM-001`, `IMIN-TIE-SWAP-001`,
  `KSG-INTEGER-HARMONIC-001`, `SX-COUNT-EVENT-BRIDGE-001`,
  `SX-CERTIFIED-AVERAGED-PID2-001`, and the proposed
  `SX-CERTIFIED-AVERAGED-PID3-001` packet was reread, including all retained failures, formal maps,
  route registries, evidence matrices, decisions, and revision-preservation boundaries; and
- lines 124--408 of the controlling final Wibral audit were reread in five separate bounded calls
  as recommendation input rather than authority.

The three PID2 audits and root source review agree on a truthful hardening target of 129 registered
mutations and 18 compiled baseline tests. The current 125-mutation suite is dormant because its
unmodified aggregate preflight stops at stale revision-2 catalog bindings; therefore it is not
reported as a successful current 125-mutation replay. All 56 current source mutants are textual,
nine advertised test removals merely rename tests, and exact-versus-Neumaier guard behavior
remains open. Frozen revision-2 and revision-3 packet bytes remain immutable; corrections belong
only in mutable checker, source documentation, catalog, assurance, review-evidence, release, and
identity layers.

The comprehensive-PDF rule is now a release obligation: every scientific PDF must retain exact
definitions, assumptions, domains and ranges, theorem statements, derivations, proof obligations,
counterexamples, negative and open results, failed routes, implementation correspondence,
independent verification methods and their shared cuts, hashes and receipts, limitations, and
reproducible commands. A polished result-only synopsis cannot satisfy the PDF gate. Rendered PDFs
must be visually inspected and checked for semantic parity before release.

The semantic, mathematical, combinatorial, formal, exact-arithmetic, numerical, statistical,
executable, provenance, release, portability, resource, and downstream-authority lenses are again
active. The strongest accepted Makkeh--Gutknecht--Wibral state remains the bounded supplied-count
two-source cumulative bridge. The 108-coordinate SxPID3 target remains proposed without an
accepted producer, dependency-disjoint verifier, dual-formal closure, compiled keyed refinement,
or archive/custody result. PID2 times-two remains a binary64/API counterexample only. Catalog,
generated-method, release-identity, software-identity, and PDF bytes remain unsettled until PID2
and KSG hardening, exact inventories, comprehensive rendering, and final replay finish.

## Compaction-resume checkpoint — 2026-07-26, thirty-third active context

The thirty-second context completed the full governing/skill/claim/audit replay, added and pushed
this durable ledger in unsigned/no-attribution commit `56fefea`, updated the milestone plan, and
then compacted while dispatching a read-only PID2 checker follow-up. The truncated dispatch output
was rejected. No source, checker, catalog, release, identity, formal, PDF, or test result was lost.

This context authenticated the goal, worktree, agent tree, `AGENTS.md`, the complete mathematical
workflow, the relevant ledger authority/decision/latest-state sections, the complete active PID2
packet, and the controlling final-audit passages. During recovery the user explicitly corrected
the context strategy: rigor requires relevant state, not repeated ingestion of every historical
packet. The new `completion-active-resume.md` therefore becomes the mutable first-read pointer.
It pins governing digests, the exact stop point, active claim and frozen hashes, settled facts,
open risks, next tests, and a deferred routing table. This ledger remains append-only history.

The revised protocol is a scientific-control improvement, not a relaxation. It still requires the
complete active claim revision and failures, five or more dependency-aware lenses, explicit
counterexample and mutation routes, and expansion on any digest mismatch or contradiction. It
deliberately excludes inactive Imin, KSG, count-bridge, certified-SxPID2, proposed-SxPID3, old audit,
and full external-model transcripts until a named dependency activates them. This prevents stale
recommendations and repetitive checkpoints from crowding out the source, proof obligations, and
tests currently under decision.

The active stop remains before the PID2 checker edit. A recovered read-only checker audit supports
isolating `source` from arithmetic, exact scope-specific stdout under normal and optimized Python,
and explicit routing of the omitted 536-case-by-24-permutation statistics oracle as compiled
baseline 18. Two additional read-only audits are examining the four proposed checker/model
mutations, honest textual-versus-compiled classification, Rustdoc, unchecked-public-field scope,
and debug/release evidence. Catalog/all remain intentionally red until six revision-3 method
bindings are migrated; exact-versus-Neumaier remains open.

## Instruction intake — publishable PID discovery/orchestration method — 2026-07-26

The user added a separate publication requirement: the scientific process and multi-agent
orchestration used to pursue PID mathematical breakthroughs must itself be documented at a level
suitable for publication. This is not satisfied by `AGENTS.md`, an internal run ledger, or a terse
final-results paper.

The accepted deliverable is a canonical methods paper plus a semantically paired and visually
verified PDF. Its scope includes exact PID-object firewalls, claim freezing, obligation
hypergraphs, critical-cut-set independence, role and information-flow separation, agent dispatch
and decision gates, counterexample and mutation design, formal/certificate/compiled/statistical
evidence layers, conflict and stopping rules, negative-result retention, durable recovery,
resource/reproducibility contracts, and worked pid-rs cases with both successful and failed/open
routes. It must expose threats to validity and may not imply that orchestration or model agreement
guarantees correctness. The detailed acceptance outline is now pinned in
`completion-active-resume.md`; authoring, hostile review, machine-readable schema checks,
citation/source audit, LaTeX parity, text extraction, and page-by-page rendering remain open
release milestones.

The active manifest now also carries the single long-horizon milestone queue, including the exact
next action and exit evidence for recovery, PID2, KSG, categorical SxPID3, the methods paper,
comprehensive PDFs, and final release/cross-repository work. This replaces reconstructing a plan
from dozens of historical checkpoints after compaction. An independent Rust API audit corrected
an overbroad draft statement before commit: `Pid2Result` is `#[non_exhaustive]`, so downstream safe
Rust cannot use a struct literal, but its public fields can be mutated after checked construction.
The invariant is therefore guaranteed at the checked return boundary, not by the type for every
later value.

## Advancing checkpoint — PID2 hardening verified; KSG dependency activated — 2026-07-26

The active manifest was stale after the preceding implementation run and has now been corrected
before another scientific edit. `HEAD` and `origin/main` both resolve to
`626ded7b24c62e24ee6cdda21b04bec63675272b`; the governing `AGENTS.md`, mathematical-workflow,
and final-Wibral-audit digests reauthenticated exactly. The lifecycle goal remains paused by the
goal service, but the user's explicit continuation instruction remains active. This checkpoint
does not promote a claim or declare a milestone complete.

PID2 revision-3 implementation and evidence hardening reached a coherent **combined dirty-tree**
state:

- all six checker scopes pass in normal and optimized Python;
- the self-test rejects 129 registered mutations with exact partition
  `24/56/22/9/16/2`, including 17 custody-first and 17 freshly hash-rebased semantic claim
  variants;
- two bidirectional scope-isolation preflights rule out arithmetic/repository false coupling;
- 18 distinct named compiled tests pass in debug and release, for 36 exact invocations;
- focused PID2/statistics integration tests, doctests, docsrs Rustdoc, method catalog, release
  scope, review evidence, and software identity passed on the combined bytes; and
- the exact times-two overflow-midpoint counterexample, inclusive 32-position acceptance
  boundary, typed rejection stage, field/path signed-zero behavior, and MGW non-implication were
  independently recomputed by native, Fable 5 Max, and Opus 5 Max reviews.

The external reviews are attack input, not authority. Fable session
`3ef39b39-826b-40b5-bb2a-6fbb647c1f3c` gave a conditional milestone GO; Opus session
`11729dfd-8606-4e78-9c3b-5ebfbf46dcb4` gave a milestone NO-GO. Their labels disagree, but their
load-bearing factual finding agrees and survives source inspection: the two checker-inventory
mutations read live checkout artifacts, while `method-catalog.json`, `METHODS.md`, and
`scripts/README.md` say copied artifacts. That provenance wording must be corrected. Additional
requests for revision-1 hash custody, copied-root baselines for every repository scope, mutations
for two pinned files and the eighteenth compiled-test name, exact diagnostic pinning, and a
compiler/target receipt remain explicit hardening candidates pending independent adjudication.
The active manifest itself was also a valid Opus blocker and is corrected by this checkpoint.

A read-only commit-isolation audit refuted the planned PID2-first order. Four final release
revision strings combine the KSG integer-harmonic change with PID2 exact represented-input
synergy; parent `626ded7` contains neither. Publishing those combined strings in a PID2-only commit
would be false. KSG must land first with intermediate KSG-only revisions for those four families,
after which PID2 can advance them to the combined revisions. The verified PID2 tree is therefore
held, not discarded or misreported.

Activating `KSG-INTEGER-HARMONIC-001` exposed a second evidence-state discrepancy. The live KSG
self-test rejects 91 mutations: four checker/model probes, two fixture/generator custody probes,
19 source/dataflow probes, and 66 release probes. Revision-2 documents state 85 because they
record six of those gates open: maximum-error tie custody, generator-drift/reseal custody, and
three shadow/overwrite dataflow defenses. The implementation result cannot be backfilled into a
claim packet after observation. Revision 2 remains frozen; the active decision is to issue a new
revision 3 with truthful 91-probe evidence and KSG-only intermediate release identities. Exact
`Fraction`, 8,198-cell Decimal, Lean, and Z3 routes currently replay green, including nine Lean
mutation kills and eight Z3 mutation kills, but their shared analytic premise and human
sign/index mapping remain an explicit common cut.

One whole-tree clippy run is retained as a negative result: it stopped on two
`needless_range_loop` diagnostics in the uncommitted Imin boundary test. A prior KSG-specific
diagnostic was resolved without changing audited dataflow by retaining the mathematical index
loop under a scoped explanatory allow. No KSG or PID2 scientific conclusion is inferred from the
unrelated Imin failure. Two native hostile audits are now examining KSG mathematics/formal seams
and the exact KSG-only release slice. The next advancing action is a non-destructive KSG revision
and isolated staged-snapshot replay, followed by a small unsigned push; PID2 is replayed only from
that pushed parent.

## Manual-resume handoff checkpoint — KSG revision 4 required — 2026-07-26

The user requested a very detailed Codex `/goal` handoff and then a clean stop. The new
`codex-goal-prompt-2026-07-26.md` is the copyable long-horizon objective; the paired
`completion-handoff-2026-07-26-ksg-rev4.md` is the exact state/evidence/next-command authority.
`completion-active-resume.md` now routes to both before its older active-workstream narrative.

Three hostile KSG audits changed the disposition materially. Exact mathematics, schema revision 2,
the bounded binary64 corpus, and W1/W2 remain GO on their stated domains. Revision 3 is NO-GO for
completion: it lacked claim-byte/semantic custody, a current revision index, a genuinely encoded
formal value bound, exact generated-view/identity coherence, and KSG-only staged-tree isolation.
Its files remain frozen. A retained pre-closure failure records the defects, and revision 4 must
supersede rather than rewrite it.

This turn strengthened the live but unsettled KSG routes: Python and Rust recompute the 240/114
endpoint partition from rows; both bind 150/354 nonzero ordinary-left evaluations over the selected
Neumaier prefix and zero negative zeros; production-private W1 diagnostics bind radius 79, ordered
counts `(4,1)`, and selected bits under both brute and kd-tree backends; release checking pins full
15 affected/20 protected objects and top metadata; catalog checking derives the exact 21-node
reverse closure minus the sole shared-config exclusion, requires evidence targets, and protects
unaffected methods/references/metadata. The planned pre-claim mutation partition is 142 plus two
scope preflights, but no settled full replay is credited.

Formal custody was repaired before stopping. Frozen `formal-assurance-v3.md` is restored to
SHA-256 `062d51b0...b08db25`; the unversioned and revision-scoped v2 Lean sources are byte-identical
at `812188bd...0f943`. A v4 Lean extension (`32b5d5e1...76ee4`) directly compiles and a v4 Z3 bound
file (`33c9bb7a...0df31`) directly returns `unsat`. The dedicated checkers still reference obsolete
v3 extension names and must be rewired before the earlier 19-theorem/14-mutant and four-proof/
11-mutant results can be replayed or credited.

An independent finite-field route found three primes above every corpus denominator for which all
354 structural endpoints reduce to zero and all 7,844 nonendpoints reduce to nonzero separately.
The proposed canonical certificate digest is `1d5f61b1...120bc`; prime `1,000,003` is retained as a
negative control because four exact-nonzero rows collide modulo that prime. This can prove an iff
classification only for the frozen 8,198 rational cells after a replayable checker and mutation
suite exist; it is not a universal harmonic-zero theorem.

The ambient tree remains intentionally contaminated by later PID2, Imin, SxPID/frontier, Python,
and PDF waves. No implementation commit was made. An ephemeral alternate-index/tree export at
`/tmp/pid-rs-ksg-only.o80cJV` still matched pushed parent `e96122b` exactly when the candidate agent
stopped; synthesis had not begun. Generated `METHODS.md` and release Markdown are stale, software
identity rejects the current catalog digest, formatting needs replay, and no compiled crate result
after the final bytes is creditable. The exact next work is revision-4 custody/formal/modular closure
and an isolated parent-derived KSG tree, not an ambient commit or a return to PID2.

## KSG revision-4 conditional formal milestone — 2026-07-26

The candidate was reconstructed from delivery parent `9bbcf5e`, then advanced by the durable
execution-plan commit `8bcf33f`. Frozen revision-2/revision-3 proof and claim bytes were
reauthenticated before revision-4 formal work. A proof-blind hostile review rederived the exact
tail bound, index maps, rational-to-real bridge, and `k=n` arithmetic endpoint. Its only material
finding was missing mutation coverage for the middle harmonic-order premise; that mutation was
added and exposes an exact satisfiable countermodel.

On settled bytes, both the working candidate and an anonymous staged-tree worktree passed the
eight Lean/Z3 checker and self-test commands in normal and optimized Python: 19 Lean theorem
inventories with only `propext`, `Classical.choice`, and `Quot.sound`; 14/14 Lean mutations; four
Z3 satisfiable premise preflights; four unsatisfiable negated obligations; and 12/12 Z3
countermodel mutations. Unsigned commit
`afc45ff27e5af7fe04e44f2bb9f4147fb472c81e` was fast-forward pushed to `main`. The exact
arithmetic result remains conditional on the positive-integer digamma identity and does not prove
compiled refinement, binary64 behavior, neighbor geometry, an estimator, support, continuous
shared exclusions, MGW PID, calibration, or downstream validity.

The independent modular route also settled after hostile replay. Its final canonical certificate
SHA-256 is `ae4645c3c9db7e8ad39d74edb1093114aeefc99d9ca9f41285ffffaeab277102`;
the earlier `1d5f61b1...120bc` value is retained only as a pre-artifact observation. Three selected
primes independently classify 354 structural endpoints as zero and all 7,844 nonendpoints as
nonzero on the frozen corpus. The rejected prime `1,000,003` retains four exact-nonzero residue-zero
collisions, so residue zero is never promoted to exact zero without separate structure. A
26-mutation suite passes in normal and optimized Python. This lane remains uncommitted pending
revision-4 claim custody.

A separate source-hostile review reproduced W1/W2, the 240/114 split, 354 positive zeros and no
negative zeros, the 150/354 ordinary-left discrepancy, maximum absolute error of eight epsilons,
40 ties, and source-swap parity. It found no arithmetic or dataflow defect but rejected merge
readiness for two release blockers: the heuristic-only general digamma helper causes
warnings-as-errors failures without `experimental-heuristics`, and four KSG-dependent frozen
serial vectors are stale. Root README prose also still states obsolete 96/256-epsilon limits.
Those failures remain open; repository/publication integration is NO-GO.
