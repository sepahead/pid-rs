# Comprehensive handoff — KSG revision 4 and long-horizon Wibral PID completion

## Handoff objective

Resume and complete the existing goal without replacing it:

> Finish `pid-rs` as publication-grade software and science centered on the
> Makkeh--Gutknecht--Wibral shared-exclusions PID; independently audit every inherited claim;
> complete the currently active KSG integer-harmonic milestone, then PID2 represented-sum
> hardening, categorical SxPID3 certificate programs, the research-process paper and complete-detail
> PDFs, release/identity closure, and only then authorized sibling-repository integration. Every
> promoted statement must name its mathematical object, domain, range, assumptions, quantifiers,
> units, evidence class, failure modes, and non-implications. Use multiple genuinely different
> verification routes, preserve counterexamples and failed routes, obtain external Fable/Opus
> attacks for consequential final candidates without treating model agreement as proof, commit
> unsigned without agent attribution, and push `main` in small coherent milestones.

This is a completion objective, not permission to call bounded evidence universal, transfer
results between PID definitions, weaken support contracts, hide negative atoms, or publish an
unfinished claim.

## First actions after manual resume

1. Read this file completely, then `audit/evidence/completion-active-resume.md` and the last delta in
   `audit/evidence/completion-run-ledger-2026-07-25.md`.
2. Query `get_goal`. The existing goal id is `019f9ac7-1deb-7663-a780-160d540ceaed`; the service
   reports it paused, but the user explicitly authorizes continuation. Do not replace its objective.
3. Run `git status --short --branch`, `git log -3 --oneline --decorate`, and list agents. Authenticate:

   ```text
   b576d5433f778715d904cd5b8657f7d7fce726419c391a8db8da2371b0f51bdb  live AGENTS.md at this stop
   717015b862995b1003d66badceccfc4535f5bb231681212a9b2ceff3b8204f94  MATHEMATICAL_PROBLEM_SOLVING_WORKFLOW.md
   7c4aec062863c88f496176188eaace3baaae06201e2c85aa2c1ed200ac1d1330  latest final Wibral audit Markdown
   ```

   `AGENTS.md` is dirty relative to `HEAD`; its live bytes include formal-wiring work and several
   future waves. Read it fully on resume even when this digest matches. If it differs, re-read and
   reconcile before acting. Do not confuse the dirty CI workflow hash with the mathematical-workflow
   hash.
4. Confirm `HEAD` and `origin/main` contain the durable milestone
   `e96122b56c15e895c081379210103d1a26eac25f` (`audit: advance durable KSG integration state`), or
   reconcile any later manual-resume commit before continuing.
5. Ask any surviving agents for their exact stop reports. Never credit tests that ran while inputs
   were moving.
6. Do not reload old audits wholesale. The active claim is KSG; load deferred PID2/I_min/SxPID
   packets only when that workstream becomes active.

## Mandatory scientific loop

For every consequential change, perform at least these five lenses and add more where applicable:

1. **Object/domain lens:** identify KSG arithmetic, an estimator, continuous Ehrlich shared
   exclusions, categorical MGW PID, `I_min`, or a project-defined wrapper; freeze domains,
   quantifier order, bounds, units, support assumptions, and non-transfer rules.
2. **Exact mathematical lens:** derive identities from first principles; check boundary cases,
   signs, order maps, cancellation, monotonicity, and construct falsifiers or smallest witnesses.
3. **Formal/certificate lens:** use Lean, Z3, exact rationals, modular certificates, or another
   mechanized route; expose all premises and shared cuts; mutate load-bearing statements.
4. **Numerical/compiled lens:** distinguish exact-real truth from binary64 association, signed zero,
   overflow, underflow, ULP-position language, debug/release, serial/parallel, and actual production
   dataflow.
5. **Provenance/release lens:** bind exact bytes, generator/fixture custody, catalog dependency
   closure, release identities, audience documents, software identity, Git phase isolation, and
   downstream authority.

For statistical claims also add estimand/support, finite-sample calibration, dependence/UQ, and
simulation-oracle lenses. For published artifacts add citation, semantic-parity, PDF rendering, and
page-by-page visual review. A green route may close only its stated obligation; correlated routes
sharing a premise, fixture, generator, source, or human transcription do not count as independent
proofs of that cut.

The iteration order is always:

```text
review -> freeze scoped claim -> plan -> edit -> implement -> test -> hostile review
       -> improve -> settled-byte replay -> staged-tree isolation -> commit/push -> durable update
```

Retain first failures, counterexamples, discarded formulas, inconclusive searches, and correction
history. Never rewrite a frozen revision after observing a result; issue a later revision.

## Current worktree and contamination boundary

The shared checkout is intentionally a multi-wave dirty tree. It contains KSG work plus later PID2,
I_min, finite-convergence/frontier, SxPID, Python, PDF, and audience-document changes. **Never run
`git add -A`, never commit the ambient tree, and never infer a KSG-only candidate from a green
ambient checker.** Preserve unrelated user/agent work.

The KSG release candidate must be synthesized from the pushed parent, initially `e96122b`, using an
alternate index or clean temporary worktree. Shared files such as `stats.rs`,
`parallel_bit_identity.rs`, `method-catalog.json`, release metadata, review evidence, ecosystem
capabilities, identity, CI, `justfile`, AGENTS, and `scripts/README.md` require parent-plus-KSG-only
reconstruction. Exclude all PID2 represented-sum, I_min tie/swap, exact-binary64-sum, frontier
convergence, and unrelated PDF changes.

An agent prepared but did not modify an isolated base export:

```text
root/tree       /tmp/pid-rs-ksg-only.o80cJV/tree
alternate index /tmp/pid-rs-ksg-only.o80cJV/index
pointer          /tmp/pid-rs-ksg-only-current-path
```

At stop, both the index and tree matched `e96122b` exactly and candidate synthesis had not begun.
Treat `/tmp` as ephemeral: verify it before use and recreate it safely if absent.

One combined dirty catalog version was preserved in the Git object store before KSG-only rebuilding:

```text
Git blob 6280b8e800dc56dbfbf84f33bfa934035e2c0ca2
SHA-256 63413141d1f7e17e7abdbccffc5d3a77a14ad9d22dca25922bae459f8d6e7ef8
```

Use it only to recover deferred PID2 catalog work; it is forbidden in the KSG-only milestone.

## KSG claim state at this stop

The exact arithmetic core is GO on its stated positive-integer domain. Repository integration is
NO-GO until revision 4 and an isolated candidate close the remaining obligations.

Frozen revision 2 hashes remain:

```text
2a114fca75c52d65410bc2b80bd561c7a1858035d5643a2d660044a53823f7f3  claim-v2.md
2c108aef29e833a6bf9f41968f917ad05b645606b377fc55ff3b0f9bccc1d389  obligations-v2.md
5cfe75c9572ee7742a2428dcd119018a6ae1bd92c7cfb1ed0bce5257f7691ab5  routes-v2.md
6b750c010a00debde29ec2b3959e1bd55751f7ebe9c136beac202503b1b6196c  evidence-matrix-v2.md
540d7f468bbcbc8771adeae8ce3ee103dad5d98d7bc5298a8c1e91a67a19fd26  decision-v2.md
0c65acef2b96bcac208be78a1d781bccb6c079b249076544d2227b3634e5b61b  correction-ledger-v2.md
e8e3d936d94bc25ed1eaa49e22d3cbdee0e65a649192f613e76dce8c22a99151  behavioral-witnesses-v2.md
1068d90dcfe7a20b5237305c0468a6a74eedeb5b91196ff6bfe9969dec300c10  formal-assurance-v2.md
```

Revision 3 was frozen pre-closure. Its primary pre-audit hashes were:

```text
457f55ef444b931cefa05d0dcb06d084cd51f510810080a80a30f0b9f5d59071  claim-v3.md
a2d29661b07a4b855c97ec6fb2e371bb4f422a1bdb3e24f5291a3022b49e889d  obligations-v3.md
ed1f9324eb537eb4e752d7b147942562290ab9f6aeeab453fa91f7d73c80d9bc  routes-v3.md
d17e8eed0f3944d2d4a8dd0e67cf44ffc7ddfb1a5d2194269d17a4003a9f6fa0  behavioral-witnesses-v3.md
062d51b03cbcfbfee9a16cba1e29ba3cb83480e6e48e603788828f917b08db25  formal-assurance-v3.md
e0f7badb2a5f929c3d91fd7193d2ab3fe4e9cf7a2ae83995b7465c2bae2a7724  implementation-v2.md
8d4f289d5b1ee9a10995bd8ae1bc086ae276812d1e09005c9006a730adab0949  correction-ledger-v3.md
```

Do not edit those bytes. `formal-assurance-v3.md` was accidentally expanded during the last turn;
the formal agent was instructed to restore the exact 1,985-byte/40-line file with SHA-256
`062d51b0...b08db25` before stopping. Verify that restoration first. The retained negative audit is
`claims/KSG-INTEGER-HARMONIC-001/failures/preclosure-audit-findings-v3.md`.

Revision 3 remains NO-GO because its index/custody/formal/mutation/phase/evidence statements were
not simultaneously true. Create revision 4; do not silently repair revision 3.

## Settled KSG mathematics and bounded numerics

Domain and maps:

- `n >= 2`, `1 <= k < n`;
- exclusive KSG has `k-1 <= nx,ny < n` and passes `x=nx+1`, `y=ny+1`;
- inclusive Ehrlich has `k <= x,y <= n` after its declared shell checks and passes them directly;
- only coefficients `(+1,+1,-1,-1)` are eligible; the non-cancelling heuristic remains on general
  digamma arithmetic.

Under the typed positive-integer premise `psi(m)=H_(m-1)-gamma`, exact algebra gives

```text
T = H_(k-1)+H_(n-1)-H_(x-1)-H_(y-1)
  = (H_(n-1)-H_(max(x,y)-1))-(H_(min(x,y)-1)-H_(k-1)).
```

With `D=H_(n-1)-H_(k-1)`, monotonicity gives `-D <= T <= D`. This is an exact-real local-term
bound, permits negative values, and is not a binary64-error, MI, redundancy, PID-atom, estimator
bias, calibration, support, or application bound.

Schema revision 2 is stable:

```text
generator SHA-256  a4ef8a87a154ad0e1edd84013f025462fe80c32e2012f07154bb8db8ca78143b
fixture SHA-256    560e36346272c845ad1cd443c13741738b06b02a8035ea43c8ced06b1d80147c
sidecar SHA-256    fb91172bdb767b3e11e15ef4e89bb0482b932c5c2450f87d566245eda87a8ec7
```

- 8,198 unique ordered cells = 6,920 exhaustive through `n=16` + 1,278 stress;
- 354 structural endpoints = 240 exhaustive + 114 stress;
- relative to schema 1, 352 strings changed: 26 tiny nonzero Decimal residuals became exact zero,
  326 zero spellings were canonicalized, and two were already `"0"`; no nonendpoint changed;
- selected Neumaier prefix plus sorted symmetric range: maximum absolute error `8*EPSILON` nats,
  allowed corpus ceiling `32*EPSILON`, 40 ties, first `(4096,1,2048,2048)`, zero swap asymmetries;
- all 354 selected endpoint outputs are `+0`, none `-0`;
- non-selected ordinary left association over the **same selected Neumaier prefix** is nonzero on
  150/354 endpoints and produces no `-0`; naive-prefix left is a different 121/354 result;
- W1 exact target is `107/210`, selected bits `0x3fe04e04e04e04e0`; the new private Rust test pins
  production radius `79` and ordered counts `(4,1)` in both brute and kd-tree backends;
- W2 uses the inclusive mapping and reaches the same local target; the public mean differs by eight
  ordered-binary64 positions from correctly rounded exact `71/840`. Do not call that eight ULPs.

An independent modular route selected primes `1_000_033`, `1_000_037`, and `1_000_081`, all larger
than every denominator. Fresh exact residues were:

```text
p=1_000_033: 7,844 nonzero residues; digest 931c30fab8560d5692121f3c16be42afa4e9d0b73e640ca4285f5352f4cfff9b
p=1_000_037: 7,844 nonzero residues; digest 09b6d9e5a4f9f5ee4346dbfc869ba254710f6198cba97f2ac3449db8adb16479
p=1_000_081: 7,844 nonzero residues; digest 20b2596be7ed67e9fb07039465196da9c289f87d0e13b87d85e8bcf964b18de0
canonical certificate digest: 1d5f61b1135b8bb69f6cf11c377ad8e9ba3ba3b806421bdff10a1d24355120bc
```

For each selected prime independently, all 354 endpoints were zero and all 7,844 nonendpoints were
nonzero: exhaustive split `240/6,680`, stress split `114/1,164`. Because denominators are nonzero in
each field, a nonzero residue proves the exact rational is nonzero. Retain the rejected-prime
negative control: `1_000_003` had four nonendpoint modular collisions and only 7,840 nonzero
residues (digest `d90959d7...54111`). Turn the selected set into a replayable, mutated bounded
certificate before promoting the corpus-scoped iff result; it is not a universal classification of
harmonic zeros.

## Live changes made after the last pushed milestone

The following are useful but not yet settled or committed:

- Python and Rust now derive the 240/114 endpoint split from fixture rows rather than trusting only
  metadata; a resealed endpoint migration mutant attacks the split.
- Python and Rust freeze the 150 selected-prefix ordinary-left nonzeros and zero negative-zero
  count; two checker-model mutants attack both numbers.
- `ksg.rs` has a private production-diagnostic W1 regression for brute and kd-tree; the source
  mutation swaps ordered counts.
- release checking now pins full affected/protected family-object projections and top metadata.
  Current projection digests are:

  ```text
  affected 15  a0c7f7f625e787a86d08435d8eb1fbcea0c045813efd774215b58c59a73271f2
  protected 20 3596fc9899e8f632f5165fe0138958919f41204d671b70484a5142bb1e72decb
  metadata      24e2f99f8e11d2e2270c77e92f9aa8b4bddecea24574fa39d8980e8616141d19
  ```

- catalog checking now derives the 21-node reverse dependency closure from raw/report KSG roots,
  removes exactly the non-numerical shared-config node to obtain the 20 affected entries, requires
  affected evidence targets to exist, and pins 49 protected methods, 45 references, top metadata,
  and forbidden later-wave tokens.
- intended pre-claim-route mutation partitions are currently
  `8 checker-model + 2 fixture-custody + 3 fixture-semantics + 20 source + 73 release + 36 catalog
  = 142`, plus two scope-isolation preflights. This number has **not** had a settled full replay and
  is not a final revision-4 result.
- normal and optimized `--binary64-only`, `--release-only`, and `--catalog-only` routes passed on
  the then-current bytes. A full self-test was deliberately interrupted after files changed and is
  invalid evidence.
- current raw file hashes before further revision-4 edits were
  `51404d5f...6c76c` for `method-catalog.json` and `4fe9e5e4...4319d` for
  `release-scope-1.0.json`. Software identity still pins an earlier catalog digest, so cargo builds
  fail closed until the final KSG-only catalog is settled and identity is repinned.
- `cargo fmt --all --check` was red only in newly edited KSG assertions when last inspected; format
  the KSG-only candidate and rerun. Do not format unrelated ambient waves into the milestone.

## Formal-route stop state

The formal agent reconstructed the exact revision-2 Lean source at
`audit/formal/lean-ksg-harmonic/v2/PidKsgIntegerHarmonic.lean` with SHA-256
`812188bd1e0d76d8a19f4f2b410b566b6909c7bddb5b0024f6a272a4f240f943`.

Its revision-4 source is
`audit/formal/lean-ksg-harmonic/v4/PidKsgIntegerHarmonic.lean`, SHA-256
`32b5d5e11aa244cb9683d71281f05b27e8093dd9a4d5e677ad4b1e68ffc76ee4`.
It compiled with 19 Lean theorem declarations and a planned 14-mutant suite. The
new theorems cover universal rational harmonic monotonicity, rational range components and bound,
a rational-to-real bridge, and a combined real theorem composing the typed digamma premise,
direct/range identity, cast, and inequalities. Z3 adds a fourth premise-explicit local-bound
obligation at `audit/formal/z3-ksg-harmonic/ksg-local-bound-v4.smt2`, SHA-256
`33c9bb7a13c9e8c0cc88ca1750b9510481b3f64ea4ecac8c7497e16d6850df31`, and plans 11 mutants.
`formal-assurance-v4.md` currently has SHA-256
`4afa871970f193724c30e42bfafce466d9d54dc08501d77802176a321f001ddb`.
Direct Lean compilation passed for the unversioned historical source, v2, and v4; direct Z3 v4
returned exact `unsat`. However, the dedicated Lean/Z3 checkers and self-tests still reference the
now-absent v3 extension names and old digests/mirror semantics. Update them to revision 4 and rerun
all eight normal/optimized commands before crediting the earlier 19/14 and 4/11 full results.

Formal boundaries remain:

- the positive-integer digamma identity is a typed premise, not derived from the analytic digamma;
- Z3's harmonic function is uninterpreted and the local order premises are explicit;
- Lean and Z3 share statement, sign, index, and analytic-premise cuts;
- neither route proves Rust refinement, binary64, neighbor geometry, KSG/Ehrlich validity, support,
  MGW PID, or application validity.

## Required revision-4 closure

Do not declare GO until all items are complete on settled bytes:

1. Restore every frozen revision-2/revision-3 file byte-for-byte and verify hashes.
2. Author revision-4 claim, obligations, routes, behavioral witnesses, formal assurance,
   implementation map, correction ledger, immutable evidence matrix, decision, and revision-index
   entries. State explicitly that revision 4 is post-result integration, not preregistration.
3. Add a `--claim-only` main-checker route that pins all retained active/historical packet bytes and
   validates semantic invariants. Its self-test must first reject an unmodified custody mutation,
   then rebase exactly one expected hash and still reject each semantic mutation, preventing hash
   masking.
   Prefer a canonical `active-packet-v4.json` with an ordered path-to-SHA-256 map and reviewed scalar
   facts. Pin its digest in the checker; let the pushed Git commit anchor checker plus manifest to
   avoid a digest cycle. Require exactly one active revision and mutate deletion, unresealed edits,
   hash-resealed semantic edits, domains, counts, formal inventories, and the MGW firewall.
4. Add exact Git phase isolation against the declared parent: exact changed-path allowlist, full
   blob pins for shared/high-risk files, forbidden PID2/I_min/exact-sum markers, and mutations. A
   KSG checker green on the ambient tree is insufficient.
5. Finish and mutate the modular endpoint/nonendpoint certificate, or retain it explicitly as
   external bounded evidence without promotion.
6. Point active catalog evidence to revision 4 and revision-scoped formal artifacts; require exact
   file existence. Preserve the shared config as the sole reverse-closure exclusion.
7. Regenerate `METHODS.md` from the final catalog and `RELEASE_SCOPE_1_0.md` from the exact 15/20
   release vector. Do not accept moving-tree output.
8. Rebuild KSG-only review-evidence constants, task dispositions, assurance registry, ecosystem
   capabilities, and software identity. `check-review-evidence.py` still had stale v1 KSG paths and
   T138; update only KSG records. Preserve the ecosystem base projection digest
   `63a843b4...` and change only authorized KSG bindings.
9. Reconstruct `parallel_bit_identity.rs` from the parent and recapture 13 KSG-only constants with
   parent PID2 code. Do not reuse combined-tree constants.
10. Run the generator no-write check, main checker/self-test normal and `-O`, Lean/Z3 checker and
    mutation suites normal and `-O`, exact catalog/release/review/ecosystem/identity gates, focused
    Rust tests in debug/release, serial/parallel bit identity, formatting, clippy, rustdoc, stable,
    all-feature, release, Python, and release-audit gates as applicable. Record every command,
    exact tree, toolchain, result, and any failure; rerun after the last byte changes.
11. Obtain final source-blind/proof-blind Fable and Opus hostile reviews with generous budgets from
    configured `.env` keys. Verify every suggestion independently; transcripts are advisory.
12. Build an isolated commit from the declared parent, compare the candidate tree to the reviewed
    alternate-index tree, commit unsigned with no agent attribution, push `main`, and add a
    follow-up receipt/decision commit if needed to bind the first commit without a self-reference.

## Later milestones — do not activate prematurely

1. **PID2 represented-sum revision 4.** Replay against the pushed KSG parent. Preserve the exact
   accepted-then-times-16-rejected threshold witness and the stronger family for every
   `k=1..1023`; use “ordered-binary64 positions,” correct live-vs-copied mutation prose, add custody,
   toolchain, and test-name hardening, and never imply a Wibral-mathematics defect. The exact versus
   Neumaier guard equivalence remains open.
2. **Categorical MGW SxPID3 Programs A–E.** Bind all 108 coordinates through independent event,
   count, Möbius, certificate, compiled, and archive routes. Preserve negative atoms and exact
   zero/product-one/count-weighting counterexamples. This is the primary next scientific PID
   milestone after infrastructure closure.
3. **Frontier theory.** Reopen mixed-rank continuous support only with an explicit estimand and
   replacement theorem; the common-radius smooth-torus bridge is refuted. Never transfer `I_min`,
   KSG, or fitted-quantized results to MGW shared exclusions without a mapping theorem.
4. **Publishable process paper and PDFs.** The scientific/agent orchestration protocol itself must
   be a reproducible methods artifact with literature review, dependency-aware evidence accounting,
   case studies, threats to validity, exact schemas, and negative mutations. Every PDF must contain
   full definitions, assumptions, domains, derivations, counterexamples, negative/open results,
   implementation correspondence, hashes, and replay commands—not only conclusions. Extract text,
   compare semantic parity, render every page, and visually inspect every page.
5. **Release and cross-repository work.** Only after pid-rs settled gates, inspect each sibling
   sepahead repository independently, respect its instructions and authority boundary, make only
   necessary authorized changes, and commit/push each `main` in small coherent milestones.

## Stop rule for the handoff agent

Continue autonomously while safe, scoped work remains. Stop and ask only for a genuinely material
choice, missing authority, or protected external action. Do not stop because the task is large,
because a search failed, or because one route is red; retain the failure, reopen the smallest
obligation, and continue. Mark the lifecycle goal complete only when every requested milestone and
cross-repository obligation is genuinely finished. Mark it blocked only under the tool's repeated
blocking-condition rule.
