# KSG M1a composite-v8 operational boundary

This record describes two bounded operational repairs. It does not change a PID or KSG formula,
Lean theorem, estimator, numerical result, scientific claim, the published composite-v7 PDF
family, or the pre-existing 83-page mathematical-workflow PDF/source.

## Published predecessor and observed defect

The predecessor is exact commit
`23b69abafb4bfdaab4b2321eb6cee7be7e1cd32e`, tree
`bb952adb0d24657a75458372d5fff1e45ad2ce81`. Its dedicated composite-v7 push run
`32257017103`, attempt 1, completed with failure. Its sole job `96080963533`,
`Validate the composite-v7 bounded correction contract`, failed at step 1, `Set up job`.
The exact raw job-log response is 1,843 bytes with SHA-256
`b935661a497e3dc257b7ba552c866d1ff52bc8269182a75f961b3e9dfa193b01`. The exact hosted
marker names the 39-hex action ref
`043fb46d1a93c77aae656e7c1c64a875d1fc6a0` and says all actions must be pinned to a
full-length commit SHA. The missing hexadecimal character is the bounded causal inference used by
the repair; the raw marker itself remains separately bound.

C7 repository CI run `32257016662`, attempt 1, separately completed with failure: 44 of 45 jobs
succeeded, while sole failed job `96080962981`, `Formal LaTeX / PDF inventory and cross-toolchain
structure`, reached step 11, `Rebuild papers and check cross-toolchain text, geometry, fonts, and
workflow renders`. Its exact 126,388-byte raw failed-job log has SHA-256
`b760292f9e448ce4e83148adc9a7675697193cac4904716eb450494f4646ff9c`; the terminal gate marker
is `mathematical workflow PDF check: extracted report text/layout changed across toolchains`.
That run retained coverage, source-state, and SBOM artifacts only, not its generated workflow PDF,
extracted text, or a diff. The marker proves a reached strict layout-comparator failure; it does not
classify the committed PDF content as defective. This portability defect was inherited from the
workflow publication/gate introduced at v4 and was first revealed at C7 after earlier generations
failed before reaching this comparator.

The no-checkout/no-repository-command fact applies only to the dedicated setup-failed run. The C7
local recorder remains a satisfiable retained tool, but no L7 record is installed and no L7 record
receives credit. R7 is permanently unissued. This statement does not claim that an operator never
invoked any local command outside the durable record.

## Bounded repair

Composite-v8 changes the upload action ref by one hexadecimal character, to
`043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`. On 2026-08-19, the public
`actions/upload-artifact` Git refs `refs/heads/main`, `refs/tags/v7`, and
`refs/tags/v7.0.1` were observed at that same 40-hex object. This is source-identity custody for the
selected action ref, not an authenticity, reproducible-build, or runtime-behaviour claim.

The v8 gate binds the complete current CI, retired-v7, and live-v8 workflow bytes. It also applies a
case-folded raw occurrence check to the reviewed upload-action route, requires the exact canonical
`uses:` lines, and proves that event/attempt refusal precedes qualification checks. A separate
repository-wide guard requires every external action declaration to use a lowercase 40-hex commit;
only the upload-action route has the exact source pin reviewed here. The guard deliberately uses a
closed, narrow workflow syntax policy rather than claiming to implement general YAML.

The second repair changes only the mathematical-workflow portability gate and its hostile suite.
Same-toolchain `--exact` continues to require byte-identical PDFs and receipts. Cross-toolchain mode
requires strict UTF-8 with exactly 83 terminal form-feed page partitions; byte-identical default
Poppler extraction; exact per-page layout-token frequency and order after collapsing only ASCII
space/tab/line-feed runs, except that the unique `Comparison result` label on page 10 and unique
`Checker` label on page 12 may move only across their exact adjacent diagram anchors; and the
existing page, geometry, navigation, font, and bounded same-renderer color/grayscale raster gates.

A later correlated native-arm64 Ubuntu 24.04 diagnostic reproduction—not C7 hosted evidence or an
independent replication—produced two byte-identical 824,549-byte builds with SHA-256
`01c16fa6a84ca28594c5d0b579c0be2971b5210f95964af625bc8a4d87e37497`. Built and committed
default extractions were byte-identical at 255,339 bytes and SHA-256
`2679543e22af0b2f0a5c0ceadc50d9a23910503c23e9cb288d8caf8d73f4e3e1`; layout projections
differed only on pages 5, 10, and 12, page 5 equalized under admitted ASCII layout whitespace, and
pages 10/12 differed only by the two bounded adjacent-label relocations. Per-page token frequencies
matched all 83 pages, and both color and grayscale raster gates passed. This supports a gate-only
repair; it does not prove identical layout, mathematical correctness, accessibility, renderer
independence, or cross-toolchain PDF byte identity.

## Fresh qualification and non-transfer

C8 must be the exact unsigned direct child of C7. R8 is permitted only as C8's exact unsigned direct
child after all of the following exist and agree on the exact C8 commit and tree:

1. a fresh Darwin arm64 L8 record from the fixed `just ksg-composite-v8` command, including the
   workflow-PDF hostile suite and same-toolchain `--exact` gate;
2. fresh attempt-1 success observations for repository CI, CodeQL, and the dedicated v8 workflow;
3. a fresh r13 Lean replay and current-source state for C8; and
4. a receipt derived from those exact records.

C7 hosted observations, the absence of L7, and retained r12 have zero C8/R8 qualification credit.
R4, R5, R6, and R7 remain permanently unissued. The v8 boundary adds no new PDF: the exact C7
composite publication family and the pre-existing mathematical-workflow
Markdown/LaTeX/SVG/PDF/rendering-receipt bytes remain unchanged. Only the latter's portability gate
and process documentation change.

## Durable Git workflow

Work is staged as two direct-child commits so useful bytes cannot be stranded in a worktree or
side branch. First, land the source-only C8 repair on `main` and verify local and remote `main` at
that exact commit. Then capture L8 and the fresh hosted observations against C8, derive the four-file
R8 evidence cut (current-source modification plus L8, hosted capture, and receipt additions), and
land R8 directly on `main`. Only after both commits are reachable from local and remote `main` may
temporary branches and worktrees be removed. Every cleanup starts with read-only ancestry, status,
and unpushed-commit checks; cleanup itself is not qualification evidence.
