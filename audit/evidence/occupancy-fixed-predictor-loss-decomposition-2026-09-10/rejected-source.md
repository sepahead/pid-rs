# Rejected calculator source

The [retained text](rejected-calculator.py.txt) is an inactive source preimage.
Do not run it or use it as current calculation evidence. Its 5,987 bytes have
SHA-256 `125ceb1aa1a07da03c2da7cb5f512f638064537b6a02376dff06ac1f32e9d144`.

The draft assumed every recording contained historical predictor losses.
The original training record instead has an empty `predictive` object. A source
review caught the missing-key access before execution. No native run or numerical
result is attributed to this rejected source.

The [successor](calculate.py) requires that training absence explicitly. It
reports five training residuals and seven per evaluation recording, without
inventing historical training values. The [evidence note](../occupancy-fixed-predictor-loss-decomposition-2026-09-10.md)
records the actual successful run and its boundaries. Reconsider a historical
training comparison only if a separately preserved original score source becomes
available; recomputing a value now does not create historical evidence.
