# Public packaging replay evidence

This packet records the separate local replay of the portable DNF/order package. The selected
source passed both Python modes, each with thirteen actual semantic records and a fresh kernel
check. The unchanged control suite passed in both modes. A separate regression restored the
failed reporting module against the accepted imports and observed the thirteen original monad
mismatches. The exact scopes and resource outcomes remain in the projected receipts.

The campaign used two full selected runs, two control runs and one separate reporting regression.
It used no retry and made no candidate edit. It does not add credit to the earlier seven-run
campaign or claim an additional independent selected pair. The original three failures,
after-candidate judge amendment, ordinary-decide repair and four successful source/mode results
remain in the original formal-verification packet.

`CURRENT_PUBLICATION_MANIFEST.json` describes the reviewed reader revision at the106 original
public paths. The earlier `dnf-order-formal-verification-2026-09-05/MANIFEST.json` remains the
historical packaging input inventory. Its README and exposition bytes are preserved inert in
`history/*.md.txt`; `history/PREIMAGES.json` maps their original paths to those exact preimages.
Interpret links inside those inert sources relative to their original paths. Use the current
lane README and exposition for reading. The unchanged20-input `replay-v1.json` binds executable
replay inputs and excludes reader prose.

The five JSON receipt projections preserve result fields, commands, resource observations,
chronology, source digests and artifact inventories. Machine-specific repository, stage, run,
toolchain and Python paths become explicit role labels. Device/inode/mode/time identity arrays
are omitted; exact captured byte sizes and SHA-256 values remain. Raw-local receipt hashes bind
the retained originals, while this packet's manifest binds the public projected bytes. They are
different objects. The private prefix map is not included, so the public projections do not
claim reconstruction of the complete raw receipts.

The common semantic output is included byte-for-byte. The reporting diagnostic changes only
the run-root prefix to `REPORTING_REGRESSION_RUN`; all thirteen source positions and messages
remain. Each conversion has its own record in `MANIFEST.json`. Historical reader sources are
exact bytes, not edited summaries.

`ARTIFACT_REHASH.json` records the read-only verification of every control and regression artifact
reference, input-byte capture and command record. Its artifact inventories describe retained
local run files, including compiled objects and hostile fixtures; they are not a claim that all
those files are included in this public packet. The complete raw local directories remain
retained. The original accepted source, contract, frozen judge and reproducible control definitions
are public in the standalone lane. This is a bounded evidence projection, not an external archive,
attestation, complete execution-environment snapshot or mathematical novelty claim.

Self-test receipts retain their original `pending` statements: complete candidate replay was
a separate obligation from those tests. The selected receipts and reporting receipt document
that separate execution; the self-test history has not been rewritten. Resource-control failures
inside deliberately hostile cases remain operational test outcomes, not mathematical
counterexamples. No new proof or checker execution was used to make this projection.
