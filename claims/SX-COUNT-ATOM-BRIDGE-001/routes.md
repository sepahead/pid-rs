# Route registry for SX-COUNT-ATOM-BRIDGE-001 revision 1

> **Historical superseded route registry.** These candidate states are retained for provenance.
> The active route registry is [`routes-v2.md`](routes-v2.md).

| Route | Family | Starting point | Strongest candidate result | State |
|---|---|---|---|---|
| R-PAPER | publication transcription | Makkeh–Gutknecht–Wibral categorical event, component, and Möbius formulas | fixed two-source Lean definitions and explicit paper/project boundary | preliminary reviewed transcription |
| R-EVENT | inherited formal semantics | arbitrary natural counts with positive total | exact node events, event counts, support positivity, and signed-net cumulative expression | complete dependency |
| R-MOBIUS | exact additive algebra | four cumulatives in an additive commutative group | concrete integer Möbius/zeta inverse, reconstruction, and source-labelled coordinate-swap equivariance | implemented candidate |
| R-AVERAGE | finite empirical linearity | positive-support empirical weights and local atoms | averaging pointwise atoms equals inversion of averaged cumulatives for all three components | implemented candidate |
| R-COUNT | exact supplied-count algebra | informative, misinformative, and net rational local arguments | every one of the 24 averaged coordinates equals its supplied-count expression | implemented candidate |
| R-PRODUCT | exact multiplicative algebra | positive exact rational local arguments | every coordinate is `(1/N) log R`; its sign and zero reduce to comparison of `R` with one | implemented candidate |
| R-KERNEL | formal replay | pinned Lean toolchain, imports, source, declarations, and axiom inventory | no accepted result yet | pending |
| R-VARIATION | semantic negative controls | type-correct statement/body/order/event/product changes | no accepted result yet | pending |
| R-RUST | implementation refinement | `pid-core` categorical row processing, `NODES2`, and `invert2` | semantic similarity only | open boundary |
| R-CERT | standalone exact evaluator | accepted table/report inputs and exact product logic | mathematical specification overlap only | open boundary |
| R-PDF | deterministic document rendering | this Markdown packet and LaTeX source | source drafted; no rendered artifact yet | pending |
| R-REVIEW | independent adjudication | complete candidate diff and rendered pages | no accepted result yet | pending |

`R-EVENT`, `R-MOBIUS`, `R-AVERAGE`, `R-COUNT`, and `R-PRODUCT` form one Lean dependency chain;
they are not five independent proofs. `R-PAPER` is a transcription boundary, not a machine-checked
derivation from publication text. `R-RUST` and `R-CERT` must not be described as closed merely
because their formulas look similar to the Lean definitions.

Acceptance requires the pending replay, semantic-variation, integration, rendering, and review
routes. The route registry does not infer component nonnegativity, executable equivalence,
statistical validity, or higher-source results from the candidate algebra.
