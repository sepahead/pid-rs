# Decision for `KSG-INTEGER-HARMONIC-001` revision 2

## Current adjudication

**GO to implement the evidence corrections; NO-GO to declare the claim complete.** The exact
positive-integer reduction remains accepted. The source-symmetric range association remains the
selected binary64 implementation on the frozen corpus. Revision 1's E4 and mutation/custody
wording cannot close until the newly named obligations replay on settled bytes.

**GO for scoped formal obligations `L1`, `Z1`, and `N1`.** Pinned Lean and independently encoded
SMT routes check the finite-sum recurrence and exact conditional cancellation/range/index
consequences with baseline-first negative mutations. This is not formal verification of analytic
digamma truth, neighbor geometry, binary64, Rust conformance, KSG consistency, shared-exclusions
PID semantics, support, calibration, or downstream readiness.

Do not silently update the reference constants. First retain the failure arrays, capture the
serial result without `parallel`, explain the one unchanged value and the PID2 dual provenance,
then require the parallel-enabled build to reproduce the same values.

## Permitted wording after closure

> Positive-integer coefficient-cancelling KSG and inventoried Ehrlich shared-exclusions count terms
> use an exact-real-equivalent, source-symmetric harmonic-range expression. On the frozen
> 8,198-cell corpus, the tested binary64 implementation has an observed maximum absolute error of
> eight epsilons in nats, attained by 40 cells, with `(4096,1,2048,2048)` first in corpus order,
> and has no count-swap bit asymmetry. A 32-epsilon finite-corpus gate is enforced.

This wording does not assert correct rounding, a universal error bound, KSG or shared-exclusions
estimator consistency, population support, calibration, compatibility with v3 values, formal
verification of the complete estimator, or downstream readiness.

## Required implementation order

1. Bind the fixture to live generator bytes and freeze the 40-way maximum tie.
2. Add marker-preserving shadow/overwrite mutations, while keeping the textual-evidence boundary.
3. Add the tiny KSG exact local witness and the paired ISX local/public propagation witness.
4. Make the frozen-reference test runnable without `parallel`; capture all 13 serial values.
5. Correct the 12 changed constants, leave the reviewed midpoint unchanged, and replay with
   `parallel`.
6. Run debug/release, feature, resource, Python, documentation, release, and identity gates on
   settled bytes.

The final replay in step 6 includes the normal and optimized Lean/SMT checkers and their mutation
self-tests listed in `formal-assurance-v2.md`.

## Re-adjudication triggers

Create revision 3 if the exact coefficient/index classification, prefix compensation, range
association, corpus membership/order, maximum-error criterion, tie multiplicity, shell rule,
support contract, runtime estimator identity, or affected release-family closure changes. A new
empirical corpus or floating target also requires a new bounded claim rather than silently moving
revision-2 constants.
