# Correction ledger for `KSG-INTEGER-HARMONIC-001`, revision 4

Revision 4 is post-result integration. These entries do not rewrite revisions 1--3.

## C10 — revision-3 packet was not in checker custody

- **Detector:** deletion/edit thought experiments and hostile preclosure audit.
- **Failure:** arithmetic/catalog routes could remain green after changing claim prose.
- **Correction:** canonical active manifest, exact file set/digests, regular non-symlink paths,
  exactly one active revision, semantic invariants, and hash-rebased semantic mutations.
- **Residual:** SHA-256/Git provide custody, not authenticity or scientific truth.

## C11 — revision index described revision 2 as active

- **Detector:** direct index inspection.
- **Failure:** live chronology disagreed with frozen revision-3 files.
- **Correction:** preserve the stale bytes as `revision-index-pre-v4.md`; make the live index name
  revisions 3 and 4 truthfully.
- **Residual:** historical revision-3 completion remained NO-GO and is not backfilled.

## C12 — formal bound exceeded the frozen revision-3 encoding

- **Detector:** theorem/source inventory comparison.
- **Failure:** revision-3 prose attributed `-D<=T<=D` to formal files that lacked monotonicity and
  bound theorems.
- **Correction:** retain revision-3 bytes; add a revision-scoped Lean v4 source with the rational
  monotonicity, rational bound, rational-to-real bridge, and combined real theorem; add a fourth
  premise-explicit Z3 obligation.
- **Residual:** digamma truth is typed; Z3 order facts are premises; no runtime proof follows.

## C13 — formal mutation inventory advanced

- **Observed:** Lean closes at 19 theorems/14 mutants; Z3 closes at four obligations/12 mutants,
  including reversal of the middle order premise.
- **Correction:** revision-4 documents bind these settled counts and source/checker hashes.
- **Residual:** mutation kills establish load-bearing checks, not theorem-to-program refinement.

## C14 — endpoint association wording omitted the prefix

- **Detector:** selected Neumaier prefix and naive prefix yield 150 and 121 ordinary-left nonzeros.
- **Correction:** every revision-4 statement says “selected-Neumaier-prefix ordinary-left” for
  150/354 and binds zero negative zeros.
- **Residual:** neither count is a universal signed-zero theorem.

## C15 — structural endpoint sufficiency was not a corpus-wide iff

- **Detector:** exact residue exploration.
- **Result:** each of three selected prime fields separates every one of the 7,844 frozen
  nonendpoints, while structural cancellation covers all 354 endpoints.
- **Correction:** promote only the **frozen-corpus** iff via the canonical modular certificate.
- **Residual:** no universal harmonic-zero classification is asserted.

## C16 — an earlier modular digest had no retained schema

- **Observed:** `1d5f61b...` described a pre-artifact computation but had no canonical retained
  byte schema.
- **Correction:** classify it as historical observation only. The then-canonical `ae4645c3...`
  artifact was superseded by C31; current certificate custody is
  `5c1923413edecb27bde19d388ab3365844e07bc0ba5f0fa9b28672053ef8901f`.
- **Residual:** internal generator/checker diversity is not external custody.

## C17 — zero-residue converse is false

- **Detector:** rejected prime `1000003`.
- **Smallest retained corpus witnesses:** zero-based indices 8045, 8049, 8069, and 8093 are exact
  nonzero but have zero residue.
- **Correction:** only nonzero residue implies exact nonzero; retain exact reciprocal-tail signs.
- **Residual:** selected primes are separators for this corpus, not CRT or a universal theorem.

## C18 — “eight” had two incompatible numerical meanings

- **Failure:** `8*EPSILON` absolute nats and W2's eight integer-encoding steps could be conflated
  with ULP claims.
- **First correction:** called the former absolute error in nats and the latter
  ordered-binary64 positions.
- **Later detector:** C20 showed that “absolute error” still omitted the rounded-reference
  qualifier and therefore conflated two reference objects.
- **Current boundary:** use “binary64-rounded-reference comparator” for the 8-epsilon quantity;
  neither numerical observation is a ULP or universal estimator-error theorem.

## C19 — bounded core GO could be mistaken for release GO

- **Detector:** two-parent/dirty-tree phase audit.
- **Correction:** status is `integration_no_go`; catalog, release, audience, identity, phase,
  compiled/full CI, hostile review, final evidence matrix, decision, commit, and receipt remain
  open.
- **Residual:** no ambient-tree green run can close an isolated-candidate obligation.

## C20 — two Decimal-reference error metrics were conflated

- **Detector:** separate hostile replay with `Decimal.from_float` and exact harmonic rationals.
- **Failure:** `8*EPSILON` with 40 ties compares the selected value with each Decimal string only
  after the string is rounded to binary64; it is not error against the stored Decimal or exact
  rational.
- **Correction:** retain that comparator under its full name. Add a separate 160-digit directed
  enclosure: the exact-rational maximum is unique at row 7673 and is below
  `9.761311*EPSILON`; both remain below the 32-epsilon corpus ceiling.
- **Negative result:** 6,509 stored strings differ textually and 5,934 values differ numerically
  from exact-rounded 80-digit references, although zero pairs differ after binary64 conversion.
- **Hardening:** exact `Fraction` containment checks all 6,920 exhaustive rows; the strict epsilon
  comparison uses a downward-rounded threshold; 29/29 registered checker mutations die in normal
  and optimized Python.
- **Residual:** Decimal directed-rounding semantics are a computational premise; neither metric
  is universal, portable, or an estimator theorem.

## C21 — marker presence admitted contradictory claim prose

- **Detector:** leaf-plus-manifest reseals that preserved positive markers while appending an MGW
  theorem or integration GO, or changing W1's index base and the selected endpoint count.
- **Failure:** the preclosure claim route accepted all four contradictions.
- **Correction:** bind exact reviewed revision-4 prose digests, exact high-risk statements,
  forbidden promotions, and all four hostile mutations.
- **Residual:** automated prose checks are a fail-closed reviewed-byte/statement envelope, not
  general natural-language theorem proving.

## C22 — JSON equality and claim-tree inventory were not strict

- **Detector:** resealed `4.0`, `true`, and `0` substitutions plus root, nested, case-variant, and
  symlink active-packet aliases.
- **Failure:** Python equality conflated boolean, integer, and float values; only the active rows
  inside one manifest were counted.
- **Correction:** recursively require exact JSON scalar types and exact values. Recursively require
  equality with the complete mapped claim-tree file/directory inventory, rejecting every symlink
  and case/Unicode-normalized collision.
- **Residual:** this establishes local tree custody, not external authenticity.

## C23 — binary64 signed-zero and naive-prefix facts were not derived

- **Detector:** a checker mutation forced every selected exact zero to negative zero without
  failing; the naive-prefix 121 count had no executable route.
- **Correction:** derive endpoint selected `+0/-0/nonzero = 354/0/0`, full-corpus selected
  `+0/-0/nonzero = 354/0/7844`, selected-prefix
  ordinary-left `nonzero/-0 = 150/0`, and separately constructed naive-prefix
  `nonzero/-0 = 121/0` in both Python and compiled Rust. Bind the 32-epsilon ceiling to its
  reviewed multiplier.
- **Residual:** signed-zero counts are structural regression tripwires, not independent scientific
  validation.

## C24 — the default checker could become green during preclosure

- **Detector:** simulated closure of the only incidentally red catalog route.
- **Failure:** the generic default route then exited successfully while status remained
  `integration_no_go`, 13 gates were open, and final artifacts were absent.
- **Correction at that revision:** the default route had to exit nonzero with the exact lifecycle
  status and open-gate count during preclosure; scoped routes remained diagnostic.
- **Supersession:** C46--C48 removed the speculative positive final parser entirely. The current
  checker now rejects its default route unconditionally and accepts only scoped preclosure routes.
- **Residual:** a future final status depends on a separately reviewed versioned M1c checker plus
  the phase, build, review, identity, Git, release, and hosted evidence named by that lifecycle.

## C25 — marker bags are not semantic correspondence

- **Detector:** nine active documents were replaced by fenced bags of every required substring;
  leaf hashes and the manifest pin were rebound.
- **Failure:** claim-only passed even though every argument had been erased. Rephrased
  contradictions, decoy domains, a weakened bound, and changed endpoint counts also survived.
- **Correction:** the typed fact object remains the machine authority, while every reviewed active
  scientific document is bound by its complete SHA-256 bytes separately from the packet
  envelope. Substring/forbidden markers remain only supplemental tripwires.
- **Residual:** exact reviewed-byte custody is not natural-language theorem proving. A malicious
  simultaneous rewrite of checker, facts, and prose remains a code-review/Git/receipt boundary.

## C26 — permissive fixture parsing and aggregate NaN silence

- **Detector:** resealed schema `2.0`, boolean counts, row swaps, unknown/duplicate keys, three NaN
  spellings, and a selected-helper NaN all passed the old binary64 aggregate.
- **Correction:** require canonical duplicate-free JSON, exact keys and scalar types, separately
  reconstructed ordered rows, canonical finite Decimal strings, finite actual/swapped/error
  operands, the fixed fixture digest, and the pinned generator's no-write exact-output replay.
- **Residual:** same-repository source/digest/replay is internal correspondence, not authenticity;
  Python/Rust compiled correspondence remains a separate gate.

## C27 — final lifecycle constants admitted hollow GO

- **Detector:** simulated `integration_go` plus zero gates retained the preclosure stage and used
  two one-line final files; claim-only passed and printed stale hardcoded counts/status.
- **Correction at that revision:** admit only the exact `(NO-GO, preclosure stage)` or `(GO,
  immutable-final stage)` tuple and derive success output from accepted data.
- **Supersession:** C46--C48 later showed that the proposed final tuple still admitted
  cross-subject, fabricated-output, profile, and review conflations. The speculative final parser
  and positive fixture are now removed in full; only the scoped preclosure tuple remains.
- **Residual:** final authority requires a separately reviewed versioned M1c checker after the real
  M1a receipt exists. This historical correction is not evidence that such a checker exists.

## C28 — default lifecycle rejection was defined but not exercised

- **Detector:** the self-test helper had zero call sites.
- **Correction:** baseline-test the exact default preclosure failure in normal and optimized
  Python and kill removal of the default lifecycle call.
- **Residual:** the current checker intentionally has no final positive transition. C46--C48
  supersede the earlier anticipated route; dummy final artifacts remain negative controls only.

## C29 — external hardlink aliases escaped the tree inventory

- **Detector:** `claim-v4.md` was replaced by a same-byte hardlink to an untracked external path.
- **Correction:** every packet file, including the manifest, must be a regular non-symlink with
  link count one in the checked worktree.
- **Residual:** link-count custody is a live-filesystem check, not a Git-object authenticity or
  atomic-snapshot theorem.

## C30 — endpoint terminology and an alleged `k+1` lower bound were false

- **Detector:** fresh hostile review of the anchor-inclusive Ehrlich count bridge.
- **False allegation:** counting the anchor and `k` joint neighbours forces `n_alpha >= k+1`.
- **Exact counterexample:** with `n=3,k=1`, target `[0,0.4,0.8]`, source 1 `[0,1,3]`, and source 2
  `[0,10,30]`, query row 0 has a unique joint shell at radius `1` and attains
  `(n_alpha,n_t)=(1,3)=(k,n)`. All coordinate values and all three query shells are unique.
- **Correction:** add private count/radius/positive-zero and public preflight/mean compiled
  regressions; distinguish structural-zero count endpoints from the `+D/-D` range extrema.
- **Negative result:** the proposed universal absolute non-structural gap
  `|T|>=1/(n-1)` for nonzero `T` is false: `(n,k,x,y)=(8,2,3,5)` gives
  `0<|T|=1/105<1/7=1/(n-1)` exactly.
- **Residual:** a finite accepted all-unique sample does not prove the population support
  declaration, and this witness does not validate the estimator or PID functional.

## C31 — “maximum harmonic denominator” named the wrong object

- **Detector:** modular-route hostile review.
- **Failure:** the reduced denominator of `H_999999` is not `999999`; the phrase could be read as
  asserting that false statement.
- **Correction:** name the bounded object exactly: the maximum reciprocal summand
  denominator/index is `999999`, so every `j` in `1/j` is nonzero modulo each prime greater than
  `999999`.
- **Machine hardening:** rename the generator/checker constant, certificate key, and per-prime
  predicate; regenerate canonical certificate
  `5c1923413edecb27bde19d388ab3365844e07bc0ba5f0fa9b28672053ef8901f`;
  kill the stale object name and a removed invertibility premise, raising the modular suite from
  26 to 28 mutations in both normal and optimized Python.
- **Residual:** invertible summands prove only that the modular image is defined. Exact nonzero
  follows from a nonzero residue; zero residue still has no converse without a separate argument.

## C32 — rectangular sharpness was conflated with runtime-shell attainability

- **Detector:** separate count-set review after W2b corrected the inclusive lower endpoint.
- **Failure:** the arithmetic box `k<=x,y<=n` was called estimator-facing and its `-D` endpoint was
  described beside runtime mappings without saying that shell geometry can forbid box tuples.
- **Exact separator:** at `n=2,k=1`, helper tuple `(x,y)=(2,2)` gives `-D=-1`, but a unique shell
  has strict marginal sets `A,B` with `|A intersect B|=k-1`, hence
  `x+y=|A union B|+k+1<=n+k=3`; `(2,2)` is not runtime-realizable.
- **Correction:** call `[-D,D]` sharp only on the rectangular arithmetic domain and deny runtime
  attainability transfer.
- **Follow-up hypothesis:** the same set map suggests `x+y<=n+k` and, with
  `a=floor((n+k)/2)`, `b=ceil((n+k)/2)`, the stronger runtime lower bound
  `H_(k-1)+H_(n-1)-H_(a-1)-H_(b-1)`. Neither is promoted here until exact, formal, compiled,
  mutation, and provenance routes close.
- **Residual:** a valid arithmetic theorem can still be wrong for an estimator if its domain is
  treated as an exact image rather than an outer approximation.

## C33 — compiled Rust did not directly scan the full output partition

- **Detector:** hostile source-to-claim correspondence review of C23 against the Rust corpus test.
- **Failure:** C23 said Python and compiled Rust both derived full-corpus
  `+0/-0/nonzero = 354/0/7844`, but Rust classified only endpoint zeros and association
  diagnostics. The full nonzero partition was inferred rather than counted in compiled code.
- **Correction:** retain C23 as chronology and add direct compiled classification of every one of
  the 8,198 selected helper outputs. Require finite rounded-reference, selected, and source-swapped
  operands before classifying exact binary64 bits, then assert `354/0/7844`.
- **Residual:** the Rust and Python routes share fixture rows, endpoint rules, and the selected
  helper. Their agreement is fixed-corpus correspondence, not an independent mathematical proof
  or a universal nonzero theorem.

## C34 — raw SMT substring checks admitted grammar and snapshot false greens

- **Detector:** adversarial grammar, command-tail, wrong-theorem, and file-replacement
  counterexamples against the Z3 checker.
- **Failure:** raw substring exclusions admitted unsupported forms after whitespace changes or
  `exit`, and the checker reopened a path after validation. After rebasing raw hashes, a
  type-correct but wrong theorem could retain the old positive/negative solver outcomes and all 12
  registered semantic-countermodel outcomes.
- **Correction:** use a bounded ASCII lexer and S-expression parser; require the exact ordered
  command/declaration/sort/operator/arity/terminal profile for each obligation; pin both raw bytes
  and a whitespace/comment-insensitive token stream; read and validate all four snapshots before
  launching the solver; derive the positive formulation only from the validated in-memory
  negative snapshot; and send exact bytes over standard input without reopening source paths.
- **Control separation:** the retained 12 cases are solver-semantic countermodels. Parser limits,
  forbidden commands, profile/type failures, token pins, snapshot replacement, transport, and
  solver-failure cases form a separate 52-control firewall: 16 lexer/parser, 25 profile/type, and
  11 custody/transport/result controls. Normal and optimized checker/self-test replay is
  byte-identical green. These are not additional theorem mutations.
- **Retained adequacy boundary:** a well-typed wrong theorem with SHA-256
  `88e67f4289caf81770c9457d3ac77de4f470fe56d8bf3eb0a8139ac42c23ec52`
  and token-stream SHA-256
  `f8c8334b0cd73a55072e833463ae6ec43bd0f6042c0f7e888eff01b8f75caa8e`
  still gives positive `sat`, negated `unsat`, and all 12 old mutant `sat` results after deliberate
  dual-pin rebase.
- **Residual:** raw and token pins are correlated custody of the same source, and a bounded
  type/profile checker does not establish that the chosen theorem is the intended theorem.
  Simultaneous checker/source resealing remains a Git, receipt, and human-review cut.

## C35 — modular certificate equality admitted Boolean/integer coercion

- **Detector:** strict-JSON hostile controls after certificate, sidecar, and checker-digest rebase.
- **Failure:** Python scalar equality treated `certificate_revision: 1 -> true` and
  `residue_encoding.include_zero_residues: true -> 1` as equal, so both changed certificates
  passed the former modular checker in normal and optimized execution.
- **Correction:** compare static certificate sections and replayed selected/rejected records using
  recursive, path-aware equality of JSON shape, scalar type, and value. Keep the two type-firewall
  controls separate from the 28 registered modular scientific/custody mutations. Normal and
  optimized replay rejects all `2/2` firewall controls while retaining `28/28` registered
  scientific/custody kills.
- **Related boundary:** the phase policy had the same local scalar weakness. Its corresponding
  schema/authority helper probes reject `2/2` controls in normal and optimized Python and remain
  separate from the existing phase attacks. A one-digest phase repin was not a demonstrated
  whole-phase false green because changed-projection custody already rejected it; the full phase
  suite remains open pending policy/generated-fact reseal.
- **Residual:** strict local JSON typing closes these coercions. It does not authenticate the
  certificate, prove the residue algorithm, or make repeated computations independent.

## C36 — Decimal difference ordering relied on implicit context exactness

- **Detector:** exact `Fraction` reconstruction of every stored-versus-exact-rounded Decimal pair.
- **Failure:** the reported `8.18e-77` maximum was correct on the observed runtime, but Decimal
  subtraction and ordering did not carry an explicit exactness guard. That left an unnecessary
  precision-context premise in the maximum comparison.
- **Correction:** first require finite canonical Decimal operands, then convert each operand
  exactly to `Fraction` and order exact absolute differences. The unique maximum is
  `818/10^79 = 409/(5*10^78)` at row 7952; `8.18e-77` is its bound presentation, not the authority.
  Normal and optimized replay rejects `2/2` exact-comparator controls separately from the existing
  `29/29` exact-enclosure mutations.
- **Residual:** this makes only the comparison of the 8,198 finite Decimal operands rational-exact.
  The 1,278 stress-row harmonic enclosures still rely on Python Decimal directed-rounding
  semantics; the route is not a universal precision or implementation-refinement theorem.

## C37 — implementation purity was called statistical independence

- **Detector:** statistical-language review of the data-parallel Ehrlich local-term comment.
- **Failure:** “each point is independent” could be read as asserting independent observations,
  although local contributions reuse the sample and can be statistically dependent.
- **Correction:** state only the implementation property: each index mutates its own scratch and
  no cross-index state, results are collected in index order, and serial/parallel reduction is
  deterministic. Explicitly deny any statistical-independence inference.
- **Residual:** bit-identical parallel execution is a reproducibility property. It proves neither
  IID sampling nor validity of an estimator, uncertainty interval, or PID functional.

## C38 — W1/W2 helper tuples did not machine-label their field order

- **Detector:** packet/prose tuple-order comparison.
- **Failure:** the values `(2,8,5,2)` were correct, but a generic `helper_arguments` key allowed
  readers or machines to confuse helper call order `(k,n,x,y)` with row-description order
  `(n,k,x,y)`.
- **Correction:** label W1 and W2 fields as `helper_arguments_k_n_x_y`; in prose write
  “helper call order `(k,n,x,y)`.” Use separately named fields for any `(n,k,x,y)` tuple.
- **Residual:** labels prevent this permutation ambiguity; they do not prove that runtime count
  diagnostics were mapped correctly.

## C39 — the rejected modular collision mechanism and field correlation were unstated

- **Detector:** first-principles finite-field replay of the rejected prime.
- **Derivation:** for an odd prime `p`, pairing reciprocal terms `r` and `p-r` gives
  `H_(p-1-t)=H_t mod p`. At `p=1000003`, `999999=p-4`, hence
  `H_999999=H_3 mod p`; the four retained rows are signed copies of that one event.
- **Correction:** record the elementary reflection identity and do not describe the selected fields
  as structurally independent. Index 33 occurs in the corpus, and absence or presence of a
  reflected index alone is not a proof of separation.
- **Residual:** the exhaustive canonical certificate, not an index-co-occurrence story, is the
  authority that each selected field separates all frozen nonendpoints. Reflection explains the
  rejected collision but supplies no universal zero classification.

## C40 — a provisional checkpoint was mislabeled as the canonical M1a anchor

- **Detector:** chronology audit across the durable resume record, implementation bytes, and
  decision-authority ordering.
- **Failure:** an older progress section called
  `afc45ff27e5af7fe04e44f2bb9f4147fb472c81e` the M1a implementation commit. It predates the current
  repaired revision-4 candidate and cannot be its canonical implementation anchor.
- **Correction:** classify that commit as a provisional historical checkpoint. Canonical M1a is a
  future unsigned settled-implementation commit pushed and remotely verified while integration
  remains NO-GO. Only a separate descendant/re-anchored M1c milestone may add the immutable
  evidence matrix, decision, and final receipt.
- **Residual:** M1a and M1c order provenance and prevent decision authority from preceding its
  implementation. Neither Git milestone makes a scientific claim true.

## C41 — the runtime count constraint advanced from hypothesis to conditional lemma only

- **Detector:** explicit set reconstruction for both exclusive and anchor-inclusive mappings.
- **Derivation:** with a finite positive max-product radius and an unambiguous kth shell, let
  `A,B` be the two strict-radius marginal sets on the common rows. Exclusive KSG has
  `|A intersect B|=k-1`, `|A union B|<=n-1`, and `(x,y)=(|A|+1,|B|+1)`;
  anchor-inclusive Ehrlich has `|A intersect B|=k`, `|A union B|<=n`, and
  `(x,y)=(|A|,|B|)`. Inclusion--exclusion gives `x+y<=n+k` in either case.
- **Constrained consequence:** harmonic prefixes increase, so a maximizer saturates
  `x+y=n+k`; harmonic increments decrease, so at that sum
  `H_(x-1)+H_(y-1)` is maximized by the balanced pair. This yields the sharper candidate lower
  bound using `floor((n+k)/2)` and `ceil((n+k)/2)`, sharp on the constrained integer outer domain.
- **Correction:** record the result as a conditional source lemma and constrained arithmetic
  candidate, not as a revision-4 promoted theorem.
- **Residual:** exact source refinement, formal proof inventory, compiled witnesses, semantic
  mutations, provenance, and runtime attainability of the balanced pair remain open.

## C42 — the composite-modulus mutation stopped at the small-prime prefilter

- **Detector:** independent hostile review of primality-path mutation coverage.
- **Failure:** composite modulus `1000035` is divisible by 5. Its rejection tested the small-prime
  prefilter but did not enter the deterministic u32 Miller--Rabin witness loop.
- **Correction:** replace that mutation, without increasing the registered count, by
  `1000001=101*9901`. It remains above the maximum reciprocal index `999999` and is not divisible
  by the prefilter primes 2 through 37, so the expected `is composite` rejection exercises the
  witness loop. Normal and optimized replay retain `28/28` registered mutation kills.
- **Residual:** one composite witness is path coverage, not a universal proof of the primality
  implementation. Generator trial division and checker Miller--Rabin remain correlated arithmetic
  code under the same selected-modulus specification.

## C43 — phase self-custody was mistaken for an internal property

- **Detector:** independent coordinated policy/checker rebase after the policy-only advisory
  mutations passed.
- **Retained negative result:** the phase checker and its self-test are deliberately excluded from
  the digest block stored inside the checker. Reclassifying one Fable record as claim evidence,
  repinning the policy digest, weakening the checker predicate, and regenerating its mechanical
  facts therefore passes the internally modified checker. A tree and checkpoint generated after
  that mutation also pass. This is the unavoidable self-reference cut, not evidence that Fable
  output became scientific authority.
- **Correction:** pin the exact 14-file advisory inventory and exact advisory-only contract so a
  policy-only rebase fails. Retain a separate executable boundary control that demonstrates the
  coordinated acceptance, the post-hoc-tree acceptance, and rejection by a pristine tree pinned
  before the mutation. M1a closure must use an independently recorded exact alternate-index tree
  and detached checkpoint; ordinary `candidate-tree=not-requested` output is not closure evidence.
- **Residual:** an internally or post-hoc generated tree supplies consistency, not trust. The
  reviewed tree/checkpoint, eventual unsigned commit, remote observation, and human review anchor
  the two self-unhashed scripts. These custody layers are not authenticity, arithmetic, estimator,
  PID, or application proofs.

## C44 — the strict-radius test did not bind the immediate predecessor

- **Detector:** exact binary64 mutation replay at a predecessor-adjacent marginal distance.
- **Failure:** the prior test required only `0 < strict_radius(1.0) < 1.0`; moving two
  representable predecessor steps satisfied it and changed a production KSG count.
- **Correction:** require exact `to_bits()-1` at `1.0`, require the smallest positive subnormal to
  map to positive zero, and replay pair/xblocks fixtures in both marginal orientations under
  forced brute and kd-tree backends. The fixtures distinguish the correct count map's exact-real
  `5/6` target from the two-step predecessor's `11/6` and identity radius's `-1/6`; production
  selected bits are asserted separately and are one ordered position below correctly rounded
  exact-real `5/6`.
- **Residual:** this finite Rust binary64/call-site witness is not a universal nextafter,
  neighbor-geometry, backend-equivalence, support, consistency, or calibration theorem.

## C45 — backend overflow tests admitted unequal error classes

- **Detector:** structured error-variant comparison for a finite-coordinate span whose subtraction
  overflows binary64.
- **Failure:** brute checked distance returned `NumericalInstability`, while kd-tree build returned
  `NonFiniteInput`; the old parity test accepted any two errors.
- **Correction:** return `NumericalInstability` from the kd-tree span check and assert that exact
  variant on both routes. Diagnostic contexts remain route-specific.
- **Residual:** matching this error class does not prove numerical equality, identical payloads,
  resource behavior, or all-error parity.

## C46 — final gate paths were not content-bound to their gate IDs

- **Detector:** dormant final-authority substitution and duplication attacks.
- **Failure:** a candidate dormant final parser could map all 13 gates to one arbitrary packet
  file; it checked only path, digest, and status, while `implementation_commit` was only a
  hex-shaped string.
- **Correction:** remove the entire speculative final-authority parser and its positive synthetic
  fixture. The revision-4 checker now accepts only the exact preclosure tuple on scoped diagnostic
  routes and deliberately fails its default route while any integration gate is open. A future
  M1c checker must be a separately reviewed versioned artifact built after the real M1a receipt
  exists.
- **Residual:** removing a false-green route does not close any integration gate. No typed final
  receipt, evidence matrix, decision, independent review, arithmetic result, estimator result, or
  hosted-finalization claim follows from this correction.

## C47 — one subject could not type implementation, integration, and finalization

- **Detector:** an adversarial synthetic direct child whose declared integration subject reused
  the pre-implementation anchor tree while later checkout bytes retained the implementation and
  final artifacts.
- **Failure:** the candidate dormant design labeled M1a phase custody, settled integration
  execution, and later claim/decision finalization with one `subject_commit`. It could therefore
  accept a subject that did not contain the implementation being checked, while final-only routes
  necessarily executed on different descendant bytes.
- **Correction:** remove that design rather than weakening its claims. Any future lifecycle must
  type the M1a implementation anchor, settled integration-execution subject, and later
  finalization/observation descendant separately; bind every permitted inter-subject byte delta;
  and keep a containing commit from claiming hosted validation of its own not-yet-existing bytes.
- **Residual:** the current preclosure checker has no positive M1c path. A real M1a receipt and a
  later acyclic execution/observation sequence remain required.

## C48 — self-reported output was not gate execution or independent review

- **Detector:** fabricated hosted JSON, duplicated debug-profile output, inconsistent hostile-test
  totals, and substitution of normal/optimized mutation runs for source-blind and proof-blind
  reviews.
- **Failure:** the candidate dormant parser accepted locally fabricated `success` metadata,
  profile-untyped test summaries, incoherent mutation counts, and no retained role-specific review
  artifacts. Those records could not establish `settled_full_ci`, debug/release/feature coverage,
  or the two required external review classes.
- **Correction:** delete the generic gate-observation/receipt machinery and keep all 13 gates open.
  A future final route must retain and parse exact run/job/ref/repository evidence, bind explicit
  command profiles and coherent inventories, and carry distinct source-blind and proof-blind
  review artifacts with their visibility, disposition, independence, and nonclaim boundaries.
- **Residual:** even provider-observed metadata is transport evidence rather than authenticity,
  scientific validity, or independent semantic review; those cuts must remain explicit.
